# First engagement workflow

User-authorized implementation begins from `398d8f0d` in isolated branch `work/engagement-first-workflow`. Another OpenPlan session is open in main; do not change main or operate its processes while acceptance may be active.

## Required outcome

Complete project-specific setup with editable templates and blank setup, retained published definitions, dates, reviewed layers and preview. Complete point/route/area/non-map participation, durable draft/retry receipts, complete feeds/threads. Human review must retain restricted originals, reasons and immutable history, reject stale changes and ambiguous bulk selection. Reports must produce PDF/XLSX/portable companions from one immutable scoped snapshot through recoverable workers and authenticated storage.

## Verification still required

Two explicitly labelled demonstrations: complete streets and countywide wayfinding. Large multilingual campaign with historical definitions, repeated/missing answers, photos, threads, redactions and formula-like text. Retry/concurrency/privacy/restart/cancellation/revocation checks; independent export reconciliation, every PDF page viewed, LibreOffice workbook opening/recalculation; identified desktop/390px keyboard/non-map journeys and actual download. Changed guards require no-op survivors and targeted consequential failures. Applicable CI/live RLS/additive upgrade checks precede landing.

Human usability observation, competitive evidence, advanced synthesis, decision follow-through and the separate v0.44 release gate remain open unless separately proved.

## Initial findings

- Direction check passes; main CI, RLS and dispatched upgrade run are green at inspection.
- Public portal read caps approved contributions at 200.
- Item PATCH changes content/status without expected-version comparison or immutable originals/history.
- Submission endpoint returns an identifier but shared browser submit helper drops it; retry relies on recent duplicate detection.
- Existing templates, geometry controls, survey definition snapshots, Reports and document export worker will be extended.

## Recovery checkpoint, 2026-09-06 21:56 Pacific

Implementation remains **in progress**, not a completed acceptance or released version. All edits are in the isolated worktree `/home/nathaniel/.local/state/openplan/engagement-first-workflow-2026-09-06`; main has not been changed.

- Added six `2026090800000*` migrations for original/review custody, immutable configuration versions, public-copy guards, leased report snapshots/delivery and atomic survey receipts/review. They are installed only in `supabase_db_openplan-engagement-verification` (API 56321). Initial numbering conflicted with OWP; renamed before installation. A first empty-stack migration missed a runtime reference to nonexistent `place_bbox`; corrected to the actual four place columns and exercised on live campaigns.
- Existing document worker now also prepares immutable campaign PDF/XLSX/ZIP jobs, stores checksummed bytes and exposes authenticated streaming downloads through Campaign and Reports. Public copies are rechecked against current publication/redactions at download. Actual file delivery and worker interruption remain unverified.
- Campaign categories are editable; dates, retained published definitions and translations, submission retry IDs, local draft/receipt recovery, scoped feeds/threads, conditional moderation, immutable history and survey review are implemented. Remaining implementation review includes inherited map framing, reusable custom setup, multilingual receipts, read/filter pagination, and recovery edge cases.
- Focused integration suite: 9 files / 56 tests passed after fixing adapters and audit coverage. Earlier full suite: 1,171 files passed; 15 failed (32 tests). Several failures were subsequently fixed; **the full suite has not been rerun green**. Type checking passed before the latest UI recovery changes; rerun required. Schema subset: 4 passed, one stale column-ratchet entry subsequently removed; rerun required.
- SQL custody probe and harmless/targeted mutations are in `evidence/`. They established immutable originals/definitions/snapshots, mandatory review reasons, scoped public snapshot and anonymous/cross-workspace denial on a disposable rollback fixture. These do not establish all privacy routes, live upgrade or usable files.
- Two explicitly labelled demonstration projects/campaigns were created through normal project→Engagement navigation. Dates saved through the browser. Desktop setup inspected. No public participation or export journey is yet accepted. A selector initially matched the floating selection heading before navigation finished; corrected to wait for the detail URL and content. Earlier shell captures are not acceptance evidence.
- Private scratch evidence and browser scripts: `/home/nathaniel/.local/state/openplan/engagement-evidence` (contains disposable account credentials/auth; never commit). Chrome profile `~/.config/google-chrome-automation/engagement-first-workflow`. Dev server 3225 uses this checkout and isolated stack; Documents worker PID 4066929 was started by this session and needs restarting for latest source. Logs `/tmp/engagement-*.log`. Never print env files or Supabase status credentials. Initial startup log accidentally exposed disposable local-stack credentials; user informed; shared secrets unaffected.
- Next: complete demo setup/public participation/moderation and actual queued report delivery; large labelled multilingual fixture; privacy/concurrency/recovery mutations; PDF every-page inspection and LibreOffice recalculation; full required checks and honest roadmap/ledger/issues update. Preserve the separate human/comparative and v0.44 gates.

## Recovery checkpoint, 2026-09-06 22:35 Pacific

Checkpoint `108b46fd` was pushed on the isolated feature branch. Work remains in progress; do not merge or declare M9a accepted yet.

- Both normal demonstration campaigns were configured through project navigation, dated, given sourced study geography, published questions and an explicitly illustrative reviewed route layer, then opened publicly. Browser submissions returned stored receipts for keyboard Point, LineString and Polygon after draft reload, and a text-only location with no geometry. Database values matched those four paths. Pending review and staff journeys still need completion. Mobile automation initially missed the collapsed participation sheet; opening its real handle resolved the selector failure.
- Labelled synthetic stress campaign has 1,005 contributions, 905 public approved copies, 100 pending, 20 unavailable legacy versions, historical/revised definitions, a photo, a thread, long multilingual text, repeated answers across respondents, missing answers and redactions. The database correctly refused an attempted duplicate answer within one session; the fixture was corrected to repeat across sessions.
- Worker delivered two immutable scope snapshots. Fresh authenticated Chrome downloaded all six PDF/XLSX/ZIP files after the new access policies. Public PDF 219 pages; internal 236 pages. Root viewed all public contact sheets; independent reviewer B viewed all internal contact sheets plus nine individual pages. These are pagination/layout inspections, not line-by-line language validation. Original workbook copies opened and rewrote through LibreOffice; independent value reconciliation remains required. Persistent automation profile downloads failed with context closure; fresh Chrome passed without disabling download protection. The retained legacy PDF/ZIP failure is still a separate unclosed check.
- Independent protocol reviewers started from `/tmp/engagement-direction-packet.md`, with no shared conclusions. Reports `/tmp/engagement-independent-review-{a,b}.md` are initial in-progress inspections and must be preserved unchanged. They found stale photo cache after removal/replacement, legacy coordinate omission from GeoJSON, historical answers using current scales, public historical labels, reuse language/accessibility omissions and report navigation weaknesses. Corrections are being implemented and require regression/mutation/live evidence. Review B reports no gross PDF clipping but the long response lacked continuation IDs.
- Added migrations 7 (definition-only reuse) and 8 (direct snapshot/storage and internal Reports access restrictions), installed in the isolated stack. Revisions to original unreleased migration functions were applied there without resets. Latest reuse language/accessibility fix needs its CREATE OR REPLACE applied before live reuse verification.
- Current source additionally retains inherited study framing, source-bound public-report revocation checks for survey answers/parents/staff responses, and checks campaign files when copied into a project evidence bundle. These privacy boundaries still require full direct-query/HTTP/storage/revocation tests. Reports from old snapshots are immutable; create new jobs to verify renderer revisions.
- Latest uncommitted report edits add shared legacy geometry normalization, question/session/repeated/redacted/unanswered counts, linked contents and bounded long-text pieces with record IDs. Latest uncommitted aggregate reader groups by immutable version and refuses interpretation for unknown legacy definitions. New regression tests are running. Type checking passed before these last changes; rerun required. Full lint/tests/QA/live-upgrade are not yet green.
- Own dev server 3225 remains on this checkout and isolated API56321. Worker restart session72651 is the most recent known worker; inspect actual process cwd before restarting it. Private scratch evidence remains outside git. Main unchanged.

## Recovery checkpoint, 2026-09-06 23:01 Pacific

**Acceptance remains incomplete. Serious artifact defects were found and are being repaired.**

- Independent reviewer B found revised ZIP snapshot bytes corrupted at a UTF-16 chunk boundary: one emoji became two replacement characters. Both revised ZIP snapshot hashes failed their declared custody hashes. Root's initial reconciliation failed to compare the extracted hash with the declaration; this error was reported prominently. A new unit fixture reproduces corruption before the fix. All text is now explicitly encoded to UTF-8 buffers before JSZip; renderer, cached worker files and ZIP downloads verify extracted snapshot bytes and manifest members. A damaged download moves the job to failed so normal retry can repair the retained snapshot. These latest changes need live same-snapshot retry and final file verification.
- Independent LibreOffice verification poisoned four summary formula caches with 987654321, confirmed those values loaded, then forced calculateAll. Public totals became 905/11/10/1 and internal 1005/12/11/1. Exact long text survived, but multiline companion rows and Read me cells clipped visually. Renderer now splits text by conservative wrapped-line count and explicit newlines, with row heights based on those lines; reviewer is checking whether Calc also needs explicit style index 0. Latest files still need regeneration. Originals and failing evidence remain intact.
- Revised PDFs were independently inspected across all public241 and internal258 pages. They now contain question summaries, contents links and record/part IDs on long-text pieces. Final wording/layout correction may require bounded reinspection. Independent artifact addendum is being written under `/tmp/engagement-review-b-artifact-addendum.md`, scratch `/tmp/engagement-review-b-artifacts/`; reviewer B remains active. Initial A/B reports were copied unchanged into this directory.
- Normal UI queueing exposed a route parameter typo (`prequest` instead of `p_request`) missed by direct database-function tests. Fixed and covered by a route assertion and real UI202 queueing. Both revised jobs completed and six files arrived in fresh authenticated Chrome. Job IDs are in private `revised-stress-jobs.json`. Original `stress-jobs.json` stays unchanged. Original public job was intentionally withdrawn after a reviewed-text correction during access verification.
- Live access probe verified denied staff direct snapshots/storage/signed URLs, anonymous jobs, viewer internal Reports/artifact metadata, revoked viewer access, positive authenticated stream and 404 withdrawal after changed reviewed text. The initial anonymous test assumed empty rows but the database correctly returned permission denied; the probe now accepts only explicit42501 or empty successful rows. Setup reuse returned one private draft across retries and copied zero items/sessions/staff responses. New clone remains a labelled disposable demonstration.
- Browser staff journeys from Projects→Engagement reviewed all four demo inputs, approved three complete-streets examples and withheld the countywide area with reasons, loaded original definitions/history, and refused every replayed stale payload with409. Desktop/390px screenshots, no page errors and no horizontal overflow recorded. New map→detail selection now uses the existing map, scoped to the current page; this last link needs browser verification. Rendering fixes for restored Point/LineString/Polygon drafts were verified against stored geometry types. Photo remove/replace retry and disabled-storage participation tests pass.
- Full diagnostic suite before the last fixes: 1,179files passed,8failed,14skipped;13,156tests passed,10failed. Subsequently five migration/access/UI-fixture files70tests passed, and new renderer/route/survey37tests passed. Remaining full-suite issues include dated direction binding, copy ratchet (two extra record terms; one later changed to Give a reason), and whatever the latest changes expose. Latest lint passed before recovery-copy edits. Typecheck last failed only an invalid Testing Library exact option, now removed; next result pending.
- Original eight unit mutations reran successfully (one harmless survivor, seven consequential failures). Latest ZIP, photo, aggregation, policy-body, scoped-map and summary guards still need their own harmless/specific mutations. The original mutation script long-text string anchor is now stale after line-aware splitting; update the mutation to remove parts, never weaken the test.
- Worker session16918 was started from this checkout after verifying and terminating our own PID85496. It predates the ZIP and workbook fixes and MUST be restarted for repair tests. Inspect process cwd/command before acting. Dev3225 and isolated Supabase56321 remain unchanged. Source has not been merged to main.
- Next: finish artifact repair/retry and independent view; live survey/thread/photo/redaction and feed privacy, submission interrupted/corrupt drafts, concurrent/bulk review, worker lease/restart/cancel tests; retained legacy PDF/ZIP delivery failure; live RLS/additive upgrade, final full checks/mutations; fresh direction synthesis, roadmap/ledger/known issues, tested commit/push and CI. Preserve human usability/comparison, advanced synthesis/decision follow-through and v0.44 release gate.


Workbook candidate follow-up: independent Calc inspection confirms explicit body style2 fixes wrapping/top alignment; SHA/guidance and first four long-text pieces are readable. All three reconstructed long fields match previous intact CSV/XLSX exactly. Candidate files are private `style-candidate-{public,internal}.xlsx`; they have not been delivered through the browser. Root must finish same-snapshot corruption repair and verify final arrived files. Artifact addendum preserved unchanged here. Restored artifact downloads now accept the authenticated artifact's exact original path/checksum, avoiding silently serving a later rendering when an earlier report artifact is requested. Latest focused renderer/photo tests:10passed. ZIP regression failed before explicit UTF8 buffers and passed afterward. Full acceptance still pending.


## Crash recovery checkpoint, 2026-09-07 Pacific

The computer crash interrupted the full application and live-RLS reruns. Their logs contain only startup, so neither is a pass. Source, isolated Supabase data and delivered files survived. Main remains untouched. Focused recovery/moderation/attachment/bundle/page-summary checks reran after recovery: 9 files, 133 tests passed. Latest typecheck and lint passed before the still-pending project-report coverage repair.

- The repaired stress files passed independent external snapshot SHA, ZIP manifest/member and text reconciliation, native Calc recalculation after poisoned formula caches, and every-page PDF comparison. All 499 final pages match the previously inspected revised pages. Preserve initial corrupt ZIP evidence and the root verification error above. See artifact-review-b-final-repair.md.
- Both final demonstration campaigns were reached from Projects, queued public/internal snapshots through Record, and downloaded all 12 PDF/XLSX/ZIP files in fresh authenticated Chrome. Desktop/390px file controls fit without horizontal overflow; no page errors. Independent final demo artifact inspection was interrupted and has resumed in a new read-only reviewer. Do not mark artifact acceptance before its report.
- Legacy campaign handoff PDF and containing project ZIP both arrived through ordinary authenticated browser links. Independent inspection covered all eight PDF pages and 23 manifest/hash/embedded-PDF checks. The previous profile-specific ERR_BLOCKED_BY_CLIENT did not reproduce in fresh Chrome. That is bounded non-reproduction, not proof that the earlier browser profile was repaired. Initial cold browser attempts timed out and remain failed attempts.
- Survey browser recovery uploaded an illustrative photo, restored text/photo after reload, aborted the real successful response, and retried to the same session receipt. Staff inspected current photographs, redacted answers with reasons, retained original history, and received 409 on stale review; anonymous original-photo access was refused. See survey-journey-evidence.json. Extra pending attempts from early failed automation are retained as truthful fixtures.
- Live worker checks covered cancelled claimed jobs, requester revocation before claim, restart after expired lease, preserved snapshot SHA, second-attempt completion and refusal of the dead lease token. See worker-recovery-evidence.json. The Documents worker and dev3225 died in the crash; restart only these owned processes.
- Browser review found a real leftover 20-item cutoff between the complete campaign query and the review component. Removed it; a 1,005-item page-prop regression passes. Live map/list selection and 41-page navigation pass. The search step in the first script did not await updated results; that claim is not accepted and needs rerun. Geometry-only routes/areas now count as mapped, with invalid-coordinate negative cases.
- Added migrations 9 and 10 in the isolated stack. Project bundle metadata/direct storage are fenced. Each direct content update now consumes an expected timestamp and fresh review reason. Independent review caught an INSERT escape in this new guard: corrected by INSERT clearing, a NULL-only stored-row constraint and excluded snapshot fields. Updated rollback custody probe verifies inserted intent is discarded, prior reasons cannot be reused, stale direct updates fail and public snapshots omit transient fields.
- Independent final A found a remaining reachability defect: campaign-targeted review Reports cannot enter project evidence bundles because inventory and byte readers require reports.project_id. Extend the existing lead/shared campaign resolver across discovery, byte read and download recheck. Keep the exact-one-target invariant. This is the next implementation step; current checkpoint is NOT M9a acceptance.
- Remaining: coverage-aware project bundle repair and live withdrawal/revocation; comment/thread/feed/privacy and corrupt-draft recovery checks; final artifact review; updated harmless/consequential mutations; full application/live-RLS/upgrade/QA/CI, final identified-build journeys, direction synthesis and bounded roadmap/ledger/issues updates. Human usability/comparison, advanced synthesis/decision follow-through and v0.44 release remain open.


## Final geometry and shared-report correction, September 7, 2026

**A material verification error was found and reported.** The original keyboard demo line repeated its endpoint and the polygon repeated its sole vertex. Root verified types without distinct positions; reviewer B initially described only a scale problem. Both claims were inadequate. The retained failed files/reviews remain unchanged. Animated keyboard map panning allowed immediate Enter to read the prior center. Keyboard panning is now immediate in both drawing controls; collapsed routes and zero-area polygons cannot be submitted anew. Exact retry of an already accepted historical geometry still returns its original receipt. Historical invalid geometry remains in immutable JSON and is explicitly identified and omitted from new map/GeoJSON output.

New actual keyboard contributions survived draft reload and were stored with two distinct line endpoints and three distinct polygon corners. Independent database measurements found polygon doubled signed area -0.007895801471999891 square degrees. Staff approved these distinct records and withheld the original invalid line with a reason. Current receipt regression passes 34 cases; six geometry/recovery files passed 95 cases before the extra retained-geometry regression. Fresh demo artifacts are being delivered and require independent every-page/native-Calc review.

Campaign-targeted Reports now use complete lead/shared project coverage in candidate discovery, byte reading and download rechecks. Actual project Evidence navigation created three bundles containing the selected campaign PDF. Independent verification reconciled 51 bundle checks. Thirteen live checks then established public-copy withdrawal in both projects, preserved staff internal access, coverage unlink withdrawal, viewer internal metadata denial, refused storage signed URLs and revoked staff denial. One earlier run overlapped temporary unit mutations and is invalid, not a runtime defect. Another cleanup attempt hit the legitimate last-owner guard before revoking the main account; the fixture now uses a temporary second owner and restores membership. Final run is shared-bundle-revocation-evidence.json.

Manual reviewed responses now select addressed contributions without AI; both demo cases have one actual published staff response. The countywide public case also has a recovered written-location contribution. Comment corrupt-draft preservation and real accepted-response interruption/retry were exercised. The separate 1,005-item stress report still retains its independently verified 499 PDF pages, exact multilingual text and poisoned-cache Calc recalculation; these do not substitute for the new demo geometry review.

Post-crash full application run completed: 1,190 files passed, two failed, 14 skipped; 13,202 tests passed, three failed, 126 skipped. Remaining failures were the honestly stale direction binding and an SQLSTATE literal misclassified as a place code. The latter now has a specific harmless positive control. Full rerun is pending final direction synthesis. Isolated live RLS completed 21 files and 149 tests. Main remains untouched; this is still an implementation checkpoint, not M9 human usability acceptance or a v0.44 release.


County context correction: the first nondegenerate area was outside the irregular county polygon although inside its bounding rectangle. The campaign legitimately permits outside input; the demonstration was inadequate for a within-county case. Staff withheld that example with an explicit reason and approved a separately submitted keyboard area e1048444-d42b-4a3a-9f0c-23cc4e2b369b. All vertices and 303 edge samples lie inside the retained sourced boundary. This sampling is not a general topology proof. New files are demo-county-reviewed; previous deliveries remain unchanged. Line labels now use the last endpoint and polygon labels use their vertex mean to avoid the demo's coincident starting point. Dense labels can still overlap and the report discloses that limit.

Historical invalid drawings with legacy center coordinates no longer count as mapped. Receipt restoration in the browser also tolerates an already received legacy invalid drawing while refusing an unsent corrupt draft. Geometry, retained receipt, numbered map, bulk stale-selection, photo cache and count mutations have harmless survivors and consequential assertion failures. The first asynchronous browser pan mutation survived because intervening automation calls let the animation advance. A same-frame key sequence catches the removed immediate pan; both records are preserved. Database mutations separately protect immutability, review versions and public snapshot scope. These checks do not establish human motor accessibility or every map topology.

The unchanged Python worker runner was attempted and exited1: 52 suites did not run because this isolated checkout has no worker virtualenvs. No Python worker source changed. The changed TypeScript Documents worker has its application regressions and live lease/restart/cancel/retry/delivery evidence. Do not report the unrelated Python suites as passed.

## Final narrow-panel correction and post-crash continuation, September 7

At30639051 the full local QA gate completed successfully:1,192 app files/13,211 tests passed,14 files/126 tests skipped;21 live RLS files/149 tests passed; lint, production dependency audit and webpack production build passed. GitHub CI34154962292, live RLS34154962185 and restore34154962246 also passed. Vercel remains blocked at the account boundary; no deployment or paid infrastructure was commissioned.

The outer document-width check missed inner-panel overflow. Measured panel and history-button bounds now catch the failure. A harmless border mutation survives; removing only fieldset minimum width also survives because other constraints still protect it. Restoring automatic grid minimum width or unwrapped history text fails the actual containment check. Both the initial inadequate probe and corrected result are retained. Root visually read the final390px panel/history screenshots.

A subsequent popup containment assertion accidentally accepted an empty popup collection. That result is withdrawn. Staff map selection now uses the full adjacent review detail for native point buttons and shapes; duplicate map popups are suppressed only for that linked workspace. Ordinary reading maps retain popups and have no universal edge-containment claim. The focused19-test file passes; a harmless comment survives and restoring either point or shape duplicate popups fails the intended assertion. TypeScript and focused ESLint pass. Real browser confirmation and final build binding follow separately.

The production delivery script initially timed out because it did not wait for Record navigation. Waiting for the actual route and loaded files produced all twelve expected files with identical bytes and external SHA-256 to the independently accepted set. The unstamped30639051 server is not final identified-build evidence; final stamped production repetition remains required.

The final browser role query caught an actual regression in314f946f: without `setPopup`, Mapbox assigns the native marker button `role="img"`. The unit mock had omitted that library side effect. Explicitly restoring `role="button"` after marker construction repairs the accessible action. The mock now reproduces the observed image-role assignment and fails when the explicit restoration is removed. All19 focused tests pass; the harmless comment survives and point-popup, shape-popup and button-role mutations each fail their intended assertion. The full QA attempt on e5909576 was stopped in its owned process group during tests before changing source; it is not a completed gate.

The same browser journey then found pointer clicks on a point overlapping a route also reached the map's shape handler and selected the route. Marker clicks now stop propagation. The focused regression asserts that a containing map click listener is not invoked; removing stopPropagation fails, while the harmless comment survives. This further correction is required by real pointer input, not a change in review scope.

The corrected development journey passed all nine linked-map/list checks from project navigation at desktop and390px: keyboard and pointer choose the same point even above the route, no duplicate staff popup, invalid retained route omitted,25 entries per page,1,005 scoped total, distinct second page, next-item review and search returning the independently known last record. Console review retains GPU ReadPixels stalls, development image-aspect and unused preload warnings; there were no application page errors. The final production repetition will distinguish development-only warnings.

## Final production repetition

The full d58370b8 local QA gate passed with13,212 app tests and149 live RLS tests. After its successful webpack build, the owned production server was restarted with the exact built SHA and matched the checkout. The identity probe first rejected the old unknown commit; it initially compared a full SHA against the health API's intentional twelve-character value, then corrected that harness expectation while independently checking the full process environment.

Actual project-navigation journeys on the identified production build pass all nine map/list checks and six narrow-panel baseline/mutation/restoration checks. Twelve arrived files match the independently accepted final files exactly. The delivery script required explicit campaign and Record navigation waits; its initial hidden-panel timeouts are not treated as delivered outcomes. Production screenshots were read; only GPU ReadPixels warnings remain in its console record. Source is unchanged since the final gate. Final statuses and limitations are in VERIFICATION.md; CI and merge disposition remain separately identifiable through PR102.

## Automatic PR review follow-up, September 7

After the d58370b8 production evidence and9ce10129 documentation checkpoint, the repository's automatic review identified four valid defects. They were not dismissed because the preceding checks were green. Missing configuration IDs could produce new unversioned contributions; a campaign pointer could reference another campaign; offset feed reads shifted during moderation; uploads ignored participation dates. All four required implementation changes before merging.

New comment and survey writes now compare nullable IDs exactly, requiring the current published version while preserving receipt lookup before the comparison. Authenticated campaign reads expose that version. The new eleventh additive migration adds a composite campaign/version FK and makes pointer-only edits recapture the canonical definition; an older own-campaign pointer cannot silently mislabel new participation. The original simple FK remains. The public feed advances by created-at/ID cursor and continues to an empty page even under a smaller server cap. It refuses failed/nonadvancing traversal and keeps the public projection. Photo upload checks both dates before writing storage.

The targeted tests pass, including mocked projection checks, a1,005-row changing feed under500/73-row caps, and discarded failed reads. The live authenticated writer test runs transactionally inside the existing CI RLS suite. It refuses a foreign campaign pointer and restores an attempted older pointer to the unchanged current definition. SQL mutations disabling the scoped FK or restoring the old pointer exemption fail their intended assertions; the harmless SQL comment passes, and every SQL case rolls back. Route/feed mutations retain a harmless survivor and eight consequential failures.

A separate labelled synthetic campaign was created using normal Setup reuse navigation. HTTP checks refuse omitted/stale versions on both submission paths, refuse uploads before/after participation dates, accept in-window uploads and version-bound contributions/surveys, and recover both exact received identifiers after instructions change. The upload probe uses a JPEG signature fixture to isolate transport/date enforcement; it is not additional photograph-rendering evidence. The earlier full photograph journeys remain the visual attachment evidence.

A separate live feed probe reads that fixture in two-row pages, publishes a newer item after its first real PostgREST read, and receives all six initially published IDs exactly once. Refresh then receives seven; private moderation text is absent. Cursor traversal avoids shifting offsets but does not freeze every public value at one instant. The immutable export snapshot is a different boundary. Existing demonstrated report snapshots, originals and artifact bytes were not changed.

The d58370b8 gate/browser evidence remains valid for its identified source. This follow-up requires a new full QA/build, current browser/API confirmation and populated upgrade including migration11 before final landing.
