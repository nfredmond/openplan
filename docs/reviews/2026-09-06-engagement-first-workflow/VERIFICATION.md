# First engagement workflow engineering evidence

September 7, 2026. Implementation and final gate verification are recorded separately below. This is the bounded first workflow, not human usability acceptance, competitive superiority or a v0.44 release.

## Implemented outcome

Existing Projects → Engagement now supports tailored templates or blank setup, editable categories/questions, sourced study framing, reviewed public layers, dates and participant preview. Contributions retain published configuration versions; definition-only reuse excludes participation and makes a private draft. Unknown historical definitions remain unavailable.

Point, route, area and written-location contributions have drafts, durable received identifiers and exact retry receipts. Feeds and threads retain complete eligible records and exclude restricted/orphaned replies. Staff review links map, list and detail with scoped counts, 25-item pages and next-item selection. Fresh reasons, expected versions and immutable originals/history govern approval, redaction and withholding. Publication and a reviewed response are separate states. Manual staff responses can identify addressed contributions without AI.

Existing Reports and the Documents worker deliver PDF, XLSX and portable ZIP from a single retained scoped snapshot. Jobs retain identity/checksums and support cancellation/retry/lease recovery. Downloads recheck workspace permission, public-copy changes and shared-project coverage, including containing Project bundles. Restricted originals and attachments remain authenticated streams. The self-hosting guide describes the free local worker and installed Chrome renderer.

## Runtime and file evidence

- Both labelled cases were created through project navigation: complete streets and countywide wayfinding. Desktop/390px keyboard participation, draft reload, non-map submission, staff approval/withholding/history, reviewed responses and actual file downloads are retained outside git; sanitized identities and checks are in evidence/. Synthetic fixtures are labelled, never represented as public opinion.
- The final county polygon has a separate received ID after withholding the outside demonstration. Independent GEOS validates its shape and complete containment in the retained county polygon. Prior degenerate/outside shapes and receipts remain readable history. Report maps and GeoJSON explicitly omit invalid raw drawings while snapshot JSON retains them.
- [Final demo artifact review](artifact-review-b-county-reviewed.md): twelve arrived files, 161 record/integrity checks, four genuinely poisoned then native-recalculated workbooks and every page of the 24 PDF pages. Point/route/area labels and the within-county example are visible. Minor county internal response page split remains; no missing/clipped text found.
- [Stress repair review](artifact-review-b-final-repair.md): 1,005 contributions, multilingual long text, repeated/missing/historical answers, photographs, thread and redactions. Repaired public241/internal258 pages match the independently inspected pages. External snapshot SHA, all ZIP members, exact continuation text and native Calc totals reconcile. Retain the earlier corruption and inadequate initial hash check in IMPLEMENTATION.md.
- [Shared report inspection](artifact-review-b-valid-geometry.md) and [bundle checks](artifact-review-b-insets-final.md): three actual Project bundles, 51 member/identity checks, all nine pages of the two unique included PDFs. Thirteen separate live checks establish changed-public-copy withdrawal, secondary unlink withdrawal, viewer internal metadata denial, refused storage signed URLs and revoked-staff byte denial. Full engagement companions require their own Reports downloads when only the PDF was selected for a Project bundle.
- [Legacy delivery](legacy-artifact-review.md) separately establishes normal campaign PDF and containing ZIP arrival in fresh Chrome. It is a bounded non-reproduction of the earlier browser block, not repair of that profile.
- The large review UI reaches 41 pages with no duplicate page items and returns exactly the independently known last contribution through search. Keyboard map selection opens the matching detail. Current/historical survey photo authorization, corrupt comment draft preservation, actual accepted-response interruption and same-ID retries, stale moderation and cancelled/revoked/restarted export jobs have separate evidence records.

## Verification status and blind categories

Source checkpoint eccfc826 has focused tests and an independently successful populated prior-release upgrade, GitHub run34153627484, all ten additive migrations. At30639051 the full local QA gate passed:1,192 app files/13,211 tests, with14 files/126 tests skipped;21 live RLS files/149 tests; lint, dependency audit and webpack production build. GitHub CI34154962292, live RLS34154962185 and restore34154962246 passed. The earlier dated-direction and SQLSTATE failures remain recorded. The last linked-map correction has19 focused tests, TypeScript and ESLint passing; final build/browser results follow below.

Mutations cover receipt/version/public-copy custody, UTF-8/checksum corruption, long text, map detail/numbering, shared report coverage, attachment authorization, exact bulk selection and queue parameters. Harmless controls survive. Initial asynchronous keyboard mutation survived because animation advanced between automation calls; the preserved same-frame probe fails on the broken pan. SQL mutations protect immutable definitions/history/snapshots, fresh review versions and public scope. Unit mocks cannot establish real database or browser behavior; separate live and identified-browser checks protect those boundaries.

No claim of human usability, comparative advantage, complete authoritative backup/restore, broad language correctness, Microsoft Excel behavior, nationwide planning/scientific validation or production commissioning follows from this record. M9b synthesis/decision follow-through, M9c comparison and v0.44 remain open. The unchanged Python worker matrix was attempted and reports52 suites not run because this isolated checkout has no virtualenvs; no Python worker source changed.

## Final source and production acceptance

The complete local QA gate exited0 on d58370b846e26b4d727fa9efb4483c8bd7e7ea42:1,192 application files/13,212 tests passed,14 files/126 tests skipped;21 live RLS files/149 tests passed; lint, production audit with zero vulnerabilities and optimized webpack/TypeScript build passed. The earlier interrupted e5909576 attempt is not counted. See evidence/final-local-gate.json.

The owned production process on3226 reports the tested commit; its working directory, full commit environment and Next build ID were checked. The browser preflight refused the previous unstamped process. The health API intentionally exposes twelve commit characters; the check compares that exact prefix and separately checks the full process SHA.

From Projects, both demonstration campaigns reached Record and delivered all12 public/internal PDF/XLSX/ZIP files at desktop and390px. Each arrived byte count and SHA-256 equals the independently accepted final artifact set. No new snapshot or rendering replaced the inspected files. The nine-check map/list journey also passed on this build: native keyboard and pointer selection, no duplicate staff popup, omitted invalid retained drawing,25-item pages,1,005 total, distinct next page, next-item review and an independently known final-record search. Actual panel/history bounds pass at390px; harmless DOM styling survives and unwrapped history/automatic grid minimum mutations fail. Production console review found only GPU ReadPixels performance warnings, with no application error. Root inspected the production screenshots; their identities and sanitized results are retained in evidence/.

The source CI is [GitHub run34156983776](https://github.com/nfredmond/openplan/actions/runs/34156983776). Live RLS [34156983774](https://github.com/nfredmond/openplan/actions/runs/34156983774) and restore [34156983767](https://github.com/nfredmond/openplan/actions/runs/34156983767) passed on d58370b8. The populated additive upgrade [34153627484](https://github.com/nfredmond/openplan/actions/runs/34153627484) passed on eccfc826; all ten migration files are unchanged since that run. [PR102](https://github.com/nfredmond/openplan/pull/102) retains the exact latest merge checks. Vercel's account-blocked check remains an external deployment limitation, with no paid service or deployment provisioned.

The bounded first engagement engineering workflow is implemented and has local runtime, browser and independent artifact evidence. Practicing-planner/public-participant observation, comparative superiority, advanced synthesis/decision follow-through and the separate v0.44 release gate remain open.

## Required automatic-review follow-up

The later PR review found four defects beyond the earlier accepted checks. The follow-up requires current versions for new submissions, enforces campaign-scoped/canonical current pointers, replaces shifting feed offsets with stable cursor reads, and checks participation dates before photo storage. Earlier exact retry receipts remain readable. The eleventh additive migration is required before deploying this follow-up. Focused tests, live authenticated pointer checks, nine unit mutation cases, four transactional SQL cases, HTTP contract probes and a real two-row-cap feed race are retained in evidence/. See IMPLEMENTATION.md for the findings and scope limits. Final rebuilt/CI disposition belongs to the follow-up commit, not d58370b8.

The populated previous-release upgrade [34159624859](https://github.com/nfredmond/openplan/actions/runs/34159624859) passed all eleven migrations on c4d7a17c. Its full application run exposed only an older offset-only test double. The corrected double executes cursor predicates/order; all45 page tests pass and removal of cursor/orphan guards fails. The501-record/orphan assertions were retained. No runtime source or migration changed in that test correction. The final gate and identified-build results are retained with the exact PR102 checks and follow-up evidence.


## Accepted automatic-review follow-up

The retained local QA log for `48b2c5fcb7a78907da963a1b4b80c5218ebfe1b6` records 1,193 application files and 13,226 tests passing, with 14 files and 127 tests skipped. All 21 live RLS files and 150 tests passed. Lint, the production audit with zero vulnerabilities, TypeScript and webpack completed. The pre-crash terminal exit handle is unavailable; the retained log reaches the completed build. Independently, [CI 34160150476](https://github.com/nfredmond/openplan/actions/runs/34160150476), [live RLS 34160150475](https://github.com/nfredmond/openplan/actions/runs/34160150475) and [restore 34160150469](https://github.com/nfredmond/openplan/actions/runs/34160150469) passed on that exact source. The CI result includes shuffled application tests and the configured lightweight Python worker/modeling/ops jobs. It does not turn the unavailable full local Python matrix into a pass.

The identified production build `70v0m_-8dyJhcPdJjlly_` passed the final project-navigation repetitions. All 12 public/internal demo files arrived with their independently accepted lengths and SHA-256. Two anonymous written-location submissions sent the current configuration at desktop and 390px; both receipts survived reload. The linked map/list journey passed eleven checks, including 1,005 scoped staff records, distinct pagination, keyboard/pointer selection, all 905 independently reconciled published feed records including replies, and narrow-width containment. Six panel baseline/mutation/restoration cases also passed; harmless styling and redundant fieldset constraints survive, while unwrapped history and automatic grid minimum width fail.

Root visually inspected the receipt, file, map/review and public-feed screenshots. The staff-map/file console record contains GPU ReadPixels performance warnings and no application error. Anonymous receipt/feed pages reported no page errors. The first feed extension omitted the details navigation; its second locator omitted the link's response-history hint. Both timeouts are retained as script failures. The final journey follows the displayed link and retains the original 905-record assertion.

Sanitized identities and results are in `evidence/after-review-*.json`. Eleven populated additive migrations passed in run 34159624859; those migration files remain unchanged. Later commits containing only this evidence and review binding do not change the built application. PR102 retains final-head and merge checks separately. No practicing-planner/public-participant observation, competitive superiority, M9b completion, v0.44 release or Vercel deployment is claimed.
