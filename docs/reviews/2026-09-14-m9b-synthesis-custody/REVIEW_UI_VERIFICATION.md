# Retained review editor verification in progress

September 14, 2026. This is an implementation checkpoint, not browser acceptance or a release. Main remains 09c1cd17 and v0.61.1 remains the latest published release at the last remote inspection.

The editor connects source selection to private retained staff reviews, full notes, reasoned membership/wording changes, earlier versions and browser-local unfinished edits. Exact pending commands survive interrupted acknowledgement and can be retried without changing identity. An unavailable browser store prevents transport; the newest on-screen text can be preserved alongside the older stored copy. Quota-failed text is not guaranteed durable across reload. Source history identifies unfinished local reviews. Exact-output approval, optional generation, response/decision links and reviewed exports remain required by IMPLEMENTATION_BOUNDARY.md.

## Completed evidence

- Focused editor/recovery tests: 23 passed on e01a7781. The 38-case mutation record includes baseline and harmless controls, and targeted faults fail for their stated behavior. The only subsequent editor change replaces the access-loss message's internal word campaign with consultation.
- Full worker run: all 52 suites passed, none skipped. Unchanged worker code was exercised under each worker's own environment.
- New integration proof: 15 tests pass at baseline and with a harmless comment. Removing each of eight SQL-reader classifications, reintroducing the copy regression or removing the migration notice causes exactly the corresponding test to fail. All 12 cases matched and exact source hashes were restored. These classifications document actual read/list/retain RPCs; the identifier scan cannot itself prove SQL behavior. Native custody and the production server/RPC join supply separate evidence.
- The full QA attempt found three failures among 15,490 executed tests: undocumented SQL-only column names, one added internal term in visible copy, and migration 27 absent from Unreleased. All three were corrected without relaxing their guards. The failed log is retained privately as review-full-qa-first-failed.log. QA must finish again before landing.

## Runner errors and remaining checks

The first integration proof result parser looked for an actual column name inside Vitest's JSON failure message, which omits the assertion diff. The correct test failed, but the proof rejected its report. The parser now requires the exact failing assertion, exactly one failure and 14 other passing tests. The original log remains review-integration-parser-failure.log. A focused UI invocation accidentally used repository cwd instead of the application package and discovered no runnable tests; the corrected application-cwd run is separate. Neither is application failure evidence or a pass.

Full installed isolation passed 612 tests in 64 files against supabase_db_openplan-restore-target-2026091050 at API 29821/DB 29822, 346 installed migrations. No candidate migration flags, reset, paid provider or other session's database is involved. Corrected QA on 33b9474f passed 15,490 tests with 583 explicitly skipped and completed lint, dead-code check, provider connector tests, dependency audit and production build. Upgrade run 34901731600 succeeded from populated v0.61.1. Shuffled order, identified desktop/390px browser checks and actual two-browser correction races remain pending. These broad results precede the additional UI recovery fix below. The browser scripts are prepared but not yet executed against this implementation.

The next browser check must establish serving checkout and commit with which-openplan.sh. Browser scripts use fresh Chrome contexts and synthetic records created by earlier real navigation. Private logs/captures live under /home/nathaniel/.local/state/openplan/response-write-probe-20260913/decision-context/. Public manifests contain checksums, not credentials or private source text.

## Additional recovery defect found before browser acceptance

The earlier tests independently covered quota failures and focus refresh. Combining them reproduced loss of the newest quota-failed edit on both focus and storage-event revalidation. Two new tests failed before correction. The source panel now retains that on-screen copy in memory scoped to the account, campaign, source and exact source hash while private inspectors unmount and revalidate. Reopening review history cannot replace it with an older saved copy. The user must preserve or copy it before leaving/reloading the page; unavailable local storage cannot guarantee disk durability. Real access changes still hide private content and clear the parent's recovery map.

The corrected focused suite passed 25 tests. Final mutation proof completed 42 cases with expected outcomes, exact source hashes restored, and changed-file lint passed. Removing the source-held recovery memory, its attention notice or the block on history overwrites reproduces the failures. Full QA/shuffle and browser checks are still required for this UI change. Database, API and worker logic did not change after the 612-test isolation and populated upgrade runs. The earlier build identifies 33b9474f and does not contain this recovery fix. Do not use its existence as corrected browser evidence.

## Browser findings on 7608831f

Full corrected QA and shuffled seed 619144 each passed 15,492 tests, with 583 explicitly skipped, before the later CSS-only correction. The production identity check matched this isolated checkout on port 3262. Desktop and 390px journeys retained original/corrected source, preparation and content hashes, exercised exact retry after committed response interruption, restored full unfinished review notes, removed and restored contribution membership, preserved a quota-failed edit across actual browser-tab focus and denied anonymous/stale-user private reads. Both reached revision 5. The browser exercises seven and six contributions respectively; native/React tests separately cover 303/302 contributions and long source text. Do not claim the browser membership example itself exceeds 600 characters.

The two-browser HTTP check completed: simultaneous identical commands returned 201/200 and created one revision; competing corrections returned 201/409, preserved the winning text, and left the refused revision absent. Exact winner retry returned 200; stale and changed retries returned 409. Original bytes remained unchanged. These are simultaneous actual requests, not a deterministic schedule of every database interleaving.

Two runner errors are retained. A restored textarea's text became part of Playwright's exact label lookup, while the accessible textbox name and full saved text remained correct. The probe now uses the accessible textbox role. Playwright also enables focus emulation on all tabs by default. A separate fresh-Chrome control observed no default focus events, then actual blur/focus after disabling that emulation. The runner now disables it and waits for both source and review revalidation. An unhandled parallel response-wait rejection ended one diagnostic process before its final JSON; its partial screenshots and log remain private. No abandoned browser process remained.

Visual inspection found a real 390px layout failure even though the earlier viewport-width assertion passed: multiple buttons extended beyond their cards. The new inner-container assertion reproduces that failure for retry and review-history controls. The CSS-only correction wraps all review buttons, reduces phone padding and removes an extra phone-only border/padding layer. Desktop spacing remains unchanged. Its changed-file lint and 33 focused tests passed; rebuild and both final browser journeys are pending. Inner-container checks include surviving harmless positioning and a targeted oversized button, and must cover pending recovery, restored drafts, membership and history.

Before a synthesis release, retire the old live Generate path. The source still confirms that it truncates at 300 comments, ignores survey input and returns success after save failure. Preserve earlier results as explicitly limited history. The new retained review workflow must become the safe entry path. Optional complete model generation, output approval, response/decision links and reviewed exports remain M9b obligations rather than claims supplied by this checkpoint.

## Final review layout evidence on 6bbdf609

The final desktop and 390px review runners completed with no page errors. The only console errors are the deliberately interrupted save responses. The pending, restored-draft, membership-edit, quota-refocus and original-history screenshots were inspected at both widths: controls fit within their immediate containers and full retained text remains available. review-browser-final-layout.json records identities, result and screenshot checksums, and baseline/harmless/oversized-control bounds. It supersedes the before-layout failure for this build, without erasing that failure. The corrected revision-5 screenshots were also inspected at both widths; full notes, correction reason and revision checksum remain readable. Legacy route retirement is a subsequent change and needs its own browser evidence.
