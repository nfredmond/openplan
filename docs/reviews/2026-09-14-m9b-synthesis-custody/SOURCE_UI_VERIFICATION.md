# Retained source selection and browser recovery

September 14, 2026. The Analysis tab's private source-selection/history/inspection workflow is connected and browser-checked. This is an engineering checkpoint, not a release or completion of M9b. v0.61.1 remains the published release. The earlier capped synthesis generator does not consume these sources; preparation, durable generation/review and reviewed exports remain unfinished.

## What the browser established

Production build f3b7edff4b35a64e82bd0685d6a0cc7e3eda93e6 ran from /home/nathaniel/.local/state/openplan/translation-command-workflow-2026-09-13/openplan on local port3262. which-openplan.sh returned MATCH after confirming the full commit stamp and serving directory. An initial short startup stamp was rejected before acceptance; the owned server was restarted with the complete hash.

The runner entered through sign-in, Engagement, the existing synthetic consultation and its Setup/Analysis tabs. It created a category with the normal form, selected all moderation states with comments and surveys included, and saved through the interface. It allowed the server to retain that source, then deliberately lost the acknowledgement. Reloading and keyboard retry sent the same complete request and returned HTTP 200 with replayed=true and the original receipt. Private source text was reopened and its SHA256 independently recomputed from exact retained bytes.

The runner then corrected the category through its normal editor and saved another source. The new definition and hash were retained, while reopening the first capture returned identical original text and hash. The original was also reopened through its saved-history button and displayed the original hash. These checks passed at 1440px and 390px. They used four and three pre-existing synthetic comments respectively, not a 301-comment browser corpus. Anonymous list/read attempts returned 401 and a stale-account read returned 403. No page errors occurred. Each console contained only the deliberately injected connection-reset failure.

Images were inspected at both widths. The first document-width assertion missed an inner clipping defect: a 342px recovery control overflowed a 242px parent. The final control wraps to 242px. The final runner checks the parent's bounds; a harmless relative-position class survives, removing wrapping/maximum-width rules reproduces right edge 416.17px against parent edge 316px, and the original styles are restored. Selection, pending recovery, original and corrected inspection captures are hashed in source-ui-browser-results.json. Raw captures stay in the private artifact directory.

## Other checks and their boundaries

- Full local QA and shuffled seed 619142 on 26eded0c passed 15,431 tests, skipped 549. Lint, provider-connector checks, dependency audit and production build completed. The explicit RLS command, not QA's skipped opt-in hook, supplied live database evidence.
- Complete installed RLS: 578 tests in 62 files passed on the named restore-target stack, API 29821 / DB 29822. Native source/list subset also passed 16 tests in candidate mode and installed mode, including real faulty-policy and cursor cases. Both additive migrations are installed, 345 total through 20261014000026.
- Worker check: 52 suites passed, none failed or unrun. These are worker regression tests, not independent modeling accuracy.
- The final layout-only commit f3b7edff passed changed-file ESLint, eight focused panel/copy tests and a fresh production build. The two complete browser journeys above exercise that build. No server, database, worker or model logic changed after 26eded0c.
- source-api-mutations.json records 68 baseline checks, a harmless survivor and 45 detected targeted faults. Separate inventory and page-handoff records establish their own controls/faults. Mocked component tests establish the 301st-source search and survey-only selection; native fixtures establish survey custody. Those do not substitute for a real survey-submission-to-inspection browser journey.

GitHub Upgrade Path 34886882938 succeeded on 26eded0c. CI 34886882906 succeeded, including QA and shuffled tests. RLS 34886882895 remains active and must be read at its terminal state. The RLS job's first attempt stopped before tests because its runner could not bind port 54324; the failed job was rerun. The final layout/evidence commit requires its own final CI inspection before any release claim. No tag was created.

## Continue the complete outcome

Next add free complete preparation from these saved sources, preserving historical category/question membership and not-assessed interpretation. Then join retained drafts, optional resumable generation, explicit coverage, staff corrections, responses/decisions and usable exports as specified in IMPLEMENTATION_BOUNDARY.md. Extend browser evidence to real survey submissions and larger source histories. Keep the full V1 contract, geography obligations and separate engine validation intact; no human software-release gate is introduced.
