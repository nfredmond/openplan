# Final production evidence

Build `b8e81af737e2`, isolated checkout and local app port3226, owned Supabase57321/57322. The script requires the retained synthetic exercise and signed-in owned Chrome CDP9336; it is a reproducible checkpoint probe, not a fresh-fixture setup script. It uses real navigation and fails on identity, layout, recovery, hash, truncation or console violations. Screenshots contain only synthetic engineering records/public source context. Raw files and immutable histories remain private under the verification directory named by the script.

`browser-result.json` records nine checks and nine delivered files. Compare hashes to independent/evidence-b. `access-result.json` records actual authenticated bytes, revocation and unavailable reviewer recovery, including an instrument timeout and successful repetition. `save-recovery.json` retains only request hashes and the single event identity from the earlier lost-response exercise. No passwords or raw command payloads are retained here.

Screenshots were visually inspected at desktop and390px. Keyboard and accessible-label checks do not establish a complete screen-reader experience or observed agency usefulness.
