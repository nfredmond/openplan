# M2d.2 OWP review and amendment engineering acceptance

September 7, 2026. Implemented in the isolated `work/owp-review` checkout from main `3004393820ec6f2b05f7a00136bf887914da48b4`. This accepts the bounded engineering workflow below. It does not establish actual agency adoption, spending authority, professional usefulness or nationwide completeness. v0.44 remains unreleased and its three partial campaign outcomes remain open.

## Result and exact evidence boundary

From Programming Cycles → the program → Work program preparation, a planner submits an immutable revision to assigned reviewers, receives a return in My Work, revises/resubmits and retains separate internal approval, board-adoption, external-acceptance and scoped spending evidence. Each event identifies its revision/hash, actor, time and supporting records. Transactional permissions and conflicts protect the same boundaries as the UI. An uploaded signature or generic Program status does not establish authority.

Amendments start from an adopted baseline and retain work identities and references. Pending changes do not change that baseline or inherit authority. Undated assignments retain no due date. Review packets extend the existing private Documents worker and HTML/PDF/XLSX export paths. Preparing a public-filtered copy requires an administrator's exact revision/history acknowledgement; it does not publish the file online. Review files and supporting evidence use authenticated streaming through both Programs and ordinary Documents.

Application and migration source accepted in the final production journey is `b8e81af737e2293d6a01f6945161f2ec2ed1b529`. Health reported `b8e81af737e2`; process cwd and port 3226 identify the owned checkout. Supabase API 57321/DB 57322 belong to `owp-review-verification`. Later documentation and live-test entry changes do not change application rendering, workflow functions or accepted artifact contents. [Production records](evidence/production-final/README.md) retain machine-readable results, script and desktop/390px captures. Raw immutable histories, test credentials and downloaded files remain in the private local verification directory.

## Exercise and delivered records

The source is the retained public EDCTC FY2026–27 final work program. One WE100 reference expenditure is 76,641.63 USD, reconstructed from 44,164.10 salaries plus 32,477.53 indirect costs. This bounded reference proposal does not assert available funds. Every adoption/acceptance/spending narrative and the supporting TXT explicitly identifies a synthetic engineering exercise; none is an agency signature or receipt.

| Stage | Evidence |
|---|---|
| Submit and return | Revision 4 submitted; a different assigned reviewer returns it; My Work sends the preparer to the correction. Submitted content remains intact. |
| Revise and decide | Revision 5 resubmitted, independently internally approved and given explicitly synthetic adoption evidence. Effective baseline hash `a693ee7e7dcc64116feb8cb12064ac218ee48edc1d33db970b7ee0efa7ad865f`. |
| Amend | Revision 9 changes product description, schedule and cost. Pending cost is unresolved, while revision 5 retains 76,641.63. Work/product/cost identities remain stable. Revision 9 hash `bcf4a2173a5e1c61aa568b29d71fbfc73f6e913b2a5875436e77b784aa1722bd`. |
| Late evidence | External acceptance and scoped spending evidence can reference still-effective revision 5 while the amendment exists. They do not authorize revision 9. |
| Correct history | Early events 6/7 exposed a hidden-field defect. They remain immutable; public correction 10 explains them. Final commands 11/12 contain only applicable fields. The new UI and database guard reject that defect. |
| Export | At history 14: internal revision 5 and internal/public revision 9, each in HTML, PDF and XLSX: nine actual delivered files. Public copies omit internal-only comments and actor/reviewer metadata. Supporting TXT bytes also reconcile. |

[Independent final artifact reconciliation](independent/evidence-b/README.md) checks selected content, snapshot/revision hashes, unresolved items, differences, supporting references and public exclusions. Reviewer B inspected actual PDF and native Calc output, including a 90-line stress case that initially clipped line 64 and passed after correction. Final production downloads match all nine independently accepted hashes. Hash agreement alone was not treated as readability evidence. Native Calc is not Microsoft Excel validation.

## Verification and counterexamples

- Full local `qa:gate` at 40c5f431 passed lint, deadcode, 13,247 application tests, 157 explicitly isolated live RLS tests, dependency audit and webpack build.134 tests were intentionally skipped outside their live mode. Existing deadcode warnings remain; command exit was zero.
- A final Documents HTTP probe then found supporting evidence returned 503: the real PostgREST builder encoded a JavaScript array incorrectly. Source review and mocks had missed this. The correction uses explicit JSON containment. All 22 focused route tests pass, including the real builder; the original encoding produces 22P02. Final source rebuilt successfully and the stamped production HTTP repetition passed.
- The live-test command now loads optional checkout `.env.local` before Vitest. Two real-process checks prove configured target loading, a harmless comment survivor, a failing removed-flag mutation, explicit override precedence and CI without the file. Running `npm run test:rls-live` without a shell target then passed all 157 tests against the configured owned stack. This controls the entry command, not arbitrary direct database commands.
- [Final browser results](evidence/production-final/browser-result.json): nine checks, nine files, zero console/page errors. Actual navigation, keyboard action/revision selection, explicit accessible names, version-bound unsent-note recovery and disclosure reset work at 1440px and 390px. Screenshots were visually inspected, including readable amendment differences and unresolved values. These checks do not establish a complete screen-reader experience.
- [Access results](evidence/production-final/access-result.json): generic Documents packet and supporting bytes return 200 without bearer redirects; revoking membership makes both downloads and review metadata return 404. Restoring membership restores supporting-file access. Real 390px navigation exposes and permits removal of a recovered unavailable reviewer. Membership is restored after each probe.
- [Lost-response recovery](evidence/production-final/save-recovery.json): the first POST committed before its response was deliberately aborted; the UI retried the identical request and recovered one event. An actual intercepted download was truncated by half; no corrupt file was emitted, the UI reported mismatch, and the retry matched the accepted PDF hash.
- [Two-session concurrency](evidence/owp-review-concurrency.json): preparation edits and different workflow requests serialize on the Program lock, then reject stale state with PT409. Identical concurrent retries recover one event. This is a bounded race check, not a load test.
- [Independent upgrade and SQL proof](independent/upgrade-and-guards.md): two fresh stacks ran all 266 predecessor migrations, populated preparation/source/extraction/Documents/job rows, then applied the two additive migrations. Compared predecessor fields survived and no authority was backfilled. Final 19 SQL groups, two harmless survivors and 13 consequential failures cover membership, role, cross-workspace references, immutable revisions, stale decisions, budget/baseline separation, assignments, hidden fields and legacy retry ordering. Final function/row restoration was checked.
- [UI mutations](evidence/root-ui-mutations.json), [Documents mutations](evidence/owp-document-mutations.json) and independent component/pure/layout proofs retain harmless survivors and consequential failures. They exercise changed guards; they cannot prove all possible disclosure content or agency requirements.

Final migration SHA256 identities: `20260909000001_work_program_review.sql` = `4abc63db4c5d8eeb79e78edecae549f5a2494377b6d674228805e0aa6c6993d6`; `20260909000002_work_program_review_packets.sql` = `8ba82764736812e62f2343784b59cb2e6e52ae61e8b099a10c961a8c4a5df0fe`.

## Verification mistake and retained residue

One repeat RLS command omitted the isolated target and touched the default local database. Five existing scientific suites retained ten new synthetic accounts and twenty workspaces. Cleanup hit the existing owner-floor guard and rolled back. Only those ten newly created accounts were then banned and their sessions revoked; their fixture records and twenty owner memberships remain. No schema, foreign process or pre-existing account was changed. [Exact cleanup limits](evidence/wrong-stack-cleanup.json) are retained. That failed run is not accepted isolation evidence. The later automatic environment loading and mutation test reduce repeat risk; they do not erase the mistake or its residue.

Other failed checks, source-review misses and instrument corrections remain in [IMPLEMENTATION](IMPLEMENTATION.md), unchanged independent reports and the evidence records. An early Playwright uncheck timed out after removing the control; repeating with click and explicit absence checks passed. Already-issued bearer links can survive until expiry and downloaded copies cannot be revoked.

## Landing and next dependency

[PR103](https://github.com/nfredmond/openplan/pull/103) carries the implementation and retained evidence. The final application head b8e81af7 passed GitHub QA, shuffled tests, worker/modeling/ops checks, live RLS and Restore Drill; [retained results](evidence/production-final/application-ci.json) identify each run. Required GitHub checks must also pass on the final evidence/test-command head before merge. Earlier successful GitHub populated Upgrade Path run 34168045520 used the same final migration identities; final PR CI/RLS/Restore and merge status are separate check records. Vercel reports its account is blocked; that existing integration is not a required check, and no paid deployment is provisioned. No release tag or full v0.44 campaign is part of this milestone.

The roadmap next connects M11a contract/task/staff actuals to the versioned baselines, then completes one M2d.3 reporting and correction period. CORE-OWP-01 remains partial for full administration and closeout. Keep provider choice, engagement observation/decision follow-through, capital delivery, complete RTP updates, procurement and separate scientific validation in the active queue. Actual agency requirements, adoption, usefulness, balances and external receipts remain unresolved until supported by the appropriate records or observation.
