# OpenPlan desktop control review

September 4, 2026 Pacific. Read-only source review at `b2248315b96a31ff1c8ad91947e91cd35d897dbd`, while the developer owns acceptance. No panel launch, live Start/Stop/Update action, browser/account use, service change or application test occurred. Two behaviors were reproduced using extracted actual functions with all external effects replaced by fakes. The active checkout and demo installation were not changed.

## Where it lives and what is current

The desktop shortcut `/home/nathaniel/Desktop/openplan-control.desktop` and matching application-menu entry are outside Git. Both execute the tracked `openplan/scripts/ops/openplan-control-panel.py` inside `/home/nathaniel/code/openplan`, with the app directory as working directory. The installed icon is outside Git; its installer is tracked at `openplan/scripts/ops/install-desktop-shortcut.sh`. This is an existing repository tool to maintain, not a separate application to rebuild. New panel source is used when the shortcut launches a new window; a running Python window does not reload edited code.

The demo is a separate checkout at `/home/nathaniel/apps/openplan`, served by the active `openplan-web.service` user service from its `openplan/` directory. At observation, port3000 reported v0.43.0 commit `a8ef0df7333a`, while its filesystem HEAD was `2965670cb61f76f2352a62168000905a48a08f84`. These identities differ; the reason was not investigated or repaired. Port3200 reported candidate v0.44.0 commit `b2248315b96a` and its listener's working directory was the active development app. These read-only observations establish reported identity, not acceptance or full readiness. Do not update the demo to the unfinished candidate just because it is newer.

## Findings and required repairs

| Finding | Evidence and consequence | Repair priority |
|---|---|---|
| Foreign-process stop is allowed | `stop_dev` at line641 obtains the PID from port3200 and signals its process group without checking `self.dev_proc`, ownership or process identity. The close-window action repeats it at line746. Fake input with no owned process still requested a group signal. It can stop another agent's server; group scope can include more than the listener. | First: stop only a verified process/session owned by this launcher, with identity revalidation immediately before signalling. External sessions are visible and protected. Never infer ownership from a port or matching folder. |
| Older CI can appear current | `automated_checks` at line180 requests no commit/date identity and discards running/queued records. A new running CI plus an older success produced green `all 1 checks passing` in the isolated reproduction. The forty-run sample can omit workflows. | First: show exact target commit, current/queued/failed/missing/unknown states, timestamp and run links. Keep scheduled checks separately dated. No old success or unexpected skip substitutes for required target evidence. |
| Demo update can claim success despite uncertainty | Refresh script permits unreadable migration status, warns but continues on reported-build mismatch, then prints Done. The panel treats exit0 as latest-code success. Neither path establishes release acceptance. | First: unresolved schema or build identity must stop readiness promotion. Separate downloaded, built, running and accepted-demo states. Report the last verified step and a usable recovery action. |
| Latest development is treated as the preferred demo | `commits_behind` compares against local HEAD; no branch/ancestry or acceptance policy. UI recommends updating before showing anyone. Refresh fetches origin/main, which may still be under acceptance. | Explicit stable demonstration target and separate development target; explain newer/unaccepted work without telling Nathaniel to deploy it. Preserve selected target identity through update and health checks. |
| Desktop recovery still requires terminal work | Open Demo only opens a responding service. An absent demo prompts setup checking; missing migrations require pasted shell commands. Recent Errors reads only the log created by this panel's test-site start. Database green means one named Docker container is running. | Guided demo start/recovery, contextual demo/development/service logs, actual database/schema and required-worker readiness, actionable setup errors and a shareable redacted diagnostic record. Destructive actions remain explicitly separate. |
| Refresh can multiply polling and does not coordinate other windows | `_refresh_status` schedules itself; `_work` schedules another call after each action. Busy state belongs only to one window. | One polling schedule, serialized refresh work, shared operation ownership, visible stage/elapsed time and safe close/interruption handling. Source-inferred risk, not a measured leak or reproduced concurrent update. |
| Update recovery and messaging overpromise | Refresh merges and runs dependency install/build in the serving checkout before restart. Panel says nothing can be lost and says this again on any failure. No rollback route is exposed. | Preserve last usable build and explicit update state; prove interrupted install/build/restart recovery before promising it. Replace blanket assurances with the observed result. |

Opening Demo or Test currently does not establish the full intended identity before launching Chrome. Include wrong-service/port-reuse cases in the repaired flow. Existing fallback to the user's browser and Wayland clipboard support are useful and should be retained. Review Copy All Text and raw log content for credential/private-data exposure before calling the export safe; no actual secret leakage was tested or observed here.

## Tests and reading limits

Read the entire778-line control panel,87-line shortcut installer, full refresh script and identity script, the status classifier and existing classifier test script. Read selected CI ops-test wiring and selected doctor source. Read the actual two shortcuts, allowlisted service properties, Git identities and deployment fields from two health responses; never printed environment values or process command lines. A filename search was initially broader than intended; unrelated file names were ignored and no unrelated contents were read.

Existing `test_automated_checks.py` usefully covers success, timeout, cancellation, unknown conclusions, skips and empty input in `summarize_check_conclusions`; CI invokes the ops test scripts. It cannot test records removed before they reach that function, target-commit applicability, live ownership or update failure handling. Preserve those tests and add consequential coverage at the collector/controller boundaries. No existing suite was run or changed in this review.

[Reproduction observations](control-panel-review/reproduction-output.jsonl) record actual function results under fake I/O. Contrasting empty-run and absent-listener cases gave non-green and no-signal outputs. They do not prove a repaired guard or a GUI workflow. Source hashes are in [metadata](control-panel-review/source-metadata.json); the scratch reproducer is retained outside Git. No live signal was issued.

## Completion after the development handoff

CORE-OPS-CONTROL-01/M3a requires Nathaniel to open the desktop icon, identify the accepted demo versus current development, open the correct site, understand incomplete/failed checks, perform a permitted update, recover from an interrupted update and obtain useful redacted diagnostics without command-line steps. Use an isolated disposable service/process environment to prove owned/foreign/PID-reuse/port-conflict behavior and close-window safety. Prove current/older/in-progress/missing-check distinctions and schema/build mismatch handling. Changed guards need targeted failing mutations and a harmless survivor. Inspect the actual native window at usable desktop sizes, keyboard navigation and display scaling; native desktop-tool layout is distinct from the app's390px mobile requirement.

Small safety/truthfulness repairs come first, then guided recovery and presentation readiness. Reuse the existing Python tool and services. A future hosted installation needs a suitable operator view, but the desktop launcher must not imply it controls remote services. No paid infrastructure is required for these local improvements. Source maintenance can be prepared separately; installing it and operating the demo must wait for the active owner's safe handoff.
