# Consolidation verification

September 6, 2026. Development handoff main `cc6c3feb` was merged into the isolated review branch at `42515335`. This record covers documentation and bounded guard/control maintenance. It does not declare v0.44 released or change the final nine-pass/three-partial acceptance result.

## Implemented changes

- Repository AGENTS.md is canonical. CLAUDE.md and the former shared manual point to it. Global configuration was not edited; no additional nested AGENTS/CLAUDE files were found in the tracked checkout.
- Current contract, roadmap, requirements, capability/handoff documents, architecture, setup/deployment/recovery guides and known issues are reconciled with the completed developer record. Eighteen replaced originals were archived byte-exact against `cc6c3feb` with hashes. Historical studies, releases, independent opinions and source-reading limits remain intact.
- Positive browser-record requirements and symlink rejection supplement the existing outcome, interruption and terminal-exit guards. Capability promotion requires dated nonempty repository-contained evidence with matching hashes. Four existing Node evidence suites now run through ordinary Vitest QA.
- Narrow mechanical documentation checks cover the consolidated entry points. Retired checks required repeated wording, a minimum character count, exact promotional text or an obsolete blanket refusal of hosted workers. Existing public-interface/free-software checks remain. One redundant offline-RLS Boolean tautology was removed; the actual policy assertions remain. The developer already repaired the two scenario test environments, so the merge retains its equivalent fix rather than claiming a second repair.
- Desktop control limits automatic stop to the session started by that window with stable Linux process handles and revalidated identity. Exact-commit CI and refresh outcomes remain distinct from release acceptance. Unreadable/failed/pending migration checks stop refresh before build/restart; mismatched running identity fails after restart. See the [control report](POST_HANDOFF_CONTROL_REPAIR.md) for behavior and limits.

## Checks completed before the verification checkpoint

The focused app group ran 49 assertions successfully with nine live-RLS checks explicitly skipped. A further repository-symlink escape regression was then added and its suite rerun under a surviving comment mutation and rejected containment bypass. The full consolidated app/build/CI results are recorded below when available; the earlier focused count is not the final total.

Guard mutations after merging final developer code: capability comment survived; empty evidence and changed-hash bypasses failed their exact tests; browser comment survived; missing-record and linked-record bypasses failed via the regular Vitest wrapper; operator-doc comment survived and a broken relative link failed. Repository containment received its own harmless control and targeted outside-symlink failure. See [guard results](guard-results.json) and [containment results](guard-containment-results.json). The initial scratch attribution expected words that Vitest truncated from errors; it correctly refused to credit those runs. The reviewer inspected the actual failed assertion names, corrected attribution and reran before recording these results. No implementation was weakened.

The independent operations document checker read 83 local links, 30 npm script references, 31 named environment references and 14 Bash snippets with no unresolved reference/syntax results. Its harmless control survived and broken link/script/setting/syntax controls failed. These are static checks, not a new installation or restore. See [results](ops-doc-mechanical-results.json). Root also checked the current product/roadmap/review links and verified every archived original against the source commit.

Local lint completed successfully. Dead-code reporting completed and reported existing unused/duplicate exports; its report-only exit does not establish zero dead code. The control agent's 26 tests and 15 targeted mutation failures plus a harmless survivor are documented in its report; root reruns the final direct suite before finalizing this checkpoint. A second source reviewer found and confirmed repairs for leader-exit shutdown and refresh cleanliness/inventory gaps; see [the independent control review](CONTROL_SECOND_REVIEW.md). The newly covered roadmap links also have a harmless survivor and a broken-reference failure. No running application, worker, browser session or database was used as a mutation target. No scientific default, frozen artifact, matching rule or holdout was changed.

## Incident and limits

An initial control mutation fixture opened/truncated `~/.cache/openplan-control/dev-server.log` before the fake launcher refused execution. The zero-byte modification was confirmed at 09:56:01 Pacific. Prior contents were not captured, so loss of prior diagnostic text cannot be established or restored. The incident was promptly reported to Nathaniel. The corrected fixture uses temporary log paths and denies real process/signal/HTTP calls; the full mutation group was rerun. No app process was launched or signalled and no planning records were touched.

Control tests use fake processes and inert widgets. Native GUI, real demo update, complete stop/restart lifecycle, keyboard/size evidence and full recovery remain unverified. In-place build, actual runtime database identity and selecting an accepted demo release remain open. Therefore this is a verified guard repair, not complete desktop-control acceptance. No desktop launcher or global configuration was edited.

Local live RLS, all-worker execution, full database/object/local-artifact restoration, a stranger installation, human usefulness and a fresh full twelve-journey pass are separate evidence requirements. Preserve skipped, failed, unknown and unsupported states. The codebase still requires the broader product and engineering roadmap. Remote CI must be read separately from a successful push.

## Final verification

Pending the consolidated checkpoint. Main is not updated until the relevant checks and independent control review are reconciled.

Archive verification caught a filename collision between the archive index and the original root README. Before merging main, the index moved to INDEX.md and README was restored from cc6c3feb. All eighteen originals were then compared byte-for-byte, including the root README, and the manifest regenerated. Ignore rules also excluded new archived instruction files; those named public artifacts were explicitly staged. No historical source was lost.
