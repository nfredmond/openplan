# Post-handoff operations documentation and independent review

Reviewed September 6, 2026 in the isolated landing checkout at merge `42515335`, including completed development handoff `cc6c3feb`. The active main checkout, processes, browser, accounts and databases were untouched. Initial landing status was clean. Final status includes concurrent root changes, which were preserved. No commit, branch operation, service command or application test was performed by this lane.

## Findings

1. The final acceptance record is still nine of twelve passed and three partial, with release withheld. The corrected report evidence has separate QA/browser proof; there is no complete corrected-build first-week pass. The docs now say this in the root README and architecture introduction, linked to `docs/ops/V044_FINAL_FIRST_WEEK_OUTCOMES_2026-09-06.md`.
2. The former entry docs promised a 20-minute free agency deployment, no outgoing data, complete self-service and a sample drill implementing the full restore path. Those were unsupported or contradicted by source/provider terms. Reconciled guidance identifies local evaluation, production commissioning still to prove, outgoing integrations and actual recovery coverage.
3. `openplan/scripts/ops/disposable-restore-drill.sh` restores selected SQL rows to migration-created schema and one object through the Storage API. It does not restore the separately documented full custom-format dump or Storage `/mnt` tar. Recovery must also preserve local model artifacts, Auth/configuration and encrypted integration-key material. Concurrent database/object capture is not inherently consistent. Full backup-to-recovery and continued planner work remain unproved.
4. `modeling:up` provisions the county-onramp Compose service, whereas `modeling:local` starts general AequilibraE/ActivitySim pollers using existing Python environments. The latter permits ActivitySim preflight-only mode. Neither health nor a running poller proves authenticated execution and delivered usable outputs.
5. Run reaping still uses run/stage timestamps, while worker heartbeats are separate. Model-page reconciliation and all three cron GET endpoints can write. Inspection instructions must not casually invoke these as read-only diagnostics. Long-job interruption/restart/late-result proof remains required.
6. The health checker explicitly accepts `checks.database=not_checked` and an unknown commit. The local configuration check never connects to Supabase. The identity helper's environment stamp/process-directory fallback is an aid, not a hash of the served app. The docs now distinguish these boundaries.
7. Current maps use Mapbox; `layer-placement-preview.tsx` constructs an external Static Images URL containing the layer bounding polygon. This is not a whole-file upload, and is still an outgoing location disclosure. Provider-free cartography remains unfinished.
8. The root lane concurrently changed the walkthrough refresh helper to refuse unverifiable migration state before build/restart and to fail on served-identity mismatch after restart. SELF_HOSTING/RUNBOOK were reconciled against that current diff; the root owns its tests and live execution remains unperformed. A post-restart failure can still need recovery.
9. Independently rechecked official Supabase, Docker Desktop, Vercel Hobby/cron, Render, Fly and Railway documentation on September 6. Supabase explicitly excludes the CLI stack from production; government Docker Desktop use requires paid subscription; Vercel Hobby restricts personal non-commercial use and daily cron; free Render storage is ephemeral. No service or spending was initiated. Links sit next to the supported claims in README/SELF_HOSTING. Other legal/scientific design proposals retain their September 4 research dates and limits.

## Independent operations perspective on the ten direction questions

1. Ultimate outcome: an ordinary planning team can install and continue its actual case without Nathaniel, preserve exact sources/decisions, recover an interrupted job and transfer the complete work to another operator.
2. Every planner cannot yet complete core work. Operational independence is cross-cutting; OWP reporting, true project cost/fee management, case-specific authority, accessibility and full record transfer remain partial or unproved. The capability matrix explicitly retains those gaps.
3. All states do not yet work in substance. National data foundations and honest missing-source states do not establish complete state practice, and no full state is proven in the current matrix.
4. California is deepest but not gold-standard complete. Its modeling and end-to-end safety outcomes remain partial/inconclusive; independent operations and complete legal/delivery work have not been demonstrated.
5. Neither demand method has nationwide use-specific scientific acceptance. The final artifact audit proves selected custody, not forecasting accuracy. Preserve each method and all negative results unchanged.
6. Simplest overlooked high-impact work: prove one portable, recoverable case with database records, object bytes, local artifacts, role access and continued work. A report export, schema drill or health dashboard omits crucial parts of this outcome.
7. Wrong old rule: treating hosting accounts plus a build as a commissioned free agency deployment, or a representative SQL/object drill as the full archive recovery procedure. The document corrections remove those claims. This is not grounds to weaken the v1 operational target.
8. Join the competing install recipes into README, configuration into SELF_HOSTING, acceptance into FIRST_DEPLOYMENT and recovery into BACKUP_AND_RESTORE. Keep implementation components; deepen lifecycle/attempt recovery and full record transfer before inventing a new deployment product.
9. Disproof: an independent operator on a clean supported computer follows only repository guidance, completes representative saved work and both configured worker paths, restores the actual archives into a separate target, proves roles/bytes/relationships and resumes work after interruption and an upgrade. Preserve exact build/source identity and failures. No such end-to-end evidence was established in this lane.
10. Highest-leverage completed operations outcome: that independent portable-case installation/recovery exercise and fixes it exposes. Run it alongside the requested planning outcomes as their shared prerequisite, not as a reason to postpone OWP/engagement/capital work until every operations goal is complete. Existing sampled restore/upgrade infrastructure should be extended, not replaced.

This is an independent operations perspective from a delegated agent with inherited task context, not a claim of a different model or a brand-new full professional/legal review. The full product synthesis belongs to the root lane.

## Files

Edited only `README.md`, `CONTRIBUTING.md`, `openplan/README.md`, `openplan/docs/SELF_HOSTING.md`, `openplan/docs/FIRST_DEPLOYMENT.md`, `openplan/docs/ops/BACKUP_AND_RESTORE.md`, `openplan/docs/ops/RUNBOOK.md`, and new `docs/ARCHITECTURE.md`.

The seven existing originals were archived byte-for-byte under `docs/archive/2026-09-06-consolidation/` with original relative paths. `ops-doc-archive-check.json` records hashes and lengths; each was compared with `git show 42515335:<path>`. Root owns the archive manifest/index, canonical rules, roadmap/review and test maintenance. Architecture preserves current implementation versus proposed design sections and links the dated research collection.

## Verification and limits

`ops-doc-mechanical-check.py` runs in scratch, reads only the eight owned docs and source manifests, and uses `bash -n` without executing doc commands. `ops-doc-mechanical-results.json` contains the final counts. The initial check passed 82 relative file links, 30 npm script references, 31 named environment references and 14 Bash blocks. After reconciling the root's refresh-helper change, one added link to root-owned `docs/reviews/2026-09-06-consolidation/VERIFICATION.md` is pending creation. The final checker correctly exits nonzero for this missing link; rerun it after the root writes that record. All other checks remain clear. A harmless comment survives; a missing local link, missing npm script, invented environment name and malformed Bash each produce their specific error. The baseline exits nonzero on any unresolved errors or pending root-owned file.

`git diff --check` on owned paths found no whitespace errors. No application source/test was edited. These checks do not validate external-link availability in general, Markdown anchors, legal meaning, build/runtime compatibility, browser reachability, deployment hardening, full restore or scientific acceptance. The root must run any affected existing doc/guard tests and retain the consolidated verification record separately.

Memory quick pass used MEMORY.md lines 102-116 only to orient the known handoff boundary and current-identity rule; current task authorization and landing source supersede older run facts. No live operating fact was inferred from memory.
