# Continue v0.47 release, then M2d.4

Worktree `/home/nathaniel/.local/state/openplan/v047-release-2026-09-09`, branch `work/v047-release`, draft PR #111. Main was left unchanged at ccef7e2f. Source build 64bd6063 is running on loopback port 3257; confirm with `bash openplan/scripts/ops/which-openplan.sh http://127.0.0.1:3257`. Do not stamp later documentation commits onto that existing build.

Read VERIFICATION.md here, the current roadmap, the v1 contract and M2d.3 IMPLEMENTATION.md. The pending question asks permission to use the repository Playwright harness in an isolated Chrome profile. If authorized, use `chromium.launch({channel: 'chrome'})` from qa-harness dependencies in a separate profile/context, not the user's live Chrome profile. The original source download problem now reproduces only as the connected browser inspector blocking ordinary Documents navigation. Programs native saves work. Do not alter security headers to evade the tool's block.

The existing synthetic shared-cost credentials remain local at `/tmp/m2d3-shared-browser-fixture.json`; do not print or commit them. The named disposable stack is `supabase_db_m2d3-reimbursement-verification`, workdir `/home/nathaniel/.local/state/openplan/m2d3-verification`, API 58821 and DB 58822. The release checkout .env.local points there. No stack reset or destructive migration is needed. Local HTTP-check source and QA logs are retained at `/home/nathaniel/.local/state/openplan/v047-release-evidence-2026-09-09`. The public evidence files contain synthetic amounts and sanitized receipts only.

Finish native Documents original/corrected PDF/XLSX saves through real navigation, desktop/390px keyboard and console review, exact checksum comparison and unchanged-original retry. Reuse existing records. Current HTTP checks already deny anonymous and foreign-workspace metadata/bytes. The 250-test live suite and previous packet recovery evidence cover additional roles and exact command retries; do not call those native browser evidence.

Inspect CI/RLS on the latest PR head, finish relevant acceptance, update candidate wording in changelog/roadmap/known issues, merge safely, inspect main CI separately, and only then tag v0.47.0. Do not call the candidate released from its version fields. Preserve the reminder constraint proposal untouched and retain human PM/finance, prescribed-form and automatic-reminder limits.

Then start M2d.4 from existing Programs preparation/adoption baselines, actuals, reimbursement lineages and source reservations. Annual reconciliation must preserve outstanding claims, commitments/refunds, exact approval evidence and predecessor/successor mappings without copying physical expenses or treating acceptance as payment. The roadmap remains the sole queue and the complete v1 obligations remain in force.
