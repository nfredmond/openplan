# Public translation installation and live integration

Continues `c310690d` in the isolated translation-command worktree. This is unfinished development, not a release. The preceding goal turn made progress by committing and pushing the usage-reset checkpoint. V0.58.1 remains the latest released capability in this workstream; the old v0.48 objective wording does not restart that release.

## Installed authority and fault checks

Migration `20261014000020_engagement_public_translation_queue.sql` copies the proven candidate body with an installation-appropriate header. All existing copied migrations matched the worktree before copying migration 20 to the named isolated stack. `supabase migration up --local --workdir /home/nathaniel/.local/state/openplan/openplan-restore-target-2026091050` succeeded. The application database in `supabase_db_openplan-restore-target-2026091050` now reports 339 migrations through 20. Anonymous and authenticated users lack direct SELECT on the public mapping table; service_role cannot execute the internal create-attempt helper. The existing private proof databases remain quarantined and unchanged.

`prove-public-translation-http-client.py` completed 34 expected outcomes, including baseline, three harmless comments and 30 targeted faults. It exercises real route handlers and the React recovery hook with mocked queue/read orchestration and fetch. Source/hash/locale/request substitution, status recovery versus new creation, explicit predecessor retry, late-response cancellation, bounded polling, origin and agent markers, actual byte limits, strict response privacy and legacy-cache read failures are covered. It cannot establish native provider execution, database authority or browser visibility.

The first runner used an incorrect test title for a source-hash fault. That selected no tests and was not a real survivor. The corrected runner rejects empty selections and records assertion names. Overlapping cancellation/preview guards are removed together only where one would otherwise mask the other. Original source files were restored after every case.

The role scan initially failed because the new route delegates workspace resolution to the public queue helper. The scan now recognizes that actual named helper call and retains an explicit anonymous-authority exception. `prove-public-route-role-scan.py` has two harmless survivors and two expected targeted failures. Its static scan still cannot infer arbitrary aliases or other indirect helpers. Route/native SQL checks remain separate evidence. The adjacent role/preview suites pass 17 tests. An initial invocation from the repository root failed due incorrect working directory and was rerun from `openplan/`; the first correct run exposed the real scan gap.

## Browser work and discovered issue

The owned synthetic webpack server uses port 3260, the existing network guard and synthetic key/model. `which-openplan.sh` identifies this worktree by its live process. No paid provider call is authorized or needed. The application database had six completed, 36 cancelled and nine interrupted fields, none pending, before this journey.

`public-translation-browser.cjs` creates a synthetic campaign and approved meeting record through real UI navigation, opens the share link in an anonymous context, enters the comments page and exercises translation recovery. The actual worker wrapper intercepts the Anthropic transport and scopes every worker RPC to the named synthetic field. Planned evidence includes desktop/390px, lost queue/output acknowledgements, one retained provider attempt, original preservation and source mismatch refusal. Consult the latest runner result before claiming any of these passed.

Initial browser attempts revealed harness errors: duplicate Open portal links, a publish heading that changes after activation, and a missing click on the Community feedback tab. A navigation attempt returned to the map during the first development-route load; a separate keyboard/navigation diagnostic reached and stayed on the comments page. That observation is not yet proof of an application defect or of final acceptance. Failed logs and screenshots remain private.

A real separate UI inconsistency is visible in the saved setup snapshot: after the upper publish flow enables public submissions, the lower share controls still show Live / view only. The lower component initializes local settings from props once, derives the live status from those stale drafts, and sends all draft settings on Save. Repair `engagement-share-controls.tsx` before release so saved status reflects current server data and untouched settings cannot overwrite changes from the publish flow. Preserve user drafts and explicit clears. Existing share-control tests and fresh browser evidence must cover the repair.

## Remaining

Finish the current browser/worker journey and inspect screenshots/console, add installed public RLS evidence and explicit failed-attempt retry/privacy/revocation checks, repair the share-control inconsistency, then run final QA/shuffle/isolated RLS/workers/populated upgrade and final main CI before tagging. Public cache server abort guard coverage and stronger competing-reader cases remain useful follow-up checks. No human review or PR is required to continue. Full V1 and the current roadmap remain the destination.

## Completed browser run

The sixth run completed both 1440px and 390px journeys. `public-translation-browser-evidence.json` records the identified worktree, application/migration hashes, screenshot and private artifact hashes, worker phases and console evidence. I inspected both completion screenshots: the original comment and multiline translated words are visible, the machine caveat is visible, and neither viewport overflows. Both journeys use keyboard navigation and focus checks. The feedback panel has an empty map frame above the list in this no-map/no-coordinate synthetic setup; investigate its presentation separately before claiming polished portal usability.

Both lost POST acknowledgements recovered by GET without another POST. The actual SDK/worker dispatched exactly one intercepted provider request per journey. Losing the output-retention acknowledgement left a completed local journal, and resuming delivered the same output without another provider call. Selecting Original hides the translation; selecting Spanish again recovers the same durable request. A wrong displayed-source hash returns 409 without output. The only console errors were the deliberate dropped request and deliberate 409, with no page errors.

The first actual public worker run interrupted before reaching the intercepted provider because its synthetic wrapper compared raw newlines with JSON-escaped text. The wrapper now escapes the expected source consistently. The failed request `7da45587-8c1d-46f9-b40a-0918416109bf`, field `1c0420db-0a16-4d55-9319-336bd3ac0a88`, remains interrupted in campaign `80df8982-b2c3-4987-8de9-2267e91d497e`, item `eacaff0b-f1e5-411c-9b93-17fe6dbf4f23`. Preserve it; it can supply an explicit retry journey next. No synthetic provider event was emitted for that failed attempt. The wrapper predicate has baseline/harmless controls and faults for substituted model/source and the original multiline defect. Its first extraction regex could not parse the model-removal fault; the corrected extraction now fails on the intended assertion.

The installed public queue now has a regular opt-in live RLS test with the same byte-for-byte fixture used by the 41-case SQL candidate suite. The installed baseline passed. Its new permission-fault runner checks harmless grants with RLS still enabled and targeted anonymous/authenticated exposure, all inside rollback transactions. Check the final manifest and liveness before assuming that runner completed.

## Final checkpoint verification

The installed RLS runner is terminal with five expected outcomes: baseline, harmless comment and grant-without-policy survive; anonymous and authenticated row-exposure faults fail. RLS is enabled and both direct SELECT grants remain absent after rollback. The final database has eight completed, 36 cancelled and ten interrupted fields, with none queued/reserved/running. The new fixture's bytes are restored. Standalone TypeScript and changed-test ESLint both exited 0. Adjacent role/preview suites passed all 17 tests. Browser, mutation and verification jobs from this increment are terminal. The owned synthetic Next server may remain live on 3260; recheck its handle/process and checkout before reuse. No main merge, release tag or final release CI claim is made by this checkpoint.
