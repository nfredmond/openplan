# Generation resolution implementation in progress

Continues 1b14ed6c in the owned translation checkout. No resolution migration is installed yet. The app database remains 337/18; ordinary dev runs on 3260. This note is design and recovery guidance, not acceptance evidence.

Extend the current generation workflow with immutable, actor-scoped resolution receipts. Preserve an opaque JSON encoding of the exact damaged browser string, the reason, the original request ID and the outcome of each field. Multiple distinct browser copies may receive separate resolution receipts; retrying one resolution ID must reproduce its original payload/result. Do not delete or rewrite earlier generation output or translation history.

Resolve and create both take the existing exclusive campaign lock. Creation refuses a request already resolved by that actor in that campaign. This closes the absence/late-POST race without allowing one workspace's tombstone to deny another workspace's unrelated UUID. Existing global generation UUID conflicts remain transactionally refused. Resolution may cancel queued/reserved fields and interrupt running fields; it must disclose that a provider may already have run. Existing late-output custody must retain completion bytes without reviving interrupted work.

Read and write resolution receipts only with current staff access and the original actor. No marked Planner Agent write is allowed until a registered action exists. Preserve exact local bytes before archiving; if acknowledgement or local storage fails, retain a retryable resolution intent. A receipt resolves the old identity, not permission to silently generate a replacement.

Implementation seam: additive migration 19 and its SQL/native checks first; then typed receipt verification, authenticated API, queue preflight, browser pending-resolution recovery and explicit confirmation. All new guards need harmless and targeted controls. Run browser navigation, keyboard, 390px, late-retry and output-preservation checks before treating the UI as complete. Existing release gates and public generation durability remain open.

## Usage-reset checkpoint

This is an unfinished backup checkpoint, not a verified release or a main merge. Owned checkout: `/home/nathaniel/.local/state/openplan/translation-command-workflow-2026-09-13`, branch `work/translation-command-workflow`. Last tested checkpoint: `1b14ed6c0d2092a62c31c5ec6dd45616266f6350`; consult PORTABLE_ISOLATION_PROGRESS.md for retained results. Current new files are migration 20261014000019, translation-generation-resolution.ts and translation-generation-resolution-server.ts. They have not been tested or installed; API, queue preflight, UI and native/fault tests are still missing. Do not infer acceptance from this backup.

Resume by verifying checkout ownership, running jobs, current main/CI and browser identity. Another Codex session was active in the original checkout; preserve it and the demo. The existing ordinary dev process runs on port 3260. Recheck its identity and configuration before use. The named disposable application stack is `/home/nathaniel/.local/state/openplan/openplan-restore-target-2026091050`, container `supabase_db_openplan-restore-target-2026091050`, API 29821, database 29822. Last recorded installation is 337 migrations through 18. Do not install migration 19 before SQL/native verification. Do not attach workers to old proof queues.

First inspect the proposed SQL and receipt schemas; validate in a rollback transaction on the named disposable stack. Prove exact resolution replay, scope/privacy, simultaneous create/resolve, queued cancellation, running uncertainty and retained completed/late output. Add a resolution preflight before credential preparation while preserving the authoritative SQL gate. Add an authenticated, origin-checked, bounded API with executable agent refusal. Persist exact browser resolution intent before dispatch; retain it on lost acknowledgement and archive original bytes only after receipt and archive readback verification. Then exercise real navigation at desktop and 390px with keyboard, console inspection and targeted failures. Register migration/census/ordering checks before full QA. Public generation durability, release gates and full V1 remain open.

User direction persists: free/local, no draft PRs, directly merge verified work to main and inspect final CI before tagging; no human-review release gate. Preserve the complete V1 contract. A usage reset is not evidence that background jobs survived or resumed.
