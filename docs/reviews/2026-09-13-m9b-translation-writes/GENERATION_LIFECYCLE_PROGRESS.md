# Generation recovery across page and account changes

Continues `7c9d768b` in `/home/nathaniel/.local/state/openplan/translation-command-workflow-2026-09-13`, branch `work/translation-command-workflow`, package `openplan/`. The preceding turn made progress by verifying running-worker cancellation, worker suites and shuffled tests. This increment fixes a reproduced lifecycle defect. The full V1 goal and unfinished translation release remain active.

## What changed

Seven new delayed-response cases failed against the old generation editor. A late successful read could clear its retained browser request after unmount; a late queue acknowledgement could start another read after unmount or loss of edit access. List, view and recovery operations did not abort when access was lost and regained.

The generation editor now owns each asynchronous operation with a cancellation controller and current user/workspace/campaign identity. It checks ownership after response and body awaits and before applying verified results. Unmount and loss of edit access permanently abort the operation. A stale completion cannot retire recovery, publish output, show a stale error or release a newer operation's busy state. Restored access requires a fresh read before publishing previously viewed output. The current keyed parent remains part of the scope boundary.

Mobile account navigation had another concrete gap: CSS hid the sole account card and its sign-out action. The existing server sign-out action is now also rendered in the header at the same narrow breakpoint. Desktop retains its account card. No new auth endpoint or permission is introduced.

## Verification

All 42 focused editor tests passed, including nine added lifecycle/ownership cases. TypeScript and focused ESLint passed. `generation-lifecycle-controls.json` records baseline and harmless passes and seven targeted faults caught: ignored operation ownership, detached read signal, omitted unmount abort, ignored access loss, stale completion releasing a newer operation, publishing without a fresh post-access read and stale error display. Existing recovery controls were refreshed against 42 tests: baseline, three harmless changes and five targeted faults reached their expected outcomes. Mutation scripts restore exact source bytes and verify ownership before restoration.

The real browser journey in `translation-generation-lifecycle-browser.cjs` passed at 1440px and 390px. It starts from root/sign-in/Engagement/new campaign/Setup, saves original wording and queues an actual generation request, then holds the successful server read response. The user signs out through the visible action while that read is pending. The exact local recovery bytes survive late response release; an unauthenticated request is refused with 401. After signing back in to the same account and entering the campaign from the list, the same request remains downloadable and resolvable. Closing it cancels the queued field while preserving the original manual history row and saved wording.

Both viewports have keyboard activation, no horizontal overflow and inspected screenshots. The mobile sign-out action has visible keyboard focus and fits beside appearance controls. The browser also injected a harmless style comment, which preserved visibility, and a targeted style that hid the action, which the actual visibility assertion rejected. The final source digests and private captures are listed in `generation-lifecycle-browser-evidence.json`. No provider worker or paid provider call ran.

Errors and corrections are retained. The first test append used an extra `openplan/` prefix from inside the package, so it did not write the tests; the initial filtered run exercised none. The corrected run executed seven cases and failed all seven for the stated behavior. The first browser attempt preserved recovery across sign-out but failed on an exact campaign-link accessible name, which actually includes more text than its title. The runner now locates the real link containing that exact heading. Later functional journeys completed but failed the console assertion on a campaign-report poll returning 401 during logout. Instrumented evidence identified the same campaign reports URL and signed-out interval. The exception now permits only that exact 401 during logout until authentication returns, plus controlled aborted requests. Baseline/harmless and three targeted controls verify rejection of other phases, paths and HTTP failures. No page exception or unexpected console error remains in the final run. The server-side report denial is expected authorization behavior, not permission removal.

## Database, processes and next work

All fixtures are in `supabase_db_openplan-restore-target-2026091050`, API 29821/DB 29822, at 338 migrations through 20261014000019. The abandoned first selector-run field `e62c7f96-de94-4565-9388-dbe230ededfc`, request `bc8b258a-12c0-405b-a517-73581c1addf7`, was still queued after its browser context ended. It was cancelled using the existing `stop_translation_generation_field` service-role RPC with null attempt and `translation_browser_test_interrupted`; no source/output/history was deleted or invented. This is fixture cleanup, not browser acceptance evidence. The successful journeys cancelled their own fields through the UI. Recheck active states before starting any worker.

The last full RLS/worker/shuffled results precede this application edit; do not label them final-commit evidence. Rerun applicable full QA in the dedicated QA worktree at this increment, saving an explicit exit marker. That worktree is `/home/nathaniel/.local/state/openplan/translation-resolution-qa-20260913-125bf2a3`, originally detached at 125bf2a3, with existing dependency/worker-environment symlinks and private .env.local. Check ownership/status before moving its detached revision. Keep the original checkout and demo untouched.

Still inspect the resolution controller's own loss-and-restoration sequence: it checks current access, but its controller currently aborts only on scope cleanup, so revocation followed by restoration before a late body arrives needs its own reproduction. Same-account real sign-out is proven here; another-account switching and live role changes are not. Public-comment generation still needs durable reservation/recovery integration. Remaining release work includes worker transport boundaries, upgrade and final commit CI. Preserve the pending reminder constraint, direct verified main releases without PRs or human-review gates, free local operation and the complete nationwide contract with separate model validation.

## Full QA checkpoint

Full `npm run qa:gate` completed with saved exit 0 against implementation `e30996ae828dc2c425362ba84cf34c07d4645429` in the dedicated QA worktree, whose detached revision now matches that commit. It passed full lint, unused-code audit, 15,004 unit tests with 496 skipped, provider-connector tests, zero dependency vulnerabilities and the webpack production build including TypeScript. `generation-lifecycle-qa.json` records log checksums and excluded checks. This supersedes the old aggregate QA exit-status uncertainty for the current implementation. The optional live RLS hook is not a fresh RLS run. QA worktree is clean and tool job 17049 is terminal.

Browser jobs are terminal. The synthetic server was stopped and ordinary server PID 3083815 on port 3260 was verified against this checkout and checked for absence of the synthetic key/model preload. Its tool handle is 83114, and log is `translation-normal-server-after-generation-lifecycle.log` in the private evidence parent. Recheck its live identity before browser reuse. No schema change was made in this increment. The v0.58.1 tag ends at migration 20261014000009; a final populated release upgrade must cover all later migrations through 19, rather than treating the already-upgraded application stack as upgrade evidence.
