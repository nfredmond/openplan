# Resolution editor and browser progress

Continues the pushed usage checkpoint `10fc43ac` in `/home/nathaniel/.local/state/openplan/translation-command-workflow-2026-09-13`, branch `work/translation-command-workflow`; application commands run from `openplan/`. The preceding goal turn made progress by saving the unfinished helper and resumption instructions. This increment connects the real editor to the resolution API and verifies absent-request recovery. The translation increment is still unreleased, and the complete V1 contract remains active.

## Implemented behavior

Refused, uncertain and unreadable generation copies now offer a review and confirmation step. It retains exact stored and page-held copies before dispatch, pins the authenticated user and workspace at the HTTP boundary, verifies server receipt hashes and scope, and archives before retiring matching copies. A lost acknowledgement retries the same IDs, including when only the second of two copy acknowledgements was lost. Different raw JSON formatting remains a different retained copy. Page-held recovery survives source storage loss. Changed copies from another tab remain; stale async completion cannot clear recovery after unmount, workspace change or revoked access.

The existing blind archive of a refused generation has been replaced by durable resolution. Existing readable-request recovery remains separate. The false error saying both copies exist after an unsuccessful server read is gone. Browser inspection also found that successful resolution left the older generation-unconfirmed message visible; the integration callback now clears it, with a regression and targeted fault check. A new generation is always a separate action.

The original parent editor key was verified in `src/app/(app)/engagement/[campaignId]/page.tsx`: authenticated user, workspace and campaign. The new endpoint requires expected-user/workspace headers that must match real authenticated scope; they confer no permission. Anonymous, other-workspace and unregistered agent requests remain refused. Database migration 19 was already installed; this increment changes no SQL.

## Verification and corrections

- 38 storage/helper cases use real cryptographic checks with controlled Storage failures. Baseline and harmless change survive; 31 targeted faults fail. `resolution-recovery-controls.json` records source/test hashes and blind categories.
- 16 real React controller cases cover both copies, retained-before-dispatch, quota/readback, exact retry, second acknowledgement loss, corrupt/mismatched/refused receipts, changed source, unmount/access/workspace lifecycle and busy control. Baseline/harmless survive and 17 targeted faults fail in `resolution-panel-controls.json`.
- The 33 original editor tests include the replacement archive path and stale-message regression. Baseline, three harmless controls and five targeted faults pass their expected outcomes in `generation-recovery-controls.json`.
- The API now has 28 tests; queue tests remain 52. Refreshed baseline/harmless and 24 targeted controls include both expected-scope headers in `generation-resolution-api-controls.json`.
- TypeScript and focused ESLint pass. Final full unit run: **14,988 passed, zero failed, 503 skipped**, exit0. Private files `translation-resolution-editor-final-unit.json` and `.log` retain the result. The run immediately before the final copy fix had 14,986 passes and one real copy-ledger failure. The new heading said “Resolve”; it now says “Close this retained request?” without increasing the jargon baseline. The earlier missing-production-caller failure is closed by the actual editor request.

The first test fixture used a string instead of the existing source-version object; corrected before control runs. One access control test initially missed hidden preparation because the panel was invisible. It now restores access and checks that no hidden candidate was created. The initial workspace fault survived because abort remained protective; the combined identity/abort fault exposed a second testing weakness: assertions ran before asynchronous hashes could finish. The test now verifies that stale responses never start cryptographic verification. The corrected combined fault fails. These failed attempts and their logs remain in the private control directories; their earlier green assertions were not sufficient evidence.

## Actual browser acceptance

`translation-resolution-browser.cjs` entered through root sign-in, Engagement, New campaign and Setup using the repository Chrome harness and keyboard activation. It saved original manual wording, created a browser generation intent through the actual UI, interrupted generation before dispatch, damaged the stored copy while preserving the page copy, lost the first actual resolution acknowledgement, retried the same IDs, and downloaded both pending and final archives. Each final receipt exactly matched PostgreSQL payload/result text and checksums. The original manual wording and history were unchanged. Late creation returned 409, a changed workspace header 403 and an anonymous resolution 401. No generation request was created and no provider was called.

Both 1440px and 390px passed. Screenshots were opened and inspected; text/buttons wrap, keyboard focus is visible and document width equals viewport width. Private evidence is `/home/nathaniel/.local/state/openplan/response-write-probe-20260913/translation-resolution-browser-1789362173300`; `resolution-browser-evidence.json` records source/artifact hashes and scoped results. Desktop campaign 435b63b5-5e68-45ad-b9e6-559e9da44755, mobile 42b6835d-7729-45f9-953e-f46599c71997. Both remain synthetic drafts.

The first attempt under ordinary configuration correctly lacked a generation button because no provider was configured. Its saved manual draft remains. The second desktop run completed functional/download checks but its overbroad console assertion rejected development font/CSS preload warnings; screenshot inspection found the stale-message defect above. The final run retains 28 desktop and 6 mobile preload warnings, zero page exceptions, and exactly two intentional aborted-network errors per viewport. These are not a clean production-console claim. The actual geometry/console predicates have harmless and three failure controls in `resolution-browser-guard-controls.json`.

The owned 3260 server was temporarily launched with synthetic Anthropic/model settings and the existing provider-blocking preload. Its baseline/harmless/allow-provider fault was rerun. `.env.local` was not edited. The synthetic server was stopped after both browser contexts ended, and 3260 was relaunched with ordinary configuration. Reidentify the served checkout before more browser work; never assume its process survives a reset.

## Current boundaries and next work

The application stack remains `supabase_db_openplan-restore-target-2026091050`, API 29821/DB 29822, 338 migrations through 20261014000019. Last read: 28 cancelled/four completed fields, none queued/reserved/running. Authenticated resolution RPC allowed; anon/service RPC and direct authenticated resolution updates denied. Do not rerun the old preactivation candidate-DDL runner on this installed database. The preceding full isolated RLS 525-pass result is retained evidence, not rerun here.

Next extend browser acceptance to closing an already retained completed/running request while preserving usable original output, and to actual archive failure/retry and account-lifecycle transitions. The new browser journey covers absent identities only; native SQL covers other states but does not substitute for their joined browser workflow. Review older generation list/read/send async lifecycle handling, which predates this new controller. Capacity beyond browser storage quota remains bounded by preserved copies and explicit retry/download; no arbitrary-corruption or unlimited-capacity recovery claim is made.

The public comment producer in `src/app/api/engage/[shareToken]/items/[itemId]/translate/route.ts` still directly calls `translateEngagementText`, checks allowance before generation, meters afterward without awaiting, and treats cache failure as nonfatal. It needs durable generation/reservation/recovery integration. Worker transport controls, production 404 console investigation, applicable full QA/shuffle/worker/upgrade checks and final main CI remain release work. No human-review gate was added.

Live remote main was rechecked at `ef16f166447ab477ea36588a5c01620f61560b19`: CI 34779793816 and RLS 34779793752 succeeded. Existing v0.58.1 points to b330bcc8785dfbe54b6a591c7aafdf7ab321b8da. These checks do not validate this unpublished branch. Original checkout and demo were not changed; another Codex process remains active there. Preserve the pending reminder constraint. Continue directly toward verified main merges/releases and the full nationwide planning and separate model-validation obligations.
