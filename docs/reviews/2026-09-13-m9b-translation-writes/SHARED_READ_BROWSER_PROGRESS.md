# Shared generation reads and browser recovery

Continues the pushed usage-reset checkpoint `b87fe6b1`. Work remains on `work/translation-command-workflow` in `/home/nathaniel/.local/state/openplan/translation-command-workflow-2026-09-13`. This increment is not a release. The full V1 objective and direct-main, no-PR authorization remain active. Original checkout, demo and pending reminder constraint are untouched.

## Demonstrated defect and correction

The second generation browser run had completed publication and acceptance, then history returned 503. A private diagnostic copy of the native reader and twelve concurrent authenticated reads isolated the error to the generation RPC: `PT503`, `Translation sources are busy`. Three concurrent browser reads independently returned 200, 503, 200. Saved history itself was intact. The original reader used the exclusive write-side advisory lock and campaign row lock. Concurrent readers rejected one another, and a history containing several generation requests could contend with itself.

Additive migration `20261014000018_engagement_translation_shared_reads.sql` gives detail and catalog readers their own shared advisory and campaign-row locks. Membership rows remain locked for share, and the initial and locked staff checks remain. Existing generation writers keep their exclusive helper. Public/anonymous execution and direct saved-translation writes remain denied. The new helper is private. No retained records were rewritten.

`prove-generation-shared-reads.py` holds actual transactions open across independent psql sessions against the retained browser-created campaign. Before the migration it failed `concurrent detail readers coexist`. Afterward baseline and harmless control survived. Six targeted faults failed their intended checks: exclusive advisory lock, exclusive campaign row lock, omitted advisory lock, omitted campaign lock, omitted membership lock and viewer access. Detail/catalog coexistence, exclusive writer refusal and anonymous refusal also ran. The mutation transactions rolled back; history, receipts and function definitions matched their pre-test digests. Structured before/after reports are beside this note. These are local retained-fixture controls, not yet a portable CI concurrency test.

## Browser and worker evidence

The complete generation journey passed at 1440px and 390px from login, Engagement, new campaign and Setup. It created an original manual version, lost the generation acknowledgement, downloaded and retried the identical request, ran the real worker and SDK through a local synthetic provider transport, lost the retained-output acknowledgement, recovered delivery with one provider call, published the exact retained output, lost and retried publication, accepted that output, and verified three revisions including the unchanged original. Captured output preserves the test's NBSP/BOM boundaries. Each width recorded only the two intentional aborted-response console errors, with no page exceptions.

Private evidence is `/home/nathaniel/.local/state/openplan/response-write-probe-20260913/translation-generation-browser-1789354327298`; source/artifact hashes and scoped results are in `generation-browser-evidence.json`. Desktop campaign: `6331274d-22e1-4e56-a686-f6d10b13d61b`. Mobile campaign: `89f5c220-b7f6-4297-8e4b-a036e5e3e611`. Both remain draft. This proves saved publication and recovery, not public portal visibility or translation quality. No paid provider call was made. The server preload also has baseline, harmless and targeted allow-provider controls in `generation-server-network-controls.json`. The worker transport's other failure branches still need explicit controls.

Image inspection found the generation refresh button clipped within the mobile panel even though document width was exactly 390px. The browser geometry probe reproduced a 274px button inside a 242px parent. Long generation controls now use existing wrapping styles. `translation-generation-mobile.cjs` checks visible button geometry against its immediate parent as well as text overflow. Baseline and a harmless border-color change pass; restoring nowrap/unbounded width fails the intended clipping assertion. Corrected screenshots were inspected. The native history layout remains tall and dense on phones. Hidden recovery controls and the entire mobile application are outside this probe's coverage.

## Runtime and validation

Application database: `supabase_db_openplan-restore-target-2026091050`, database `postgres`, API 29821, workdir `/home/nathaniel/.local/state/openplan/openplan-restore-target-2026091050`. It now permanently has **337 migrations through 20261014000018**. All prior migration copies matched the owned source; the candidate parsed inside a rolled-back transaction before installation. The CLI was accidentally invoked from the repository root, but explicit isolated workdir targeted the correct stack and the applied migration and count were verified. Use the application cwd for future npm commands. Four synthetic generation fields are completed; none is queued/reserved/running. Authenticated command execute remains true, anonymous false, authenticated direct UPDATE false. Preserve the older pre-activation and worker proof databases.

Temporary synthetic server settings have been removed. The owned Next CLI 754495 was stopped after cwd/ancestry verification. Ordinary webpack dev is running on 3260, tool session 60410, launch shell 1065581, CLI 1065976. Revalidate these historical IDs before any process operation. `/proc` inspection confirms no synthetic key/model or guard preload; `.env.local` was unchanged. `which-openplan.sh` identifies this owned checkout. Three concurrent browser history reads under the ordinary server returned 200 with three revisions each. There are no remaining browser runners or workers.

Full unit run: 1289 files passed, one release-ordering file failed, 47 skipped; 14855 passed, one failed, 462 skipped. The failure caught the omitted migration-18 changelog entry. That entry is corrected; all six release-ordering tests pass on rerun. Four focused translation suites passed 78 tests. TypeScript and focused panel lint pass. This is not a new full QA, shuffled, RLS, worker or upgrade pass. The previous full RLS evidence predates migration 18. Direction check passes with the existing version/age reminders.

## Next work

1. Make the shared-read concurrency regression portable in the existing isolated live test harness and run current RLS against migration 18. Active manual/access browser wrappers still expect 336/17 and need an honest installed-head update before reuse; do not restore temporary grants.
2. Complete outstanding generation/editor recovery and worker-transport controls, including unreadable copies, quota/readback/archive failures, catalog pagination and lifecycle. Keep complete proof requirements scoped to actual changed behavior.
3. Finish the public comment translation producer's durable reservation/recovery gap. It remains the legacy inline producer; no claim of completion is made.
4. Review production browser behavior, including the separately recorded React script-tag console error on 404, then run applicable full QA, shuffled tests, isolated RLS, worker and upgrade checks. Join current main safely, inspect final CI and release directly without a PR or human-review gate when the bounded release is actually ready.

The first browser runner's missing confirmation-dialog and operation-name mistakes, the unit-run changelog omission, and the initial access mutation hitting an earlier membership check were harness/engineering errors. The access fault was narrowed to admitting viewers so it fails the intended assertion. One mobile probe creation used the wrong relative directory and failed before launching; it was corrected to the owned absolute path. No such failed run is counted as acceptance.
