# Translation access and independent header patch

Current owned checkout: /home/nathaniel/.local/state/openplan/translation-command-workflow-2026-09-13, branch work/translation-command-workflow. This follows 9f6c4b94. The separately committed header repair is 05aa194b. Ship that independent bug fix as a verified v0.58.1 patch from an isolated main checkout before resuming the larger translation-generation implementation. See WORKSPACE_SWITCH_FIX.md. Do not merge the unfinished translation branch to release it.

## Completed access journey

On the identified owned webpack server at 127.0.0.1:3260, the retained synthetic campaign was reached through normal navigation by the owner and the existing viewer. That viewer originally signed up and accepted a manual invitation through the real UI. Fresh browser sign-in chooses its own workspace, so the journey explicitly switches to the invited workspace using the visible header control. New outsider accounts signed up through the normal UI into their own separate workspaces. No existing membership was edited and no invitation or external message was sent by this increment.

At 1440 and 390px, staff could open the six linked private history entries, including the retained withdrawn original, and replay the original confirmed request. History before and after replay remained identical. The viewer could read the complete current snapshot and current wording, had no history/save controls, and received 403 for private history and the owner's original request replay. Its refusal bodies contained no history or result rows. The outsider's campaign list omitted the target, direct navigation showed the not-found page, and snapshot/history/command requests returned 404 with no protected payload. Anonymous equivalents returned 401. Private response caching was checked for viewer and outsider refusals. This is evidence for those fixture roles and paths, not every engagement permission boundary.

Baseline and harmless pass-through browser runs completed both widths. Two deliberate response-body leaks failed the intended checks: adding private history to a viewer 403, and adding a campaign snapshot to an outsider 404. The faults operate in browser response interception; they prove the new assertions can reject leaks but do not mutate database RLS or application authorization. Existing installed RLS and route mutation evidence covers those separate boundaries. See translation-access-browser-controls.json.

The grant wrapper now selects the access harness as well as the editor harness. Normal access runs and both fault exits revoked the temporary command grant. Explicit containment controls also ended with exit 0 and forced child exit 23, both with the grant revoked. The final default remains revoked. App schema is unchanged at 331 migrations through 20261014000012 on supabase_db_openplan-restore-target-2026091050, API 29821 / DB 29822. The stack, originals and synthetic history remain intact.

## Findings, failed probes and console

The access journey exposed two real header defects, now fixed in 05aa194b: CSS hid the entire workspace switcher below 960px, and overflow on the label clipped the desktop menu. Separate before/after screenshots and menu hit-testing established these failures. Keyboard-only selection had previously concealed the clipping issue. Header evidence covers 1440/900/390/320px, including reload persistence and harmless/hidden/clipped controls. The phone workspace card now uses its own header row.

Initial probe failures are retained. Prefix translation-access-1789326648719 stopped at the viewer's empty own workspace. Prefix translation-access-baseline-1789326807954 completed desktop then hit a listbox selector shared with the mobile colour picker. Prefix translation-access-baseline-1789326912353 used the named workspace selector and exposed the actual hidden control; the parent proof runner initially masked this with a missing-result FileNotFoundError. It now records a missing success artifact as an unexpected outcome instead of assuming it exists. None is recorded as a successful full journey.

Viewer screens were inspected at desktop and 390px, as was the outsider's phone not-found page. Baseline access console records include 18 expected failed-resource messages for refused requests/navigation, preload warnings and two React script-tag messages on outsider not-found navigation. The latter's cause remains unassessed; no page exception was recorded. Do not call the console clean or infer that the script warning is a permission leak. Header probes also retained map/GIS fetch warnings during navigation/reload; their cause was not independently isolated.

## Resume

All access, header, type, lint and focused-test processes have ended. The owned webpack server may remain on 3260; reidentify before reuse or stop only that owned process when preparing the main patch build. The original checkout still belongs to another active Codex session. Keep it and the demo untouched. Header source before this increment matched main exactly, so its isolated commit is a clean separation point. Recheck remote main, tags and CI, then run applicable full QA, shuffled tests and isolated RLS/release checks on the patch candidate before landing directly to main and tagging v0.58.1. No PR or human review is required.

After the header patch, return to this translation checkout and finish remaining pending-request race/deletion boundaries, machine-acceptance journeys, durable generation, retained publication, dispatch accounting, cache provenance and legacy producer conversion. GENERATION_JOIN.md records worker reuse points. The current generic AI limiter fails open and records successful events best-effort; it is not adequate proof of durable translation dispatch accounting. The workspace integration helper also falls back to environment keys on read failure, so attempt configuration must be bound explicitly instead of silently switching credential sources. These are inspected design constraints for upcoming work, not implemented generation fixes.

Full translation QA/shuffle, standard isolated RLS, worker/upgrade/restore and final main CI still precede that feature's release. M9b and V1 remain incomplete. v0.58.0 is already published; do not restart the obsolete v0.47/v0.48 plan. Keep operation local/free and preserve the pending reminder constraint and separate scientific claim boundaries.
