# M2d.4 multiple carryover allocations

Release disposition: included in [v0.48.0](../2026-09-09-v048-release/VERIFICATION.md). The dated checkpoints below retain their original findings.

Engineering increment, unreleased. Local engineering checks and inspected browser acceptance passed. Final main CI is checked separately on the commit that lands this evidence; a push alone is not acceptance.

Extends Saved reconciliation with multiple source-fund and adopted successor work/fund allocations per old work element. Several old elements can share a successor. The read-only legacy projection preserves original approval JSON, hashes and exact pending retry payloads. New arrays require null legacy scalar mapping fields. Source and successor fund ceilings aggregate the rows and retained approved reservations, including legacy mappings and successor amendments. Reopening keeps the previous approval reserved until replacement approval. No expense is copied.

Owned checkout: `/home/nathaniel/.local/state/openplan/m2d4-multi-carryover-2026-09-09`, based on `609347ad`. Named disposable stack: `supabase_db_m2d3-reimbursement-verification`, API 58821, database 58822. Migration 311 is additive and changes functions only. Upgrade preservation runs transactionally on `supabase_db_m11-contract-verification-upgrade`; neither stack is reset.

The first extended cross-cycle test had a SQL operator-precedence error in fixture construction. The harmless mutation control failed, so none of that run counted as mutation evidence. Parenthesizing the JSON expression corrected the fixture. Initial evidence is retained separately.

Limits: amounts are reviewed carryover authority, not available cash. Funding vintage, legal conditions and real authority depend on the retained human evidence. Independent two-cycle reconstruction, exact restore and practicing-finance usefulness remain open. These checks do not prove bank transfers, agency-prescribed forms, automatic reminders or full OWP administration.

## Engineering evidence

- Full local `OPENPLAN_RLS_GATE=1 npm run qa:gate` at `e2637dd7`: lint, existing deadcode baseline, 13,451 ordinary tests, 321 live tests across 40 files, zero production dependency vulnerabilities and webpack build passed. Shuffled tests independently passed 13,451 tests. All 52 worker suites passed using their existing separate local environments. The fresh checkout initially had no worker environment links and correctly reported all suites unrun; no skipped result is counted as a pass.
- The final display correction at `e8c3d7f25e521e2767d2cd5bedf14b64af707752` adds wrapping source and successor fund/vintage references. Focused UI/route checks passed 15 tests, display mutations were detected, and the final production build passed. The application served on 3265 is this build, as recorded in `build-identity.txt`. Subsequent landing evidence changes do not change the application.
- `sql-mutations.json`: harmless comment survived; 16 targeted breaks were detected. They cover array projection, malformed/ambiguous/empty/excessive lists, cents, completed work, destination/source identity, combined source/successor ceilings, old array approvals, reopening reservations and successor amendments. The malformed-list mutation proves the intended rejection message rather than admission of a bad write. Removing both redundant amount checks is necessary to test missing cents validation. `database-function-custody.json` confirms installed function bodies match migration 311 after rollback.
- `ui-mutations.json`: harmless comment survived; 12 targeted changes were detected, covering exact legacy command identity, array cents/length, legacy/new/empty readers, distinct row updates, dependent-choice clearing, removal, amounts and displayed funding vintages. SQL and UI checks cover different layers.
- `upgrade-results.json`: the named upgrade stack retained 12 tables' nonempty row counts and JSON checksums, including approved reconciliation, period closure, reports, claims and physical source entries. The harmless comment preserved them; changing a saved program was caught. The original 306-migration stack remains unchanged because each exercise rolls back. Released v0.47 migration bytes are checked against the tag; merged 308–310 are applied inside the transaction before 311.

## Browser acceptance

`browser.cjs` starts at the front door, signs in, follows Programming Cycles, opens the program, chooses Administer a reporting period and Saved reconciliation. Both 1440px and 390px journeys use the identified final build and keyboard navigation, save and approve three allocations across two successor cycles, and download the actual retained JSON. The original source and prior refund approval remain unchanged. A deliberately incorrect build identity fails before financial navigation.

The desktop save loses its response after server success. Reload and exact retry create one retained draft, not a duplicate. The mobile correction changes the third allocation from 4.00 to 3.00 while retaining the original approved 10.00/5.00/4.00 split. Approved versions are 58 and 61. The original download SHA256 is `68546de463eda8f69f1de63c8a9fb13933ef58dae4bbc4431218d6508af6741d` at both widths. The correction is `f8a31540ffeca5bf78b8eeb10e39f20030450355a71f9534ecc07fc6c2ddf8f3`.

All 61 retained source/content hashes validate. Four physical entries remain, including the original 12.35 cost. The unpaid claim remains 3.00 and remaining refund 1.00. A 31.00 allocation to a 30.00 successor fund is refused. An interrupted private-history read hides exports and recovers on reload. Owner reads succeed; anonymous, member and foreign-owner reads/writes are denied. The original closure and earlier approvals are unchanged. Console inspection found only the injected failed save and history-503 errors; there was no unexpected console error or horizontal document overflow.

The first browser attempt waited for the approved Reopen button to become enabled even though new authority evidence was blank. The application had correctly saved approval 49. Correcting the wait to observe the returned approved state fixed the instrument. Separately, the first 390px inspection found a clipped source-fund selector. The final display correction provides wrapping fund and vintage references beneath the selectors. Synthetic successor preparation initially expected 200 instead of a valid creation 201, then duplicated budget identifiers while cloning work. Both preparation-script errors were corrected; the already-created program was reused and all work-program/adoption data came through product producers.

[Desktop allocations](split-funding-1440.png) · [390px controls](split-funding-390.png) · [390px full fund references](fund-references-390.png). Raw downloads and login fixtures remain local and are not committed.

## Boundaries and next work

The SQL checks can see retained baseline identities and arithmetic, not real cash availability, external funding conditions or a responsible person's actual authority. Client tests cannot prove database isolation or browser geometry. Browser evidence covers labelled synthetic examples and does not prove practicing-finance usefulness. The upgrade checks preserve selected relational records; they are not a full backup/restore drill or independent two-cycle reconstruction. Latest approved carryover reservations remain per source program, as before; this increment does not introduce a separate cross-baseline funding ledger.

Next is independent reconstruction of overlapping cycles and unresolved claims, followed by exact restore evidence. Full M2d.4, bank verification, prescribed agency forms and finance acceptance remain open. No reminder constraint, automatic reminder behavior, provider priority or scientific claim changed. There is no tag or claim that this increment has been released.
