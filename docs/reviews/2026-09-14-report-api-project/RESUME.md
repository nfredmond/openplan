# Resume at v0.61.1 final CI and release

The optional-project API fix is complete and locally verified; final main CI and publication remain. Use /home/nathaniel/.local/state/openplan/translation-command-workflow-2026-09-13, package openplan/, branch work/engagement-decision-traceability. Other session owns /home/nathaniel/code/openplan; leave it read-only. No PRs or human release gates.

Read RELEASE_VERIFICATION.md, browser-results.json, mutation-results.json and v0611-release-records.json. v0.61 is already published at b3514277, with publication docs on main 3cd87d4a. The current fix checkpoint b0b947b3 passed full QA (15,362 tests/533 skips) and identified browser/API journeys at 1440 and 390: null-project report GET 200, actual project controlGET 200/nine sections, original download hashes, anonymous denials and empty consoles. No application source changed after that QA/browser build. Release metadata is aligned to 0.61.1 and its ledger proof passes.

The release candidate is already committed and pushed to main and work/engagement-decision-traceability at 291ce88a600547c1cd2b8b3d85047d5d07c6df44. On September 14 at this usage-limit checkpoint, CI run 34877113299 and RLS Isolation run 34877113339 were still in progress on that exact commit. CI operations, worker and modeling jobs had succeeded; full QA, shuffled tests and RLS had not finished. Recheck live results; this is not a green release claim. Next: inspect those exact-commit runs to terminal success, then annotate/tag/publish v0.61.1 at 291ce88a600547c1cd2b8b3d85047d5d07c6df44. No new migration/worker change requires another upgrade rehearsal. Keep publication records separate from candidate claims and continue the full V1 goal afterward.

Owned server 2648412 was stopped after cwd/ancestry verification once browser acceptance completed; no document worker or browser remains running. Do not trust old PIDs after a reset. Local isolated stack is restore-target API 29821 / DB 29822, 343 migrations through 20261014000024. Do not reset/drop it or disturb the demo. Private artifacts are in /home/nathaniel/.local/state/openplan/response-write-probe-20260913/decision-context/; current full QA log is report-project-api-full-qa-b0b947b3.log. Pending reminder constraint remains untouched.

For the next substantial M9b work, inspect the existing synthesis route/panel/library against the full roadmap: the route reads only 300 approved rows, the library's claimed total is the length it receives, and save errors still return a success-shaped result. These are source findings requiring a bounded reproduction and retained-source design, not permission to narrow participation or invent data. No next synthesis implementation is started yet. Continue current product direction and all V1 obligations, including distinct nationwide model validation.

## Usage-limit recovery

The user plans to return to this same thread after the weekly usage reset. Resume the existing full V1 objective without asking for permission or starting a new plan. Read this checkpoint and ../2026-09-14-m9b-synthesis-custody/INITIAL_FINDINGS.md. Its cap-reproduction.json retains the controlled 300/301-item result; no synthesis implementation has started. Commit and push these notes as a documentation-only checkpoint. The release tag belongs on the verified release candidate above, not automatically on a later documentation commit.

The GitHub watcher was alive as PID 2751062 when inspected; it is optional and should finish by itself. Its private log is report-project-api-ci-watch.log under the private artifact directory above. Do not rely on this PID, tool handles, browser tabs or servers surviving a reset. Query GitHub directly, inspect ownership again, and restart only needed owned services. Credentials remain in local private files and are not copied into this record. No new models, downloads or migrations are running in this workstream.
