# Contract reconciliation development record

User-approved v0.46 scope: project/engagement navigation, versioned original/current approved scope and budgets, task/staff/deliverable allocation, effective cost and billing rates, manual/CSV/opening sources, immutable corrections, distinct cost/commitment/gross billing/retention/documented cash, dated remaining estimates, Documents PDF/XLSX snapshots, isolated adverse/upgrade/browser/worker verification, CI/release and retained-demo refresh. M11 remains open beyond this increment.

Starting main: a0aed946, v0.45.0. Existing CI and RLS success inspected. Direction checker succeeds with honest v0.44 review reminders. Working checkout: /home/nathaniel/.local/state/openplan/contract-reconciliation-2026-09-07, branch work/contract-reconciliation. Root owns implementation; independent reviewer is read-only with /tmp scratchpad. Other checkout services remain untouched.

Reuse: existing engagement hierarchy, staff identities, project deliverables, source time/spend records, v0.45 exact decimals/private cost rates/history principles and Documents leased exports. Contract records do not require an OWP. Approved baselines require retained source documents and recorded approval evidence; proposals do not change authorization. No legal, accounting or practitioner-usefulness claim follows from synthetic engineering acceptance.

Implementation and acceptance are in progress. No v0.46 release claim yet.

## First implementation checkpoint

Added contract baselines, tasks, cost/billing rates, actual versions, dated estimates, immutable snapshots, retained CSV imports and task/staff assignments. Commands serialize with OWP's workspace source lock, enforce request identity and optimistic versions, and preserve original source valuations. Contract source billing creates transactional split invoice lines with gross retention and source links. Documents uses the existing leased export job and private artifact controls. Project and engagement entry points, staff entry, management forms and browser save recovery are implemented.

Independent static review identified six blockers: appendable issued invoice lines, historical invoice timing, currency relabeling, stale OWP valuations, mutable engagement parents and unreconciled budget allocations. Corrections are in progress with regression coverage. Review also required task/staff budgets and billing attribution in exports; those tables have been added. A generic history trigger initially referenced a baseline-only field and failed snapshot mutation; a later qualified SQL variable failed to resolve. Both were caught in the isolated tests and corrected. Do not infer acceptance from migration application alone.

Current targeted evidence: 113 tests passed across contract reconciliation, legacy gross receivables, time routes, corrected direct-spending budgets and My Work query suites. Six isolated database groups passed for retry/correction/privacy, foreign attribution, effective rates, split billing, baseline currency/budget/parent boundaries and snapshot history over 1,000 rows. New contract files pass focused ESLint. Whole-checkout TypeScript additionally reports five pre-existing unsupported `exact` options in work-program-staff-link.test.ts; no new application type errors were reported by the last run. Full QA and mutation proof remain required.

Local owned resources: disposable Supabase contract-verification on ports 58321-58324; workdir /home/nathaniel/.local/state/openplan/contract-verification. Application dev server :3246 serves this checkout, identity checked with which-openplan.sh. Documents worker uses this checkout's private .env.local and an isolated export folder. Owned browser server endpoint is retained privately under /tmp/openplan-contract-browser/endpoint. Browser connector discovery returned no browsers; repository-supported installed Chrome/Playwright fallback is in use. Synthetic owner and project were created through real signup/Projects UI, not database business fixtures. Actual contract journey is unfinished.

Setup error: startup-log output accidentally exposed disposable local Supabase test credentials. No client records were involved; subsequent setup output is filtered. Two early commands ran from the repository root instead of the app package and were rerun correctly. These failed attempts establish no verification evidence.

Remaining: finish shared-source/opening/concurrent billing/import/issued-invoice insert tests and mutation controls; reconcile all relevant existing tests, RLS census and live command inclusion; exercise full owner/member/import/amendment/correction/report journeys desktop and 390px; inspect downloaded PDF/XLSX and worker retry; populated predecessor upgrade and full QA/CI; version/changelog/roadmap/limitations; main integration, v0.46.0 tag and retained v0.45 demo refresh. No release or human-usefulness claim yet.

## September 8 verification checkpoint

Full QA gate passed before the latest custody additions: lint, deadcode, 13,291 unit tests, production dependency audit and webpack build. The explicit isolated live command passed all 184 tests across 24 suites. After adding payroll overlap review, deletion evidence and version-based estimate selection, 61 focused contract/catalog tests passed. These results are checkpoints, not final acceptance of subsequent edits.

Independent static review found and prompted corrections to additional private-rate, source identity, historical agreement/deliverable, gross credit timing, shared-currency, post-cutoff mutation and estimate-order boundaries. Mutable legacy values and their cutoff-conflict evidence now enter one SQL statement snapshot. A private immutable deletion register prevents historical cutoffs from silently losing a deleted legacy source. Approved payroll requires explicit reconciliation against other current contract/OWP/legacy time for the same workspace staff and day. Source keys and physical identities remain separately unique. Final independent confirmation and mutation proof remain pending.

Browser acceptance uses isolated synthetic workspace 0ccb6da6-c9e4-42f3-9a5f-833e8835ebdf and project 119daca9-be3d-43bc-92a8-9fe90e95f445. Engagement fed59a17-4e87-4dca-9204-2d2cc0f8420e was created from the existing register, reached through Projects and linked to My Work. The original approved baseline was fee 1000/cost 500/hours 10. A proposed amendment left those totals unchanged; its approval changed them to 1200/600/12. Task/staff budgets and agreed deadlines remain separate from incurred effort and independent progress.

The real UI recorded split time, a separate nonbillable rework entry, mapped CSV expense intake with exact retry, partial payment and gross fee credit. Correcting already billed time from 2.00 to 2.20 hours retained the original 200 gross invoice, 20 retention, and immutable earlier snapshots. Current synthetic position is incurred cost 185.50, 75 documented payments, 10 credit, gross fee remaining 1010.00. Task/staff/deliverable sums reconcile. These are synthetic records and establish no real client expenditure or authority.

At 390px, a keyboard-submitted save was deliberately interrupted before reaching the server; reload and retained-request retry saved one source. A separate fresh console review and response-loss/concurrent retry checks remain necessary. Browser evidence and downloaded originals are retained under /tmp/openplan-contract-browser/evidence. No client data is involved.

Export defect found through inspection: the first issued XLSX skipped the existing shared formatter and printed across 57 pages. The renderer now uses the shared formatter with a protected read-only option; a newly issued immutable workbook rendered to 18 pages, one per sheet. Its PDF counterpart is readable. The defective first issued files remain retained history; the new snapshot supersedes them for formatting. Full final export arithmetic and page inspection remain pending. The initial bad Documents link was corrected to the actual /knowledge-base route and exercised.

Mutation proof at /tmp/openplan-contract-proof/results.json currently records 15 successful controls: two harmless survivors and 13 targeted failures. Added workbook/privacy/cutoff/payroll/estimate controls still need execution. Blind categories include real human usefulness, external accounting evidence, and scientific accuracy. No such claims are made.

Remaining release work: final member entry and privacy browser journey; opening/commitment and final amended/corrected export reconciliation; response-loss/concurrent billing and worker retry; clean/populated v0.45 upgrade; current complete QA/isolated RLS/worker checks and CI; final identified-build desktop/390px evidence; version/changelog/roadmap/limitations, main integration, v0.46.0 tag and local demo refresh retaining v0.45. Own services and branch remain separate from main and its demo.

## Release candidate preparation

The complete member journey exposed two additional defects: a redacted nullable spending ID broke draft correction, and owner approval left members looking at their earlier draft status. Both are fixed. Member responses retain original member input, current review status/hours and no owner valuation notes; approved correction controls are disabled. A focused form regression and live status/privacy checks cover the boundary.

Latest complete fresh-schema isolated RLS run passed 187 checks across 24 suites, including contract export retry, expired lease refusal and private indexing. All eight migrations installed from scratch. The populated v0.45 upgrade retained original records byte for byte. Source attribution moves now retain departure evidence and original creation timestamps cannot be rewritten after contract custody begins. Current-source OWP/time/spending pickers reuse the prior OWP pattern; browser mapping preserved the existing physical time identity and excluded an explicitly synthetic probe without changing contract totals.

The final synthetic snapshot, dated 2026-09-08T07:35:19.046704Z, is 10743383-1fb0-42c4-8f85-5530840f8c54. Downloaded PDF/XLSX, exact reconciliation JSON, page contact sheet, member privacy capture, concurrent billing and upgrade evidence are retained under evidence/. It reconciles incurred 249.50, commitments100, payments75, credits10, hours4.80, gross billed200, retention20, fee remaining1010, remaining estimate400 and actual-plus-remaining649.50. Twenty-four PDF pages and eighteen workbook sheets were inspected/rendered. Original CSV bytes matched their retained hash. A failed export requeued the same document/job and the restarted worker completed both formats.

Mutation run3 completed 25 controls, including three harmless survivors and targeted payroll/cutoff/deletion/estimate/member failures. A separate new OWP picker privacy mutation is running. New focused picker/form/contract checks passed 30 tests. Full candidate QA, production browser provenance/captures, GitHub CI and release/demo landing remain required. Package/changelog now identify the 0.46.0 candidate; no release tag exists yet.

Owned Documents worker final session10294 logs to /tmp/openplan-contract-worker-final.log. The dev server remains :3246 until final production build. New fresh stack is contract-verification-fresh on58521/58522; populated upgrade stack is contract-verification-upgrade on58421/58422. Main is still clean a0aed946. All fixtures remain synthetic and isolated. Worker environments were temporarily linked read-only from their installed interpreters to run all52 suites; those temporary links were removed afterward.

Final candidate QA first found release bookkeeping omissions: four current-release markers still named v0.45, the release migration inventory had not advanced, and two SQL-only cutoff timestamp columns needed explicit inventory classification. No review dates, frozen readiness versions, capability statuses or scientific evidence were changed. The registry/package assertion now compares the two real version sources instead of pinning each release in a test literal. The 17 focused release/direction/schema checks pass. Four separate controls prove harmless release comments survive while a wrong migration count, missing column accounting and mismatched registry version fail. The following full QA rerun and GitHub checks remain separate.

## Final production acceptance checkpoint

Final current-source QA passed on 67345968: lint, deadcode, 13,293 tests, zero reported dependency vulnerabilities and webpack build. The fresh production owner/member journey passed at desktop and 390px. My Work navigation, keyboard tab activation, approved member read-only state, populated private report/download refusal and exact downloaded file comparisons passed. Fresh owner console and member page-error review were empty. Evidence and explicit blind categories are retained in VERIFICATION.md and evidence/production-browser.json.

GitHub current-source RLS, restore drill and all three script/worker jobs passed; QA and shuffled test order are still running. Release/tag/demo remain pending. The main/demo database remains on v0.45. A private logical database backup is retained at ~/.local/state/openplan/backups/pre-v046-20260908/postgres.dump (SHA256 2c68d4916a75e1237ae9b86b7a4d681c56d5d3d1a7d77b8d82234d6e2cf70f); this is not a complete database-plus-storage restore point. The supported safe demo updater retains prior runtime/code/configuration separately during promotion.
