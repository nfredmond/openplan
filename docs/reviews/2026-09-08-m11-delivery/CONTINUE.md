# Continue M11 after refreshing browser tooling

Resume implementation and verification; do not re-plan or call M11 complete. Nathaniel supplied the full four-increment plan: (1) agency reconciliation, scoped PM/finance/consultant access, master/task-order controls, received invoices and accounting intake; (2) staff-proposed/PM-reviewed remaining work, calendars/capacity/dependencies/review periods, explained and retained cost/date forecasts; (3) actionable weekly PM review, responses versus approved baselines, My Work/reminders and management exports; (4) settlement, separate deliverable acceptance, continuing obligations, immutable closeout/reopening and accounting handoff. The agency team is the first case, a small consulting practice the second. Human PM observation and independent finance acceptance remain required and absent. Do not shrink the milestone to the work already implemented.

## Start here

1. Read this file and `IMPLEMENTATION.md` beside it, repository `AGENTS.md`, the current V1 contract/roadmap/requirements ledger and release policy. Keep the roadmap the sole queue. Earlier statements of completion are dated claims with limited scope.
2. Use **the tools supplied to the fresh session** to discover the browser before starting a long journey. The old session's node_repl process keeps a deleted browser-service path despite resetting JavaScript. The user's separate assessment is `/home/nathaniel/browser-tooling-assessment-2026-09-08.md`. A fresh session discovered Chrome through the current unified computer-use interface. Do not copy old bootstrap paths, patch generated workers, create old-to-new plugin symlinks, reinstall Chrome or alter permissions. Fresh discovery is not full page acceptance.
3. Worktree: `/home/nathaniel/.local/state/openplan/m11-delivery-closeout-2026-09-08`; application package `openplan/`; branch `work/m11-delivery-closeout`. Main `/home/nathaniel/code/openplan` belongs to another session; do not edit or merge it without explicit handoff. No subagents were used or authorized. Recheck ownership, processes, branch and working changes.
4. Implementation checkpoint **672db9304fd2b1379811616f46ba91f4ac7f2e5f** is pushed. Its production webpack build passed. Draft PR **107**: https://github.com/nfredmond/openplan/pull/107 . No version bump, tag, release or merge has occurred. A later documentation-only handoff commit may be HEAD; serving code is 672db930.

## Owned runtime and isolated databases

- Acceptance URL `http://m11.localhost:3247`. `bash openplan/scripts/ops/which-openplan.sh http://m11.localhost:3247` verified serving checkout and full implementation SHA. Production log `/tmp/openplan-m11-production-672db.log`, tool session 56597; PID was 807307. Recheck PID/cwd before signaling. Stop this owned server before rebuilding `.next`, then restart with `OPENPLAN_COMMIT_SHA=$(git rev-parse HEAD) npm run start -- --hostname 127.0.0.1 --port 3247` from the app. A documentation-only HEAD must not be represented as newly built code.
- Owned document worker restarted on 672db930 source: session 70186, `/tmp/openplan-m11-672db-document-worker.log`, command `OPENPLAN_DOCUMENT_EXPORT_WORK_DIR=/home/nathaniel/.local/state/openplan/m11-document-exports npm run worker:document-exports`. It loads imports once; restart after export changes. The older owned worker 326788 was stopped. Workers 392204 and 3767415 belong to other work; leave them alone.
- Owned calculation worker PID was 2860720; `/tmp/openplan-m11-calculation-recovery-worker.log`. Children load current calculation source. It already recovered a deliberately interrupted job through lease expiry with exactly one output.
- Disposable stack `/home/nathaniel/.local/state/openplan/m11-contract-verification`, DB container `supabase_db_m11-contract-verification`, API 58621/DB 58622/mail 58624. Migration directory is linked to this worktree. `.env.local` selects this stack; never print keys.
- Separate populated upgrade stack `/home/nathaniel/.local/state/openplan/m11-contract-verification-upgrade`, DB container `supabase_db_m11-contract-verification-upgrade`, API 58721/DB 58722/mail 58724. It has copied migrations. **Both stacks applied through 20260928000001.** Baseline, actual-version and physical-time fixtures remain canonically unchanged: `a6b1bfe272ecc1b77c37730603d9a0267878f7ec5a09bb50a0a1803fa42b31ca`.
- Live tests require `OPENPLAN_RLS_LIVE_TEST=1 OPENPLAN_SUPABASE_WORKDIR=/home/nathaniel/.local/state/openplan/m11-contract-verification` (or the named upgrade stack). Do not run against the demo or another unnamed stack. No DROP/reset executed.
- Pending authorization remains unanswered for **only** `NOTIFICATION_KIND_EXTENSION.sql` beside this file: it replaces the work-notification CHECK using DROP CONSTRAINT. It is intentionally outside migrations. Do not apply it or infer approval from the browser discussion. Reminder UI truthfully reports that contract automatic reminders are not installed and email delivery is not configured.

## Recent implementation and evidence

- d3744f24: received-original checksums/bytes in report format 7 and closeout package format 3; continuing obligations must survive reopening or receive new satisfaction evidence. Older issued formats remain readable and unchanged. Current settlement shown separately from legacy cash columns.
- b81ada60: documented cash remains separate from holds/disputes. Overlapping or excessive holds make currently due unassessed, without inventing an allocation or releasing a hold. A new cash test initially sat outside its mock setup, failed shuffled CI, and was repaired in b81d4795.
- b81d4795: external consultant My Work entry, complete paginated scoped reads, unresolved accounting queues, closed-assignment removal from active tasks, forecast-warning links from the real parent. Its CI caught an owner-privilege view plus inventory/changelog/copy omissions.
- 21815c29: caller-privilege participant view over a narrow caller-bound function, anonymous execute denied, migration 27; numeric-looking accounting identifiers stay text; only declared amount/hour columns become numbers. Accounting rows retain full values and stable IDs; long notes link to a readable text-parts sheet. 74 focused tests, six live queue checks, TypeScript/lint; two harmless controls and ten intended failures. **Ordinary CI, RLS and restore all passed for this SHA.**
- 996bd753: cancellable browser-worker forecasts; reviewed calculations bypass local preview and enter the durable queue directly. Worker/error/cancel/unmount/input-change/late-reply tests and real bundled worker execution through a Node message bridge. Synthetic 40 tasks x 20 staff x 180 days produced 144,000 reservations in 771 ms with 68 main-thread heartbeats and exact equality to the direct result. This is not browser bootstrap/CSP or maximum-size UI proof. Production build/TypeScript/lint and 19 focused tests passed; one harmless and nine targeted controls. RLS/restore CI passed; ordinary CI was still running at handoff.
- 672db930: optional `completedReview {startedOn,finishedOn,evidence}` on outside-review schedule nodes, retained separately from deliverable acceptance. Contradictory dependencies and future actuals keep reported dates but withhold supported finish. Database trigger enforces dates/evidence/kind; delivery method hash v4 stales prior forecasts without rewriting them. Corrected completed staff work showing start later than finish. Exact review evidence exports in a separate table. 73 focused tests, six live checks, TypeScript/lint/build passed; two harmless and thirteen targeted controls. Exact CI/RLS/restore were still running; inspect them before any release claim.
- Relevant logs `/tmp/openplan-m11-review-completion-{final,tsc,lint}.log`, `.../completed-review-build.log`, `.../completed-review-live.log`; controls and upgrade receipts are committed beside this file. Historical control scripts may have old mutation anchors; their recorded results prove their dated source, not automatically current HEAD.

## Synthetic agency state to resume in the browser

See `synthetic-identity-fixtures.json`, `IMPLEMENTATION.md` and `/tmp/openplan-m11-acceptance/identity-fixtures.json`. Test identities: m11-owner, m11-staff, m11-pm, m11-finance and m11-consultant at `synthetic.example.test`. Local fixture authentication details remain in the old session/local fixture setup; do not put credentials in committed notes. An existing finance session may still be signed in.

- Workspace `64572b1d-aaf8-400a-84a5-6167cab3e6e3`, Synthetic M11 Agency.
- Project A `e4944a27-8ab2-4ca7-b03f-0915ee7fdd84`; task order A **afbe573d-59cf-46cd-bedb-6661e1452902**.
- Project B `d4f43412-87c8-4fc9-aa20-1de9394cdd2f`; task order B **445a07ac-6ecd-488e-b3eb-a57e9a682afc**.
- Master `68b48c54-320b-4246-8748-c818bad49006`: original 1500 ceiling refused concurrent proposed authorization totaling 2250; separate approved master amendment to 2500 then allowed B. Task A current baseline 4: received/purchaser billing, fee 1250, internal cost 800, 70 hours, October 15 deadline. B baseline 1: fee 1000/cost 500/10 hours/October 15. Proposed changes never increased authorization.
- A has six approved payroll/time sources, 60 hours/USD 600, plus the single original expense USD 25. Total incurred **625**, underspend **175**. Payroll sources retain original CSV SHA and draft/approved versions. Finance imports exposed own hours but no private payroll amounts/notes/files to staff. All data and sources are labeled synthetic; no pay rate/legal rule is inferred.
- Shared capacity: 8 h/day; A reserved 4 plus B 5; September 9 unavailable. PM saw outside hours only, without B's confidential task/schedule titles. October availability was unassessed. Forecast 5 retained the overload, missing finish and fee warning. Staff then reported zero remaining effort and actual work finish September 8; PM accepted exact version 6 with zero remaining cost/billing. Forecast 6 had incurred 625, remaining 0, expected outside review October 2–3 and a supported October 3 finish. **No completedReview has yet been entered into this browser case.** New method hash now makes those old forecasts stale, as intended.
- Deliverable `16afaa5a-08b4-4b1e-9bc3-4d638f66e633`: submitted, returned, resubmitted and explicitly accepted in four separate PM browser events. First document `fe997388-cad2-4656-bfe3-c2ff452212c4`, corrected document `69d698fe-208f-4419-b18c-36847fa5cf02`; exact file SHAs in implementation notes. The corrected file retains the open October 15 records-audit obligation.
- Received invoice **fc6ae808-beef-463a-abb5-a233c2a6af9e**, SYNTH-VENDOR-01, USD 25: submitted 1, returned 2, corrected submitted 3, reviewed 4, finance-approved 5, matched to the original expense version without another cost. Original file receipts are retained. Accounting import reconciled the same source; no second posted transaction was created.
- Eight settlement events: retention hold 5; dispute 3; payment 10; credit 2; refund 1; hold release 5; dispute resolution 3; payment 14. **25 - 24 - 2 + 1 = 0**, no remaining hold/dispute. Reconstruction receipts are committed.
- Closeout revision 1 **b40809c2-826d-429d-9dcf-eaf87c7513da**, format 2/hash `4ee477076ff7863ed4e987fd624692de13d50f50422d0ca4d38db37ac960cb67`. Browser interruption before commit retained the form; retry saved exactly once. Separate revision 2 reopened it, retaining old hash/acceptance and the continuing obligation. Revision 3 **ad939fa2-30a0-4e7b-b878-507e6f409cc0**, format 3/hash `2bf6c1ab0e6734da23230bf20abeaf28d4c3d0fed0897725cd790611cdb9d6c6`, closed again at 625 incurred/175 underspend/zero open balance/one open obligation. Saved with keyboard Enter at 390px; DOM had no overflow. **Revision-3 UI downloads and screenshot failed at the plugin boundary.** Assignment A is currently closed; further schedule or financial changes require a documented reopening revision.
- Issued report **b54a3c9a-b9ad-40a7-887c-98432928fcfa**, format 6/hash `97a138d6459a378e92fbb9d269eda4c7ea3b32f510163a135570e748392b6caf`. Existing UI downloads in `/home/nathaniel/Downloads/contract-snapshot-b54a...{pdf,xlsx}`: PDF 123 pages, workbook 44 sheets. They remain unchanged; their layout/meaning defects motivated later format-7 changes.
- Original closeout JSON/CSV exist in `/home/nathaniel/Downloads/closeout-b408...{json,csv}`. `scripts/verification/verify-m11-agency-closeout.py` independently reconstructs money, version attribution and PostgreSQL JSONB package hash; `prove-m11-closeout-reconstruction.py` retained one harmless and four failure controls. This is engineering reconstruction, not independent human finance review.
- Unissued local format-7 renderer checks `/tmp/openplan-m11-acceptance/unissued-format7.{pdf,xlsx}`: PDF 133 pages and workbook 48 sheets. Current cash/closeout positions and full accounting-text parts were inspected via PDF/LibreOffice. Committed images explicitly say unissued. **These are not newly issued reports or browser downloads.** They predate completed-review evidence in the synthetic data.

## Finish the remaining work

1. Discover the current browser API. Identify the served build, reload the actual navigation, inspect current console, and restore desktop dimensions after mobile testing. The old acceptance tab was `1406501167`, finance on A Closeout, last emulated 390x844. Tab identities may change; inventory first. Do not attach old stalled 127.0.0.1 tabs or touch other projects' tabs/accounts.
2. Recheck consultant entry and own invoice correction through My Work, accounting review routing, inactive/closed assignment removal, linked warnings, preview worker cancel/error/input changes, and completed outside-review actuals. Use desktop/390px/keyboard and actual entry navigation. Retain identified screenshots and source/console/interrupted-save evidence. Earlier acdf desktop/mobile evidence applies only to that older build.
3. Download revision 3 JSON/CSV and a newly issued current PDF/XLSX through the real UI, inspect them and independently reconstruct stable identifiers, source hashes and totals. Re-download revision 1 to verify original bytes unchanged. Test document-worker interruption/retry, beyond the already proven calculation-worker recovery.
4. Complete B's remaining-work/forecast case and demonstrate A closure releases shared reservations, not merely the read-only/RLS proof. Reopen A only if explicitly needed for the synthetic continuing work/review-date case, retaining every prior obligation and hash.
5. **Small consulting-practice acceptance case is not yet built.** A bounded backend test was being prepared, not executed: fixed-fee gross billing 1000 separate from incurred cost 200; partial payments/retention/dispute/credit/refund/correction to zero, commitment release, accepted deliverable, 300 valid underspend, one continuing obligation, immutable reopening and exported reconstruction. Couple real `normalizeContractCommand` to real SQL RPCs inside one disposable transaction, then do a separate browser case. Existing tests cover individual pieces, not the full second journey. A disposable-psql helper draft exists only at `/tmp/openplan-m11-live-rpc.ts`, untested and not imported into source. No pg package was installed and no practice fixture was persisted. It can be revised or discarded; do not call it evidence.
6. Remaining technical boundaries: maximum-size preview/calculation memory/serialization and responsiveness (144,000 rows is the measured case, not 200 x 100 x 731 maximum); source-cutoff timestamp versus transaction commit/visibility race; document-worker retries; exact-head CI and recovery. Capacity cache/aggregation optimization was considered but **not implemented**. Do not widen or remove evidence gates to call these solved.
7. Resolve the pending reminder constraint authorization only when an actual answer arrives; maintain truthful delivery-configuration notices meanwhile. No external outreach or actual agency approvals are authorized.
8. Update the roadmap, requirement-to-evidence map and limitations with final scoped results; inspect GitHub CI separately; keep one dependable main and coordinate its ownership before landing. Release useful verified increments proportionately, preserving the rest of M11. Keep human agency PM observation and independent finance acceptance explicitly open. M11 closes only with the full implementation and required human evidence.

The current checkpoint is safe to resume. Browser-tool recovery is the reason for switching sessions; it is not a claim that remaining engineering or M11 acceptance is finished.
