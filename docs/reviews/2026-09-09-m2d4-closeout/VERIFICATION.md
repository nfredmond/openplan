# M2d.4 closeout review start

Release disposition: included in [v0.48.0](../2026-09-09-v048-release/VERIFICATION.md). The dated checkpoints below retain their original findings.

This increment begins the next roadmap priority after v0.47.0. It is not full year-end closeout or approved carryover. Application build `8bd928f41822fbcccd44206b2e6df8096cf3cca1` extends the existing owner/admin OWP reporting and reimbursement page, without new tables, APIs, costs, model behavior or authority writes. Work is isolated in `/home/nathaniel/.local/state/openplan/m2d4-closeout-review-2026-09-09` on `work/m2d4-closeout-review`.

## Behavior

Choose an issued management report to review its exact baseline, source cutoff and cumulative costs. The review uses that one retained report rather than summing cumulative reporting periods. A repeated source entry, a working preview or a reimbursement snapshot refuses review. Claim rows use each claim's current packet identity. Earlier versions remain evidence and never add to the current request. Baseline ID/hash, currency and claim identity must match before presenting a retained request for the selected baseline.

Acceptance does not establish cash. Outstanding claim balances and commitments remain unknown because their settlement links are not implemented. Refunds, approved carryover and successor mappings remain unassessed. Current progress and remaining estimates identify work requiring next-cycle decisions. Missing estimates remain incomplete. The review does not close a period, approve new work or copy physical costs.

Private JSON export includes the exact selected source report, review result and loaded reimbursement history, including retained claim events. Its status is `unapproved_closeout_review`. It is a local review file, not a newly issued or approved Documents artifact. A history read failure removes the review and export until history reload succeeds. Existing reporting/reimbursement owner/admin authorization protects the source records; no new read route or service-role query was introduced.

## Verification

Twelve focused behavior/UI tests and the four existing reimbursement tests pass. A harmless comment mutation survives. Twelve targeted faults fail for their stated reasons, covering old packet substitution, baseline ID/hash, currency, claim identity, acceptance treated as cash, repeated cumulative sources, unissued snapshots, failed history represented as empty, stale review after reload failure, export losing its source report, and exposing the private review link to staff. Source edits are restored after every mutation. This check cannot assess absent accounting records, real authority, agency usefulness or database isolation beyond the unchanged existing access boundary.

The first lint command was mistakenly run from the repository root and found no app files. The corrected app-root lint and focused tests passed; the failed invocation is not counted as verification. The first full QA run caught two added terms in the plain-language count guard. The new copy was simplified without changing the guard. The next run passed all 13,440 tests but TypeScript rejected two overly narrow inferred fixture types. Those fixtures now declare the existing PeriodReport type. The owner/admin-only link also gained a role check and two tests, with a harmless comment survivor and a targeted staff-visibility failure. Existing worker environments are linked from the canonical checkout so the final local run includes the same worker-availability tests as the release run. The final full QA and browser acceptance below establish this bounded preparatory increment for direct main merge. No draft PR is required or authorized.


The [full local QA receipt](qa-result.json) records exit 0 for application build `8bd928f41822fbcccd44206b2e6df8096cf3cca1`: lint, dead-code analysis, 13,442 tests in 1,229 passing files, production dependency audit with no findings, and webpack production build. 221 tests in 32 files remain explicitly skipped. The unchanged schema, workers and role-scoped server readers retain the v0.47 isolated database/worker/upgrade evidence; this read-only addition has no migration or worker change.

`which-openplan.sh http://127.0.0.1:3258` matches that full build, package version and isolated checkout. [Browser receipts](browser-results.json) start at landing/sign-in, pass through Overview, Programming Cycles, the synthetic shared-cost program, Administer a reporting period and Closeout review. At 390px the existing module chooser is entered by keyboard. Both widths save the private JSON through keyboard Enter and inspect the exact baseline/hash and 29.00 corrected request. Outstanding balance, refunds and carryover remain null. The saved review retains both original and corrected packets and all claim events. Its source report is unchanged after the journeys.

A scoped 503 history failure removes the review/export. Clearing the task-owned route override and reloading restores the exact same downloadable bytes. Both desktop and narrow exports have SHA256 `2ec8f7da103524bf2a60015a8d3521be93507344f44e97157487cd95b4e7fc7d`. The console contains only the deliberately injected endpoint's 503, recorded with its URL; there are no unexpected warnings or errors. The isolated browsers and route overrides were closed. [Desktop review](closeout-1440.png), [desktop claim](claims-1440.png), [390px review](closeout-390.png) and [390px claim](claims-390.png) were visually inspected. Text wraps, navigation and controls remain usable, and document width equals viewport width. The initial desktop claim capture stopped at its already-visible heading; the final capture scrolls that heading to the center so the actual claim is inspectable.

[HTTP access checks](access-results.json) independently confirm owner 200, anonymous 401 and foreign-workspace owner 404 for both reporting and reimbursement inputs. The selected retained report still matches the exported report exactly. Component role tests cover owner/admin versus staff navigation; this is not a separate native staff login journey. No database fixture or authority was changed.

## Remaining M2d.4 work

The roadmap's complete closeout outcome remains open. Linked claim cash receipts, commitment discharge and refunds need retained reconciliation and responsible approval. Carryover needs approved predecessor/successor mappings, preserving unfinished obligations in the old cycle without creating a second physical expense or claiming prior approval for new work. Closure and authorized reopening must preserve the exact old baseline and recover interrupted commands. This preparatory review deliberately creates none of those decisions. Human agency usefulness, prescribed forms and M11 human acceptance remain open; the reminder constraint proposal stays untouched.

## Continuing locally

Port 3258 serves application build `8bd928f4` from the isolated closeout checkout. Evidence-only commits afterward do not change the application. Port 3257 remains the separately identified v0.47 acceptance build. Both point to the named disposable M2d.3 verification database, not the demo. Local scratch scripts and synthetic private exports are in `/home/nathaniel/.local/state/openplan/m2d4-closeout-evidence-2026-09-09`. Do not print or commit the credential fixture. Follow Nathaniel's direct-main workflow without draft PRs; inspect CI separately from push success.
