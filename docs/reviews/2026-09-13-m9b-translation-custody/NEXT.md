# M9b translation custody, September 13, 2026

> A reproduced translation ownership defect takes priority. Read [the scope correction](../2026-09-13-translation-scope/VERIFICATION.md) before continuing this prototype.

v0.57.0 is published at `9cfd8aaf146bedb0946d9f56b77d9fb5be36c3bc`; all final release CI passed. [Publication evidence](../2026-09-12-m9b-response-writes/PUBLICATION.md). This next increment is a tested SQL prototype, not an installed migration or finished application feature.

Owned checkout: `/home/nathaniel/.local/state/openplan/engagement-response-writes-2026-09-12`, package `openplan/`, branch `work/engagement-response-writes`. No owned app server is running. The original `/home/nathaniel/code/openplan` checkout remains clean on local main at `de01ce9b176d31752af44e0afd0245f505c9ae1f`; do not confuse it with remote main or the owned checkout. The separate demo is unchanged. Refresh ownership and browser identity before implementing UI work.

Continue directly toward the complete v1 contract. No PRs or human-review release gate, no subagents, local/free operation, and preserve the pending reminder constraint. The current product-direction check passes with unchanged historical review dates. The roadmap remains the queue.

## Corrected gap

Do not claim that all prior translation wording disappears. capture_engagement_configuration already captures campaign/category/published-question/active-option translations in immutable content-addressed configuration versions. A transaction on named disposable supabase_db_openplan-restore-target-2026091050, database postgres, preserved original and corrected campaign-title translations after deletion of the current row. That is a positive existing-custody control.

The same probe established zero configuration versions for original/corrected/deleted close_loop_entry translations. Captured configuration translation objects omit machine_model and created_by. The translations route overwrites the current row on operator save and machine publication, changes provenance and clears model on acceptance, and deletes on withdrawal. Existing response history captures the response row, not related translation rows. The translations panel exposes no version history. Source hashes exist, but do not reconstruct unknown old source text or original authors.

Evidence: translation-custody-probe.sql and .log beside this note. It used synthetic text in an otherwise unused qaa locale only inside BEGIN/ROLLBACK, with timeouts and refusal if any such row already existed. No persistent fixture or schema change was made. This is SQL custody inventory, not browser acceptance or a claim about all export archives. API audit events and exported historical configuration snapshots must retain their existing scope.

## Reuse and delivery

Extend engagement_content_translations and campaign-translations-panel.tsx. Reuse the response-history immutable full-row snapshot/checksum/private-reader pattern, retaining original machine provenance before acceptance, corrections and removed copies. Never rewrite existing configuration definitions. A migrated current row is an observed baseline; earlier author/source words remain unknown unless an exact retained record proves them. Preserve row identity and address, record actual authenticated actor separately from the current author field, and keep withdrawal/recreation histories distinguishable. A private read must include removed targets and cannot depend on the current inventory alone.

Then replace translation upsert/update/delete writes with scoped, exact-version atomic writes, durable request receipts and reasoned review. Use PT409 for expected conflicts and PT503 for unconfirmed receipt state, never manually raised 40001. Test actual installed PostgREST. Reuse recovery UI behavior without erasing current translation fields, losing suggestions or spending again on retry. A model generation and publishing its exact generated wording are separate resumable stages; request receipts must not rerun a model silently. Source changes during translation must be detected before publication. Accepting machine wording retains its origin and separately records the staff decision that changes the public disclosure.

Tests need live authority and actor checks, whole-batch atomicity, concurrent writers, source edits, original/corrected/deleted/recreated custody, >1000 complete private reads, anonymous/foreign/viewer restrictions, source cleanup, interrupted retries and harmless/targeted mutations. Identified desktop/390px keyboard journeys must reach the editor from real navigation, preserve the exact old translation after correction and withdrawal, verify public disclosure and inspect console/screenshots. Translation history is only one remaining M9b boundary; source-to-decision linkage, other early roadmap priorities and the complete v1 contract stay open.

## Rollback-only foundation prototype

translation-history-candidate.sql implements full translation-row baselines and immutable snapshots, separate authenticated actor, observed legacy baselines with unknown historical actor, acceptance/correction/removal events, identity/address preservation and a complete private reader. It is not an installed migration and has no app UI or conflict/retry write path yet. Its next-step design remains above.

prove-translation-history.py applies the candidate only inside a transaction on the explicitly named disposable source stack, seeds a synthetic pre-upgrade translation and rolls back every case. The final baseline and harmless comment pass. Removing retained machine attribution, making the read policy public to authenticated actors, permitting history updates, recording no-op changes and truncating the reader to 1000 rows each fail for the stated reason. The positive journey includes 1005 corrections on a recreated identity, owner/foreign/viewer/anonymous reads and original checksum retention. Checksum reads are SQL-level; actual HTTP serialization, route/UI custody, concurrent writes and full integration gates remain untested.

Initial test errors are retained: a missing SQL separator in the inventory wrapper; an overly broad model-removal mutation that instead tripped sequence validation; a user predicate removal that still survived because membership-table RLS refused the foreign actor; and an attempted viewer fixture that the existing last-owner guard correctly refused. The final viewer is a separate synthetic auth user inside the rolled-back transaction. A previously empty baseline test now has an explicit pre-upgrade row. No current membership was changed persistently. Final inspection found no prototype history table and no qaa/qab probe translations.

## Reproduction and next edit

The two Python runners require OPENPLAN_TRANSLATION_PROBE_EVIDENCE to name an output directory outside this repository. They target the explicitly named disposable fixture stack in their source and refuse occupied probe locales. Run only while its fixtures are owned and no other session uses them. They install provisional definitions inside transactions and roll back after each control or failure; they never reset a database. Committed reports are outcomes, not a fresh rerun. Initial diagnostic logs remain in the private response-write-probe-20260913 directory. While separating source and output paths, the history runner briefly reused its source-directory variable for a SQL string; that wrapper error occurred before database access and was fixed before the final rerun.

Read the candidate SQL and extend it into the next additive migration only after completing the missing write/recovery design. Keep SQL source-word uncertainty explicit. Add a typed verified reader and private translation-history access in the existing panel, including removed fields. Then implement exact-version request receipts, source revalidation and reasoned save/accept/withdraw actions with interrupted retry and full browser acceptance. Migration installation, application integration, concurrency and upgrade/restore evidence are still outstanding. Do not ship the prototype alone as completed translation custody.
