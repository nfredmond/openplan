# Shared campaign project handoff, September 6

The baseline retained GeoPackage included an approved public comment for the lead project but omitted the same comment for a second covered project. The same live fixture now exports that comment once for each covered project and excludes it from an unrelated project. Both direct GeoPackage downloads and retained evidence bundles use the same paged coverage and privacy reader. Private notes, pending comments and unpublished campaigns are excluded; no comment text or submitter fields are copied into geographic output.

The campaign's Record section now offers each permitted covered project as a report destination and shows project names in report history across that coverage. It preserves the actual campaign lead in source provenance. Failed coverage reads withhold creation rather than pretending the campaign stands alone. Ordinary standalone and lead-project reports retain their existing paths. The existing project campaign list and explicit campaign report generator already supported shared coverage; contrary claims about those paths were disproved and did not justify rewriting them.

## Evidence and actual limits

- `shared-proof.json` identifies the isolated checkout, baseline, live source hashes and generated artifacts. The running server's old baseline stamp alone does not identify these changes. No other service or acceptance checkout was changed.
- Six focused suites passed 79 tests. The whole suite before the last disclosure/date checks had 13,116 passes and two direction-review failures for the uncommitted substantive changes; it was not called green. Lint, TypeScript and dead-code commands passed separately. Knip reports existing unused exports/types; its zero exit is not a claim that none exist.
- One harmless comment survived; eighteen consequential mutations failed on the intended assertions (`shared-mutations.json`). The fake pages are capped at one row, assert selected fields, scope both joined and lead queries, and exercise missing dates, conflicting publication states, partial reads and privacy. These tests do not prove database RLS or a transactionally consistent snapshot during every concurrent update.
- Separate local owner/member/outsider clients exercised real authorization. Both members retrieve the report HTML/PDF, retained ZIP and direct GeoPackages; the outsider receives 404 and sees no report rows. `live-shared-artifacts.json` records results. The ZIP's actual embedded GeoPackage was inspected with SQLite. A public and a private record plus a pending record were deliberately distinguishable; only the public approved record was present.
- The real campaign PATCH moved coverage from the second project to the third; geometry followed it on repeated reads. Coverage was restored and reread. Stored reports target the two intended projects, each with the same explicit campaign ID (`live-shared-coverage.json`). These were clearly labelled synthetic software fixtures, not agency data or public participation.
- Actual navigation: Engagement catalog → campaign → Record → select second project → create report → generate HTML and PDF; repeat lead-project creation with Enter at 390px. Follow the report to its project → Evidence → prepare/review/freeze a bundle. Desktop and mobile controls and keyboard focus were inspected. The direct export now discloses automatically included public locations and labels its on-page inventory as core project layers.
- **Browser download acceptance remains incomplete:** clicking PDF and retained ZIP links reached Chrome `ERR_BLOCKED_BY_CLIENT`. No browser safety setting was bypassed. The authorized local HTTP routes separately returned readable bytes. This proves server retrieval, not completed browser file delivery. The covered-project retained GeoPackage was inspected through the real generated-file loader; only the lead project's full retained ZIP was frozen through UI in this exercise.
- All eight generated PDF pages were rendered and visually reviewed. Text was readable, with several cards split across pages and substantial generic project context preceding the engagement summary. The summary contains internal-source counts but no private or pending comment bodies; it is a staff report, not a public comment appendix. No PDF polish or public-usefulness claim is made.
- Campaign/project history again advised refreshing a just-generated report while workspace/report views called it current. This reproduces the existing KI-2026-09-05-074 inconsistency; the change does not close it. Existing PDF pagination limitation KI-2026-09-05-032 also remains.
- Screenshot requests sometimes timed out; one clipped mobile capture magnified/cropped the viewport despite correct DOM dimensions. Full-viewport capture established the control fit. Rejected captures remain in the private archive and are not mobile acceptance evidence. The original 2648×1324 viewport was explicitly restored after checks. The final console query returned no warnings or errors; this is limited to the captured tab logs.

## Errors corrected during verification

The page regression initially looked for a real selector inside a stubbed child. It now asserts the coverage props at that seam; a separate real component test and browser exercise prove the selector. The coverage duplicate comparison initially used JSON property order and refused identical rows; primitive-field comparison and reversed-property fixtures corrected it. The mutation report initially failed to recognize Testing Library's `Error: expect(element)` as an assertion; inspection and rerun confirmed the intended disabled-control failure. A raw test UPDATE of the coverage join was denied because that table is maintained by the existing insert/delete route; the actual PATCH route was then exercised successfully. A scratch artifact reader guessed an unavailable ZIP library; it was corrected to the project's installed JSZip. None of these failed attempts is counted as passing evidence.

The earlier My Work checkpoint's remote QA failed because its plainer copy reduced a counted word without lowering the copy baseline. This checkpoint banks that reduction and the removed generic report wording. It does not raise any jargon allowance.

## Remaining scope

Coverage paging fails closed at the existing safety ceiling. Report-awareness summaries retain their existing capped reads; they are not a complete report inventory. A stale target removed from refreshed UI props is refused, but no new server-side transaction, report POST idempotency, or blanket ban on citing other permitted same-workspace evidence is established. Grant and assistant grounding still need their own shared-coverage review. Amendment action identity, complete engagement practice, OWP preparation, operations acceptance and all other v1 obligations remain on the sole roadmap. This checkpoint does not release v0.44 or replace its nine complete and three partial journeys.
