# OWP preparation increment — 2026-09-06

Status: initial preparation foundation implemented and tested; full preparation acceptance remains open; not the preparation acceptance boundary, an agency work product, or a release claim.

Development checkout: `/home/nathaniel/.local/state/openplan/handoff-development-2026-09-06`, based on `2614293b`; owned dev service at 127.0.0.1:3215. Main/walkthrough services were not restarted. Private evidence and public downloaded originals: `/home/nathaniel/.local/state/openplan/housekeeping-2026-09-06/`. Public sources and page findings: [source intake](OWP_SOURCE_INTAKE.md).

## Implemented boundary

Programs is the program-cycle home. Its new Work program entry supports OWP, UPWP and other agency work programs, responsible agency and authority, predecessor/amendment/comparison/reference PDFs through existing Documents, original checksum and PDF page custody, deterministic EDCTC source review plus manual citation/correction, carry-forward decisions, proposed tasks/products, staffing narrative, schedule, project links and proposed revenue/cost lines. No AI account is required. Imported approvals, owners, dates and financial totals are retained as source information, never promoted into live assignments or spending authority.

The two additive migrations provide append-only sources and revisions, workspace read policies, service-only save/attach functions with independent actor membership checks, request idempotency, stale-save rejection and a frozen source ID inventory per new revision. Retained sources prevent deletion of their original document. A tab keeps its unsaved proposal and pending request ID for recovery. PDF, readable HTML and XLSX export a specific saved revision. The workbook has typed values, formulas, source identifiers, source coverage and an explicit external-edit boundary: existing rows can be reviewed; structural changes require a fresh export from OpenPlan.

The source inventory correction is a second migration, not a fabricated backfill. The first two local development revisions predate that inventory. Their directly cited text remains readable with a warning; PDF/XLSX are withheld. Revision 3 captures both retained source IDs. Timestamps were deliberately rejected as a snapshot substitute because transaction start times do not establish attachment/save ordering.

## Findings and evidence so far

- Real navigation: Programming Cycles → New program → Other / OWP / UPWP → program → Work program. Dedicated fixture program `31c2ce11-8afa-4720-a426-c69fa684a92e` is explicitly an exercise using public EDCTC records, with no agency approval or affiliation.
- Both original PDFs uploaded through the visible chooser and source-retention action. Predecessor checksum `ed7f756c09a5808682957f56845fd19fd847e7f2af409e1098f37b26b8af3bd8`; final comparison `8502d3b405257992912d867eeafe97a23b4ae1a8c798a36e7efa872d18ac8a11`. Source counts are 16 and 14 recognized work elements.
- Extractor defects discovered, preserved and fixed: section divider mistaken for an element; WE 122 reordered table text; plural Objectives absorbed into four titles. New extraction is `edctc-work-program-v2`. Earlier attached v1 extraction is immutable and was not overwritten; four proposed titles were corrected manually in the actual UI. Their missing objectives still need review. Source reprocessing/versioning is not implemented.
- Revisions 1–3 were saved through the UI. Revision 3 has 16 predecessor elements, 15 unresolved carry-forward decisions, one explicitly proposed WE 100 revenue line of 76,642 and one missing cost line. This incomplete exercise is not a reconciled agency program. Missing amounts remained null in the stored record and workbook.
- Desktop and 390px inspection reached proposal editing and saved revisions. Mobile recovery text was clipped; the button now wraps and has a visible keyboard focus ring. Post-fix evidence: `handoff-browser/owp-mobile-save-after-wrap.jpg`.
- Native date input automation initially changed the displayed value without committing React state. Save correctly refused empty dates. Native keyboard interaction committed the dates. This was an automation finding, not evidence that a valid saved date disappeared.
- Unit coverage exercises strict proposal validation, cent reconciliation, indirect allocation treatment, source extraction/reference checks, proposed/source separation, text escaping, typed cells, source coverage and export formulas. Access tests assert actual projections and pagination. Live PostgreSQL tests use transactional fixtures and real authenticated/service roles for reads, append-only custody, actor checks, retry/stale behavior and retained original protection. The initial green results were not accepted as mutation proof; the follow-up proof below supplies that separate evidence.
- Private LibreOffice UNO calculation explicitly called `calculateAll`, checked formula errors and exercised six synthetic cases plus live blank/edit changes (eight checks): `owp-workbook-recalculation.json`. Earlier headless conversion merely preserved cached results after an offline cell deletion; that method was invalid for recalculation proof. Its failure remains in the private evidence, and the error was reported during the session. Later export changes require renewed calculation checks.

## Still open

Preparation acceptance is not complete: reconcile all agency-wide fund/allocation/staffing tables; map and explain every continuing, changed and completed element; support multiple amendment/supporting references per element and source extraction versions; finish independent planner/finance review. No actual agency reviewer has participated.

Large/long/scanned document intake still needs durable worker recovery. Automatic review currently has a 10 MiB bound and requires an indexed PDF with known pages; manual correction works for retained indexed PDFs, but is not a recovery path for failed indexing. Interrupted upload/index/attach, physical network interruption during save, and browser downloads still need their remaining exercises. The narrower ignored-response, local draft reload and stale-save exercises are documented below. Corrupted session storage recovery is not yet structurally validated as thoroughly as server saves.

Whole-suite checks, mutation proof and identified-build browser findings are recorded below. Final main integration and remote CI are recorded separately in the direction review. The earlier operator recovery exercise and partial release journeys remain open. Existing B2 browser download restriction KI-087 is not assumed solved by the OWP exporter.

## Follow-up verification and corrections

The full live RLS suite passed 141 checks across 20 files. The first full application test run had 13,158 passes, 118 skips and six failures: recognition of the new delegated write helper, PostgreSQL error codes misclassified as FIPS literals, copy-baseline increases, missing migration release notes and two stale direction-review checks. The helper is now recognized through its real `loadProgramAccess` invocation (which itself is checked against the role matrix); an additional HTTP test verifies that each actual route returns the authorization refusal before privileged reads/writes. `42501` is PostgreSQL insufficient_privilege, not a county branch; the bounded code exemption follows the existing guard design. Copy and operator release notes were corrected rather than increasing the jargon baseline. Direction reassessment still follows the source checkpoint.

The private dev process ended while broad checks ran; its tool session reported exit zero, and the cause was not established. Only the owned :3215 service was started again. The browser's unsaved staffing note survived the service absence and page reload after hydration. The initial immediate DOM inspection preceded hydration and did not establish loss. After recovery, a competing HTTP editor saved revision 4. Duplicate retry requests reused it. A changed retry exposed a real defect: the database's deliberate `40001` serialization error caused the installed PostgREST to retry until the proxy timed out at 60 seconds. The root agent prematurely reported the HTTP exercise complete before the subprocess returned; that report was corrected explicitly.

[PostgREST's upstream issue](https://github.com/PostgREST/postgrest/issues/3673) and [changelog](https://github.com/PostgREST/postgrest/blob/main/CHANGELOG.md) document automatic serialization retries. The third additive migration, `20260906000003_program_work_program_conflict_response.sql`, uses `PT409` for application conflicts. It was applied locally without resetting the database or restarting shared services. The pending RPCs exited after the replacement; no query/process was killed. The browser retained its local text throughout and then showed the intended stale-save message. A private verification-script revision also mistakenly compared revision 3's export hash with revision 4's content hash; correcting the compared revision settled that assertion without changing the product or prior revision.

`live-owp-save-recovery-verified.json` records the completed HTTP exercise at revision 6 (`ff774bd5-5c7e-4c09-97f4-7208bfe7a93e`, content SHA `11ee9e47f39ddc055306ea9b1cb5e568897d74507003d2a7d98dee5ea4d2c80b`): deliberately ignored successful response recovered with two retries and one new revision; changed payload and stale save both 409; invented approval 400; legacy PDF/XLSX 409; revision 3's HTML and hash unchanged. This simulates an unknown client result by discarding the response body, not a physical network interruption. Revision 3 HTTP HTML/PDF/XLSX retrieval passed for owner/member and returned 404 to an outsider; unregistered agent writes returned 403 for all three actors. The actual browser XLSX link returned Chrome `ERR_BLOCKED_BY_CLIENT`, extending KI-087 rather than closing it.

The final workbook link change was rerun in actual LibreOffice calculation: all eight synthetic cases passed. This checks formulas and automatic numeric edits, not unrestricted insertion/reordering of workbook rows, Microsoft Excel behavior or practitioner usefulness.

Mutation proof: [retained results](owp-proof-results.json) contain 50 attempts: 5 harmless survivors and 45 consequential failures. SQL replacements and policy/grant changes were injected inside each test connection and rolled back; shared persistent guards were not disabled. The final precision guard also rejects aggregate amounts that exceed exact cent arithmetic. All mutations were restored by the harness. Its private scripts and per-attempt assertion reports remain under `owp-mutations/`.

The console review retains nine failed background-map/catalog requests at the development-service interruption (22:39 UTC). No later error/warning appeared in the inspected log. This is not a clean uninterrupted journey. PDF revision 3 has 37 tagged Letter pages; pages 1 and 3 were visually inspected for readable text and source references, with whole-document inspection still pending.

## Final local checks and identified production build

Production build `11ecd1667aa5` passed on a sequential retry. The earlier parallel build compiled but received SIGTERM during type checking (exit 143); neither available kernel/user logs nor the bounded test-source search established its cause. The first instance identity check used an eight-character environment label that the checker does not accept; restarting the owned service with the full commit produced an actual MATCH, with its working directory in the isolated checkout. No unrelated service was stopped.

The full app suite passed **13,171 tests**, with **118 skips** across 1,194 files. The final full live suite passed **141 tests**; a further seven-case focused live suite adds an actual foreign-document/project and other-program-source exercise. All three ownership predicates were separately removed in transaction-local mutations and the new assertion failed. The production code was unchanged by this added test. Lint and TypeScript passed, as did the configured dependency audit. Knip exited zero but reported its configured unused-export/type inventory; that exit is not a claim that the repository has no dead code.

Production browser checks at actual 1440×900 and 390×844 reached retained sources and saved revision controls, with visible keyboard focus and no new console warnings/errors after the production start. A desktop image immediately after clearing a stale per-tab viewport override caught the navigation transition; root initially misdiagnosed a missing page container. The settled measurement showed panel left 272px and navigation right 256px, and the inspected screenshot confirmed clearance. That claim was corrected without changing the product. Evidence: `handoff-browser/owp-production-11ecd166-desktop-settled.jpg` and `owp-production-11ecd166-mobile.jpg`. Earlier mis-sized/transitional captures are retained and are not desktop pass evidence. The HTML proposal opened through the actual revision link. The XLSX browser refusal remains KI-087.

The 37-page revision-3 PDF has no extracted words outside page bounds; this does not establish every page's visual layout or complete source reconstruction. Pages 1 and 3 were visually inspected. Production HTTP retrieval of that revision's HTML/PDF/XLSX was separately exercised for owner/member and outsider in `live-owp-production-artifacts.json`.

The proof file now contains **54 attempts: six harmless survivors and 48 consequential failures**. Earlier first-batch totals above describe their historical batch only. None of these checks closes full M2d.1 or substitutes for a separate practicing planner/finance reviewer. KI-088 records that boundary without creating another development queue.
