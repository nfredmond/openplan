# OWP preparation engineering verification

The existing Programs workflow now prepares a traceable, editable next-cycle OWP from retained originals, with structured funding/staffing reconciliation and immutable PDF, HTML and XLSX review files. This is a public-source engineering exercise, not an EDCTC-approved program. The scope is M2d.1 preparation; adoption, spending, submissions and M2d.2–4 administration remain separate. Nathaniel and a recruited finance reviewer are not required for this engineering gate.

## Reference result

The retained EDCTC FY2025–26 Amendment2 and FY2026–27 final PDFs remain separate originals. Three later parser versions per original were retained without replacing previous text. Revision4 uses selected v5 extraction identities. Independent reconstruction checks831 source financial/staffing cells,183 tasks/products,30 work-element budget passages with158 whole-dollar amounts,25 proposed allocations,41 costs,13 person-month staffing rows and16 work-element mappings. Source charts, tables, footnotes and page references are included.

Supported proposed funding and expenditure subtotals both equal **$1,649,870.12**. This is not a complete whole-program balance: predecessor200EIR ($43,319.81) and263 ($70,422.53) are absent from the final comparison but have no closeout evidence. Their disposition remains unresolved. Prior salary rows total$661,434.49 against a printed$661,434.50. Actual balances, selected individual labor rates, productive-hour capacity, match applicability and the preceding original/Amendment1 are not established by the retained pair. Prior authority and reference balances do not become available next-cycle funds. WE50's indirect pool is counted through allocations once. The unresolved register retains these limits instead of inventing inputs.

Saved revision4 hash: `52e3a2c82aca68083e1fe49dc99a0523327b9b935203e2fbbb5aef9718653861`. All four saved revision hashes still match after extraction retry, local recovery and source-version selection. Source coverage counts establish recorded passages, not completeness of interpretation; independent original-PDF reconstruction supplies a different check.

## Delivered artifacts and browser

The supported browser received revision4 files through authenticated user-initiated application buttons. Actual arrival, byte counts and checksums are in [delivery-r4.json](browser/delivery-r4.json). The PDF has141 inspected pages. The workbook opens on a selected two-page Print summary; wide input tables are designed for on-screen editing, and entire-workbook printing is not the supported print path. Blue input cells, formulas, units, references and source tables remain editable for existing-row review. Unrestricted re-import is excluded.

Independent LibreOffice UNO calculateAll repaired all543 deliberately poisoned numeric formula caches and matched separately computed expectations. Eight blank/negative/zero/positive quantity cases pass. Eight harmless text controls preserve every formula result. Three isolated guard removals reproduce the previously hidden zero/negative errors. PDF R4 comparison identifies139 unchanged page bodies plus two changed pages inspected separately. [R4 independent report](independent/artifacts-r4.json) retains exact methods, failures and blind categories. Earlier R1–R3 failures remain in the adjacent reports.

The production browser journey started from normal Programming Cycles navigation on identified dcef9a03 at isolated port3218. Desktop and actual390px captures are in [browser/](browser/). Recovery preserved an unsaved manual note across navigation; keyboard Enter saved it as revision4 and downloaded XLSX. Tab focus is visible. An unresolved staffing link opens the contributing row and its enclosing panels. Selecting an older source extraction preserves corrections and presents proposed differences without duplicate elements. The extraction retry reports progress/success while retaining the original. Width and internal fields were inspected, not only an outer-width assertion. Console/exception collection returned no events in the recorded journey; this is not an accessibility-conformance claim.

The earlier navigation download failed with ERR_BLOCKED_BY_CLIENT. The corrected helper fetches the private authenticated endpoint, verifies its displayed checksum and triggers a user-initiated Blob download. Browser security settings, public links and source-document sandbox protections were not relaxed. The browser tool rejected internal-download and local-file navigation, so file opening was inspected with native PDF/LibreOffice tools using the actual browser-arrived files; that separate browser viewer path is not claimed.

## Calculations, recovery and access

Independent arithmetic covers funding vintages/carryover, explicit/percentage match and half-cent rounding, fund periods, indirect/labor double counting, missing rates, capacity/period overlap and duplicates. Fourteen match cases and42 rate/capacity workbooks were recalculated correctly after the initial cached-value method was exposed as insufficient. Seven fund-period cases reject expired/reversed/partial coverage without inventing prorated funds.

Six standalone OCR worker suites include real SQLite and process death after acceptance, retained checksum-bound source recovery with expired URLs, exact undelivered callback replay and injected persistence failures. Additional independent races establish persistence before delivery and serialized callbacks. Export fault injection interrupts after storage but before database custody; retry reuses verified bytes and rejects corruption or foreign cache identity. Live transactions cover legacy active-job recovery, partial index conflicts, exact retries, expired leases, cancellation and immutable originals/revisions. Corrupt nested local drafts are rejected safely and recoverable raw content is retained. These do not simulate physical disk loss, machine power failure or a full production restore.

The full isolated RLS suite passes149 tests. Forty-five actual HTTP checks cover owner/member/viewer/outsider/revoked access to preparation, OCR job status, export status and all three artifact downloads. New source-version tables have dedicated live cross-workspace read/write probes. Eighteen independent rollback-only guard transactions include six surviving controls and12 targeted failures, with complete restoration. Referenced foreign documents/projects/staff/contracts, actor identity and exact save request/revision contracts are covered by the route/SQL suites. Changed guards have independent mutation evidence; network/DB mocks are not presented as live isolation.

## Upgrade and operating boundary

The [independent upgrade rehearsal](independent/upgrade-review.md) exercises supported predecessor→final00001–6 and authentic early-applied00001–4→forward repair00005/6 in rollback-only private schemas on isolated database56322. Both preserve original/source/revision/job/receipt/extraction/export history and produce identical functions, constraints, policies and privileges. Repeating the repair preserves history. Four baseline/no-op runs pass; seven targeted failures include omitted repair, changed historical custody, opened RPC access and null artifact bytes. Null lease/checksum/bytes/path assertions were also added to the repository live suite. This focused prerequisite reconstruction does not substitute for full-stack bootstrap/upgrade CI.

Use the [self-hosting instructions](../../../openplan/docs/SELF_HOSTING.md) and [OCR deployment guide](../../../workers/ocr_worker/DEPLOY.md). Ordinary preparation, Poppler/OCR, calculations and exports need no AI account or paid service. Supervise the existing OCR worker and `npm run worker:document-exports`; retain their private working directories along with database and Documents storage. Long operations run in workers.

Two local isolation errors are preserved prominently in [IMPLEMENTATION.md](IMPLEMENTATION.md): predev initially applied four additive migrations to the existing stack; a later live-test invocation omitted the explicit isolated workdir and created ordinary synthetic test fixtures there. No reset, DROP or destructive rollback was performed. The database-sync guard now refuses mismatched app/stack identities; live tests still require explicit OPENPLAN_SUPABASE_WORKDIR. Temporary synthetic test sign-in material was accidentally printed during tooling; private raw records are excluded from this repository. The owned isolated test stack is to be stopped after final verification, preserving its volumes.

## Landing and separate release gate

Local verification includes the full unit suite (13,142 pass,126 skipped), lint, declared-dependency/dead-code checks,149 live RLS checks, production dependency audit and production build. Retained earlier gate failures include the undeclared direct @next/env import and two missing example settings; both were corrected without weakening checks. The final QA command and exact pushed-main CI/RLS/Upgrade Path workflows must be read together: a push alone does not prove them. Workflow run history is the authoritative final landing record; no tag follows this preparation pass.

Two fresh independent whole-product direction reports and bounded follow-ups preserve the entire v1 contract and the sole roadmap queue. The [original three v0.44 partial outcomes](independent/v044-reassessment.md) remain unachieved: Safety treatment/cost/benefit, model forecast/value and explicit validation checkpoint cap/holdout disclosures. Relevant task definitions and source were compared with the original run. No full twelve-journey rerun or release tag was attempted. Scientific defaults, consumed holdouts and inconclusive findings remain unchanged.
