# Complete OWP preparation

Status: implementation in progress; no preparation or release pass.

User direction removes mandatory Nathaniel or external finance-review dependency for engineering acceptance. Independent agent reconstruction, implementation review, calculation/recovery/access tests and inspected browser-delivered artifacts are required. Actual agency adoption, spending and submissions remain separate human decisions.

Isolated checkout: `~/.local/state/openplan/owp-preparation-2026-09-06`, branch `work/owp-preparation`, base `86cb4aa0`. Existing main services and workers are not owned by this work. Public reference originals remain in the retained housekeeping source directory.

Order: durable Documents/OCR intake and immutable extraction versions; complete source/amendment mapping; structured funding/staffing reconciliation; revision-bound retained exports; independent review and identified-build desktop/390px delivery verification; main integration and CI.

Baseline findings: PDF extraction precedes the document database insert; OCR jobs live in worker memory; undelivered results and downloaded bytes are deleted; callback receipts precede nontransactional application and can acknowledge a retry whose result was never applied. Source attachment re-extracts synchronously and requires indexed page count. Local draft recovery only checks schemaVersion and the elements array.

v0.44 remains separate: retain nine complete and three partial journeys. Reassess safety, model comparison and validation-evidence outcomes before any twelve-journey rerun. No tag authorized by preparation success alone.

## Checkpoint at 2026-09-06 17:47 local

Six standalone OCR worker suites pass. Durable acceptance, retained originals, exact result replay and request identity tests include subprocess interruption. Independent review found and reproduced a terminal-result delivery race; terminal assignment and commit now share the registry lock, and concurrent delivery is serialized per job. Delivery rechecks persistence before HTTP. Independent mutation evidence is in the review scratch directory pending final report.

The isolated application has four additive development migrations applied, with corrected unpublished function bodies reapplied without a reset. Thirteen live work-program/recovery tests pass; 118 related unit tests pass. This is not full acceptance. Remaining work includes full mutation coverage, reference reconstruction through the application, actual exports/LibreOffice comparison/PDF inspection, browser recovery/access/download journeys, comprehensive gates and main integration.

Owned runtime: application port 3217, OCR worker 8587 and the local document-export worker. Isolated Supabase project `openplan_owp_preparation` uses ports 56321 onward. Its files and private logs are under `~/.local/state/openplan/owp-preparation-db-2026-09-06`. A startup-log mistake exposed temporary test credentials in tool output; stop this isolated stack after verification. No other stack or production configuration was changed.

Browser selected through the supported Chrome extension. Landing and signup navigation reached the isolated checkout. No account or preparation workflow verified yet. Generated Next development route types contain a duplicated tail, so the latest type check fails in generated output; investigate/regenerate before reporting a pass.

### Correction: automatic predev migration targeted the existing database

The earlier statement that no other stack changed was wrong. At application startup, `npm run dev` invoked `predev -> db:sync`, whose CLI uses `openplan/supabase/config.toml`, not the app's isolated `.env.local` database URL. It applied migrations 20260907000001 through 20260907000004 to `supabase_db_openplan` as well as the intended isolated stack. These are additive, but this was an isolation failure. Disclosed to Nathaniel immediately on discovery. Read-only audit found zero new document extractions, zero source extraction versions and zero generated exports in the existing stack. No destructive rollback attempted. Existing processes were left running. Own application restarted via direct `next dev --webpack -p 3217`, bypassing lifecycle scripts. A follow-up additive repair migration will preserve correct upgrade behavior for the already-applied versions. Main compatibility must be checked before landing.

## Preparation checkpoint, 2026-09-06 evening

This supersedes the early progress snapshot, without erasing its failures. Implementation remains a development candidate, not accepted preparation or a release. The guarded database sync now compares the application's actual Next environment URL with the explicitly selected Supabase project before migration. Its independent harmless and wrong-target/environment-precedence mutations are recorded in review scratch. Six additive migrations are applied to the isolated stack; the first four were accidentally applied to the existing stack as documented above. Forward repair is additive.

The synthetic public-source exercise reached Programs through normal browser navigation, created a program, retained both original PDFs through the OWP upload interface, completed worker extraction and attached predecessor/comparison sources. The reference proposal was seeded through authenticated application APIs from an independent transcription of the original PDFs. Revision 1 has 16 work elements, 831 original financial/staffing cells, 25 proposed allocations, 41 expenditure rows and 13 person-month allocations. Supported current revenue and cost subtotals both equal $1,649,870.12. Whole-program completion remains unresolved: predecessor 200EIR/263 lack closeout evidence, prior salary rows differ from the printed total by one cent, fixed assets are separately printed, individual cost rates/capacity are absent, and preceding original/amendment-one documents are missing.

Revision 1 HTML/PDF/XLSX files were retained in Documents with immutable revision identity and checksums. Independent review checked all 95 PDF pages and original/exported tables, identifying clipped source-table headers, lost chart relationships, incomplete task schedules and omitted narrative budget/footnote passages. Corrections are implemented for a second revision; its artifacts are not yet inspected. The first source versions and revision remain unchanged. Source extraction version 4 is now retained separately for both originals.

Verification correction: initial LibreOffice conversion reused numeric formula caches and was insufficient evidence of recalculation. The reviewers now force UNO calculateAll and poison cached values first. This corrected method confirms 14 match cases and 42 capacity/rate fixtures, including fractional-cent match rounding. It exposed a real allocation duplicate-count formula defect for blank task identifiers; correction is under independent verification. The original weak-method claim must not be repeated as proof.

Browser file arrival is still unproved. Direct delivery produced ERR_BLOCKED_BY_CLIENT in the supported browser. A relative authenticated redirect did not resolve it. The document-scoped CSP now permits downloads while retaining sandbox restrictions; latest production verification is pending. Browser security policy prohibited opening its internal Downloads page; no workaround of that restriction was used. Cached artifact copies are review evidence, not browser-download evidence.

Latest focused parser/match regression: 31 tests pass. Independent source parser: 21 original-PDF assertions pass, harmless change survives and broken header/splitting behavior fails. Earlier full unit suite exposed guards/audit/inventory mismatches, which were corrected with actual audit/guard behavior and accurate registry entries; comprehensive rerun remains pending. Direction review is stale after substantial implementation and needs real independent reassessment. Production builds 2/3 passed but predate the latest fixes; the dev server's nonhydrating UI is not acceptance evidence. No main integration, CI claim, v0.44 journey rerun or release tag has occurred.

Remaining: latest type/lint/full tests; new identified production build; second-revision HTML/PDF/XLSX independent reconstruction and forced recalculation; complete browser desktop/390px/keyboard/console and actual artifact delivery; expanded live cancellation/access/recovery and additive upgrade checks; fresh direction reviews; main coordination and pushed CI; original three partial v0.44 outcome reassessment. Private exercise records and review scratch remain under ~/.local/state/openplan; they contain test authentication/session details and must not be copied wholesale into the repository.
