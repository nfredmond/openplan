# Final first-week outcome record

## Result and release boundary

The unchanged twelve-journey run on clean, pushed
`648504cf18aa9fc2feb91060b4485e17fef1b709` finished with nine passes and three
partial outcomes. The runner exited 1. Every journey had zero unexpected console
errors. The model-evidence reviewer also recorded four WebGL warnings and one
aborted Mapbox style request, so this is not an entirely clean network journey.
No v0.44 release tag is authorized or created by this record.

| Journey | Outcome | Seconds |
|---|---|---:|
| First-day setup | Pass | 433 |
| Neutral geography | Pass | 468 |
| Project end to end | Pass | 788 |
| Public engagement | Pass | 662 |
| Safety case | Partial | 909 |
| Model comparison | Partial | 1092 |
| Land-use plan | Pass | 1139 |
| GIS handoff | Pass | 258 |
| Workbook round-trip | Pass | 298 |
| Project evidence bundle | Pass | 323 |
| Two-person decision handoff | Pass | 816 |
| Model-validation evidence | Partial | 2358 |

Safety did not establish a construction treatment, cost and benefit case. The
model comparison retained an explicitly synthetic exercise assumption and did
not establish a forecast or value for money. The final model-evidence review
understood the inconclusive scientific result and downloaded the evidence, but
could not explicitly confirm no arbitrary point cap and untouched-holdout
access from the visible checkpoint prose. Independent byte checks do not change
any of those recorded outcomes.

The distributed-loading candidate remains retired unchanged and scientifically
inconclusive. No default, frozen artifact, observation match, acceptance rule,
weight, or holdout changed during this final verification. The next direction
review retains its existing priorities; this failure is not permission to fit a
replacement candidate or reduce the v1 target.

## Correction verified separately

The empty-report false-evidence defect found during Safety was corrected in an
isolated worktree while the full run retained its original checkout and server.
The tested correction build was `15a15e3023a668b6e00a6193d847d7d4262d129e`.
Projects, Reports filters and assistant grounding use nonempty evidence, while
retaining model-only evidence and its original claim label.

Full local QA passed 13,093 app tests and 135 live isolation checks, with 112
app tests skipped. Production dependency audit and build passed. Eight targeted
report mutations and the health-test environment controls failed for their
recorded reasons; harmless controls survived. All 20 corrected desktop/390px
screenshots were inspected, with zero console errors and application writes.
Internal mobile report-card clipping remains queued, not declared corrected.
See [the correction record](V044_EMPTY_REPORT_EVIDENCE_2026-09-06.md).

Exact-commit CI, RLS, upgrade and nightly checks all succeeded on both the tested
15a15e30 build and documentation follow-up
`a80be8db3fa4af919cd4a0ef759aa69df37def7a`. The latter runs were 34026223615,
34026223457, 34026504855 and 34026506247. Final main checks must be read separately.
No complete twelve-journey pass on the corrected build is claimed.

## Independent artifact and browser review

The current model-evidence journey preserved 123 distinct selected artifacts:
42 distributed-loading files, 70 comparable-observation files, four structural
audit/diagnosis files, one older structural diagnosis, three current baseline
files and three study-level files. All were checked against frozen sources or
exact current custody. Repeated generic observation filenames were checked in
their county-scoped retained paths, not against the last browser download with
the same basename.

All 42 distributed filenames and full hashes match the selected on-screen
records and frozen sources, totaling 347,273,156 bytes. Their retained copies
and copied-hash manifest also agree. The incomplete set first failed on a missing
Tulare input. Wrong hashes, byte lengths, filenames and routes were rejected.
For the 70 older-study files, copied controls survived; missing/duplicate routes,
altered bytes, wrong displayed filenames and wrong hashes were rejected.

The current AequilibraE baseline is
`f4ea5d65-92fb-4b85-9c85-b279240601b2`, with custody row
`cac9a54a-38f1-4bac-a3e5-895eabb9abb7`. Its downloaded assessment, basis and input
match their registered hashes and private stored bytes. The assessment says
the count records do not meet the required observation contract, the supplied
counts cover another area, and a same-quantity comparison was not established.
No failed measurement or accuracy claim is inferred from validation not running.
Browser filenames use hyphens while stored names use underscores; the verifier
checks that explicit mapping without renaming original downloads.

All seven final journey screenshots were inspected. A separate front-door
ActivitySim check opened its model with one click at desktop and 390px, with no
errors or writes. Its inconclusive assessment and full hashes remain separate
from AequilibraE. Scoped label/hash captures were inspected. This supplements
the original review and does not turn its partial outcome into a pass. The
existing cramped model columns remain queued.

The model header's zero-run summary counts explicit legacy run references;
the execution panel separately reads two native runs. That misleading distinction
is recorded as issue 084. Missing checkpoint explanations are issue 085. Neither
finding establishes lost output or a changed scientific result.

The Safety PDF's 12 pages, project PDF's nine pages and governed PDF's nine pages
were inspected. The approved bundle and 634-byte receipt match exact stored
custody, identify different preparer and assigned admin, and preserve the returned
predecessor and replacement. Approval does not publish, adopt or validate the
synthetic packet. The native GIS geometries and workbook source values were
independently checked. Print pagination and workbook application rendering are
not claimed as complete.

## Retained local evidence

The immutable run is under
`~/.local/state/openplan/first-week-runs/2026-09-06T08-03-55-299Z/`.
Its raw reports, execution records, screenshots and downloads remain unchanged.
The owned download-retention service was stopped only after the runner finished.

Independent review and verifier output are under
`~/.local/state/openplan/release-checks/v044-2026-09-05/`:

- `FINAL_648504CF_FULL12_REVIEW.md` contains the per-journey review.
- `final-648504cf-model11-*` records source, screen, retained-file and current-run
  checks, including failed controls and their limitations.
- `final-648504cf-decision10-*` records receipt and bundle custody.
- `empty-report-corrected-15a15e30-browser/` records the corrected UI.
- `final-648504cf-activitysim-entry-scoped-browser/` records independent entry,
  assessment and hash inspection.

These checks prove the stated file custody and observed behavior. They do not
establish scientific validation, statewide completeness, national readiness,
legal sufficiency, production deployment, or full first-week acceptance.
