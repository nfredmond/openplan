# v0.36.1 model-truth correction proof

## Boundary

Guided project modeling is complete only when the current baseline and build
runs for both AequilibraE and ActivitySim succeeded, each registered its own
non-empty assigned link-volume artifact and SHA-256, and a ready comparison
snapshot is append-only bound to those four exact outputs. Validation evidence
comes from the same runs. The stored project polygon must intersect usable
Census tracts before enqueue.

## Focused verification

| Check | Result |
|---|---|
| Guided API, sequence, starter, snapshot, and enqueue regression suites | 58 tests passed |
| Migration shape and release ordering | 9 tests passed |
| Worker suites | 48 suites passed |
| TypeScript type check | Passed |
| Product direction release agreement | Passed for v0.36.1 |

## Mutation sensitivity

The harmless mutation changed only one evidence-helper comment. All 15 focused
sequence tests survived.

| Deliberate harmful mutation | Test that rejected it |
|---|---|
| Count a succeeded run without its method-specific artifact | `analysis-sequence-facts.test.ts` and `project-comparison-route.test.ts` failed four assertions |
| Accept a ready snapshot without its four custody links | `analysis-sequence-facts.test.ts` failed the unbound-snapshot assertion |
| Drop the project match between the snapshot, model, and run | `model-truth-correction-migration.test.ts` failed the project-scope assertion |
| Accept a malformed artifact hash | `analysis-sequence-facts.test.ts` failed the exact-artifact count and packet assertions |

Every deliberate source mutation was restored before the final passing run.
