# Post-v1 Plan (written 2026-07-18)

Companion to `2026-07-18-v1-handoff-for-next-agent.md` (the deep reference). This is the sequence. Core judgment: v1's bottleneck is no longer code — it's contact with real planners.

## Part 1 — Nathaniel, this week (no agent needed)

1. **Full demo dry-run** per `2026-07-17-v1-demo-runbook.md`: reset + seed + build + start + `cloudflared tunnel --url http://localhost:3000`, walk `docs/sales/2026-07-17-v1-demo-script.md`, open the public engagement link (`/engage/nctc_demo_sr49_corridor`) from a phone.
2. **Book 2–3 friendly demos** (NCTC contacts, a neighboring RTPA, one consultant). Frame: "I want your reaction." Their feedback outranks any roadmap below.
3. **Email Vercel support**: settle the outstanding balance / downgrade to free Hobby. This is the only blocker to an always-on public site. Supabase needs nothing until deploy day (new free org under the existing account).
4. **Rotate the leaked ORS API key** hardcoded in `transitscore-3d/lib/isochrone.ts` (public repo).
5. **Keep a small Anthropic balance.** All AI is pinned to claude-haiku-4-5; a demo hour costs well under $1.

## Part 2 — Next agent sessions, in priority order

1. **Modeling quick wins** (handoff §7.4, items 1–4, one focused session):
   - VMT KPIs (`daily_vmt`, `vmt_per_capita`) written by the AequilibraE worker → lights up the CEQA §15064.3 memo arc end-to-end
   - Real LODES employment instead of jobs ≈ 0.47×population
   - Worker defects: atomic job claim, private artifact storage + signed URLs, client-side run polling + stuck-run watchdog
   - Worker runnable locally as part of the dev stack (live model run in demos)
2. **FreeChAMP sketch ABM port + calibration scoring** — mode choice for the modeling lane; fix the documented `mode-choice.ts:295` utility bug and validate against NHTS/ACS before wiring; gate outputs sketch-grade.
3. **Cloud deploy** (once Vercel replies): new Supabase free org, `supabase db push`, env vars, GitHub Actions keep-alive cron, AI spend caps. No code changes required.
4. **v1.1 features, demand-driven** (let demo feedback choose): aerial ODM port (from `OpenGeo`), grants.gov sync, BCA engine port (`DOT-Dashboard`), sign-in/sign-up copy polish, engagement downvotes if agencies ask.

## Part 3 — Standing rules for any session

Read the handoff doc first. Non-negotiables: `npm run qa:gate` before every merge; PR flow with `gh pr merge N --merge --admin` (the failing Vercel check is noise from the blocked account); guarded sequential migrations verified by `supabase db reset`; claim-boundary tests stay intact — screening-grade language everywhere until a real calibration story exists.
