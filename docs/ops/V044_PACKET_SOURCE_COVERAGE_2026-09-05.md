# Packet source coverage, continuation evidence

This supplements the earlier September 5 records. No v0.44 tag exists yet and
the complete twelve-job first-week outcome gate is still outstanding.

## Identified browser and downloaded packet

On clean `efd4eb9b55445eb914dbd9ed10cc326857c1c30c`, the ordinary Projects
workflow created the explicitly labeled software-verification project
`9c8df7c4-99cd-4713-80ab-f83c21b589e6`. A searched Franklin County, Ohio study
area led through the visible project Safety link to a real FARS acquisition,
`bfdde201-917a-4256-a1f0-6c38ad2a834a`, with 332 reported and mapped crashes.
No database fixtures or direct API writes substituted for these steps.

At desktop and 390px, supported fatal counts and ordinary crash points remained,
while combined KSI rankings and community burden were explicitly withheld.
Document width was 390px at the narrow viewport. Console errors were zero.

The visible Reports workflow created `b89f01cc-f523-476b-a6fa-857f295c9cd3`,
selected that acquisition, saved metadata, and generated artifact
`13d3fc89-9d47-4443-ba73-b1a1e375ecd9`. No model evidence, approval, treatment
recommendation, or adopted claim was attached. The real browser download is
307,219 bytes, nine pages, with SHA-256
`5d70506c2b3cd979d401f40d5cd73dc63e820b2dd5679ad230cba5a12dbb9fce`.
This hash identifies independently downloaded bytes; the report UI does not
display a matching content hash. Do not mistake its artifact identifier for one.

All nine rendered pages were inspected. Page 8 prints 332 fatal crashes and
explicitly unavailable serious-injury and combined KSI counts. Rankings and the
combined concentration map are withheld. These observations close issue 030,
not the subsequent source-description defect below.

Evidence remains under
`~/.local/state/openplan/release-checks/v044-2026-09-05/ranking-coverage/`:
desktop/mobile screenshots and snapshots, console log, the actual downloaded
GUID file, `fars-coverage-packet.pdf`, and all nine `packet-page-*.png` renders.
The CDP client's `saveAs` looked in a different temporary artifact directory and
failed even though Chrome had downloaded the file. The original GUID file was
copied to the descriptive PDF name. This was a check-path error, not a failed
product download.

## Additional false coverage sentence

The same PDF still claimed its source distinguished fatal, injury, and
property-damage-only crashes. A shared caveat originally written for one
adapter was being used for all incomplete or unknown coverage states. The
earlier filter and ranking corrections did not catch this sentence.

The shared warning now says only that separately counted suspected serious
injuries are not established. Evidence also carries the existing fatality-census
disclosure when, and only when, recorded completeness is `fatal_only`.
There is no geography or source-name special case and no raw-data rewrite.

Four new cases failed against the old implementation. A harmless comment
survived all 24 evidence tests. Restoring the false source-band sentence failed
four cases; removing the fatality-census disclosure failed one; applying it to
every incomplete or unknown source failed three. All mutations were restored.
The final focused run passed 157 tests in four files. These tests cannot prove
upstream source truth, UI reachability, or PDF pagination. Fresh production
verification of this sentence remains required.

## Lesser findings and prior gates

The PDF separates some headings from their content at page boundaries,
including Community burden screen across pages 8 and 9. All content remains
present and readable. Queue this formatting issue without calling the packet
layout flawless.

Saving a study area immediately after selecting a place returned a missing-area
message. The same selection saved after boundary resolution completed. The
screenshots and snapshot preserve the recoverable state; no data loss was seen.

CI 33942018503 and live RLS 33942018499 passed on `efd4eb9b`, before this newest
caveat correction. Its local `qa:gate` passed 12,837 app tests, 132 live-RLS
tests, build, lint, dead-code policy, and the production dependency audit.
Those results are not evidence for the later patch. The previously approved
FARS persistence migration remains applied. Frozen studies, default settings,
acceptance rules, and holdouts are unchanged; the distributed-loading candidate
remains retired and scientifically inconclusive.
