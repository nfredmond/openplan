# v0.41 comparable observation proof

Release-source SHA: `3ef6eff43d0f9dcf70afd8c308b63e819cdb7750`  
App version: `0.41.0`  
Disposition: local proof complete; remote CI and release tag pending.

## Scientific proof

- `scripts/modeling/verify_comparable_observation_study.py 3ef6eff43d0f9dcf70afd8c308b63e819cdb7750` passed every release, registry, matcher, package, audit, bundle, output, basis, assessment, diagnosis, and predecessor binding, including reconstruction from deterministic gzip storage.
- `scripts/modeling/tests/test_validation_instrument_v2.py` passed full-link, multiline, paired-carriageway, tied-road, dateline, unsupported-country, registry-only-source, zero, negative, unloaded, missing-output, lineage, and separate-method contracts.
- The development runner and normal local worker now call the same frozen-file gate. A mutation that ignored changed readiness bytes survived into invalid model output and was killed by the focused sequencing test; the restored gate passed.
- `scripts/modeling/prove_comparable_instrument_guards.py` preserved a harmless mutation and killed centroid-only matching, premature output access, proximity-only tie selection, collapsed lineage, invented vehicle/PCE facts, dropped records, and method averaging.
- All seven audits carry matcher SHA-256 `67256b4a54d5157fcc9b24172bb58f877e4b465b3adc3c4b9736d29e20e18fb4` and `model_output_bytes_read: false`.
- The frozen v0.39/v0.40 directories and `scripts/modeling/validation_instrument.py` have no Git diff.

## Custody proof

- Local migration `20260828000004_comparable_observation_v2_custody.sql` applied successfully.
- `model-validation-instrument-v2-rls-live.test.ts` passed workspace isolation, member write refusal, service-role creation, wrong-workspace/hash refusal, and mutation/deletion refusal for custody and bound artifacts.
- The existing run panel visibly labels a failed custody transaction `scientifically unchecked`.

## Product proof

- The production build passes and includes the authenticated comparable-study download route.
- The Models page retains the v0.40 diagnosis and adds the v0.41 repaired instrument, full hashes, and manifest-selected downloads.
- Reports, assistant evidence, and project evidence bundles carry the v2 custody record and all five hashes.
- First-week job 11 started from the signed-in landing page. Visible controls repaired the test project's missing study area, then launched `v0.41 job 11 genuine baseline Fast Screening 2026-08-28` into the existing-conditions baseline. Run `843f59d4-6ac5-43af-b0ca-d33666c64658` completed all three worker stages successfully and its evidence assessment remained `inconclusive`.
- The published study card was checked at 1440px and 390px with zero page/card overflow and no console errors. Seven visible downloads returned 200 and each response hash matched its downloaded and committed bytes. The genuine baseline evidence panel was also opened from the model detail page.

## Local release checks

- `test:workers`: 49 suites passed.
- `test:rls-live`: 16 files and 127 tests passed against local Supabase.
- `qa:gate`: 1,143 test files and 12,727 tests passed, followed by live RLS, a zero-vulnerability dependency audit, and the production build.
- Local migration upgrade applied without a database reset.
- `v0.41.0` remains untagged until the final proof commit and remote CI pass.
