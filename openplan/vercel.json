{
  "installCommand": "npm ci",
  "buildCommand": "npm run build",
  "functions": {
    "src/app/api/reports/[reportId]/generate/route.ts": {
      "maxDuration": 60
    }
  },
  "crons": [
    {
      "path": "/api/cron/reap-model-runs",
      "schedule": "*/5 * * * *"
    }
  ]
}
