// Placeholder test to confirm vitest setup works
import { describe, it, expect } from 'vitest'

describe('test setup', () => {
  it('should be configured correctly', () => {
    expect(true).toBe(true)
  })
})
